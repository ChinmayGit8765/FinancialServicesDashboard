---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/mvnw
  - backend/mvnw.cmd
  - backend/.mvn/wrapper/maven-wrapper.properties
  - backend/src/main/java/com/quantlens/QuantLensApplication.java
  - backend/src/main/java/com/quantlens/marketdata/package-info.java
  - backend/src/main/java/com/quantlens/portfolio/package-info.java
  - backend/src/main/java/com/quantlens/security/package-info.java
  - backend/src/main/java/com/quantlens/seed/package-info.java
  - backend/src/main/resources/application.yml
  - backend/src/test/resources/application-test.yml
  - backend/src/test/java/com/quantlens/QuantLensModulithTest.java
  - backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java
  - backend/src/test/java/com/quantlens/seed/SeedRunnerIntegrationTest.java
  - backend/src/test/java/com/quantlens/infra/VectorStoreSchemaTest.java
  - backend/src/test/java/com/quantlens/security/AuthIntegrationTest.java
  - backend/src/test/java/com/quantlens/security/PersonaIntegrationTest.java
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, AUTH-01, AUTH-02, DEVX-01]
user_setup: []

must_haves:
  truths:
    - "Backend compiles under JDK 21 and ./mvnw test-compile passes"
    - "Spring Modulith recognizes the com.quantlens base package with the planned module packages"
    - "All Wave 0 test scaffolds exist and are red (or @Disabled-pending) — not missing"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Spring Boot 3.5.13 + Spring AI BOM 1.1.6 + Spring Modulith 1.4.11 + Testcontainers, no AI starters"
      contains: "spring-ai-bom"
    - path: "backend/src/main/java/com/quantlens/QuantLensApplication.java"
      provides: "@Modulithic @SpringBootApplication entry point"
      contains: "@Modulithic"
    - path: "backend/src/test/java/com/quantlens/QuantLensModulithTest.java"
      provides: "ApplicationModules.of(QuantLensApplication.class).verify() test"
      contains: "ApplicationModules"
    - path: "backend/src/test/resources/application-test.yml"
      provides: "Test profile pointing at Testcontainers-managed datasource"
      contains: "initialize-schema"
    - path: "backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java"
      provides: "Testcontainers pgvector base class with @ServiceConnection"
      contains: "pgvector/pgvector:pg16"
  key_links:
    - from: "backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java"
      to: "pgvector/pgvector:pg16 container"
      via: "PostgreSQLContainer + @ServiceConnection"
      pattern: "PostgreSQLContainer|ServiceConnection"
    - from: "backend/pom.xml"
      to: "spring-ai-bom 1.1.6"
      via: "dependencyManagement import scope"
      pattern: "spring-ai-bom"
---

<objective>
Scaffold the QuantLens Spring Boot 3.5.13 / Java 21 backend as a Spring Modulith modular monolith and lay down all Wave 0 test scaffolds the rest of Phase 1 must satisfy. This is the walking-skeleton contract task: it establishes the package boundaries, the build (Maven wrapper + pinned BOMs), the test infrastructure (Testcontainers pgvector base + test profile), and the red end-to-end test files that downstream plans turn green.

Purpose: Every downstream plan (schema/seed, auth, compose/frontend/MCP) builds against the contracts created here. Per VALIDATION.md, the Nyquist sampling loop requires these scaffolds to EXIST (red, not missing) before implementation begins.

Output: A compiling backend skeleton, pinned dependency management, six test scaffold files, and a test profile wired to Testcontainers pgvector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@CLAUDE.md

<build_environment>
JDK 21 (Temurin) at C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot. The default `java` on PATH is JDK 11 — every `./mvnw` / `.\mvnw.cmd` invocation MUST first set JAVA_HOME:
- PowerShell: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"`
- bash: `export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot"`
No standalone mvn/gradle — use the wrapper generated by Spring Initializr. Docker 28 + Compose v2.34 available (Testcontainers needs the Docker daemon running).
</build_environment>

<pinned_versions>
- Spring Boot: 3.5.13 (bootVersion in Initializr)
- Spring AI BOM: 1.1.6 — dependencyManagement import ONLY, NO spring-ai-* starters this phase
- Spring Modulith BOM: 1.4.11 (stable line for Boot 3.5.x; research's 1.3.5 was [ASSUMED] — use 1.4.11)
- Java: 21
- Initializr dependencies: web, security, data-jpa, flyway, postgresql, validation, actuator
- Testcontainers: managed by Spring Boot 3.5.13 parent BOM (org.testcontainers:postgresql, :junit-jupiter); add spring-boot-testcontainers (test scope)
</pinned_versions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold backend via Spring Initializr and add pinned BOMs</name>
  <files>backend/pom.xml, backend/mvnw, backend/mvnw.cmd, backend/.mvn/wrapper/maven-wrapper.properties, backend/src/main/java/com/quantlens/QuantLensApplication.java</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1 Initializr curl command; Pattern 2 pom additions; Code Examples § Initializr full curl)
    - CLAUDE.md (Build Environment section — JAVA_HOME, wrapper-only rule)
    - .planning/research/STACK.md (locked versions table)
  </read_first>
  <action>
    Run the Spring Initializr curl command from RESEARCH.md Pattern 1 exactly (type=maven-project, language=java, bootVersion=3.5.13, baseDir=backend, groupId=com.quantlens, artifactId=backend, name=QuantLens, packageName=com.quantlens, javaVersion=21, dependencies=web,security,data-jpa,flyway,postgresql,validation,actuator) and unzip into the repo root so the wrapper (mvnw, mvnw.cmd, .mvn/) lands under backend/. If curl/unzip is unavailable in the shell, download starter.zip from start.spring.io with the same query params and extract. Edit the generated pom.xml: add a properties block with java.version=21, spring-ai.version=1.1.6, spring-modulith.version=1.4.11; add a dependencyManagement section importing spring-ai-bom (org.springframework.ai:spring-ai-bom, type=pom, scope=import) and spring-modulith-bom (org.springframework.modulith:spring-modulith-bom, type=pom, scope=import); add dependencies spring-modulith-starter-core, spring-modulith-starter-test (test scope), flyway-database-postgresql, and (test scope) org.springframework.boot:spring-boot-testcontainers, org.testcontainers:postgresql, org.testcontainers:junit-jupiter. Do NOT add any spring-ai-* starter — BOM import only. Annotate the generated QuantLensApplication with @org.springframework.modulith.Modulithic in addition to @SpringBootApplication. Set JAVA_HOME to Temurin 21 before any wrapper run.
  </action>
  <verify>
    <automated>cd backend; in PowerShell set $env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"; run .\mvnw.cmd -q -DskipTests test-compile — must exit 0</automated>
  </verify>
  <acceptance_criteria>
    - backend/pom.xml contains `<artifactId>spring-ai-bom</artifactId>` with `<version>1.1.6</version>` under dependencyManagement
    - backend/pom.xml contains `<spring-modulith.version>1.4.11</spring-modulith.version>` and imports spring-modulith-bom
    - backend/pom.xml contains NO artifactId starting with `spring-ai-starter-`
    - backend/pom.xml contains flyway-database-postgresql, spring-modulith-starter-core, spring-modulith-starter-test, spring-boot-testcontainers, org.testcontainers:postgresql, org.testcontainers:junit-jupiter
    - QuantLensApplication.java contains both `@Modulithic` and `@SpringBootApplication`
    - `JAVA_HOME=Temurin21 ./mvnw test-compile` exits 0 and the first build line reports Java 21 (not 11)
  </acceptance_criteria>
  <done>Backend compiles under JDK 21 with all BOMs pinned and zero AI starters present.</done>
</task>

<task type="auto">
  <name>Task 2: Create Modulith module packages and application/test profiles</name>
  <files>backend/src/main/java/com/quantlens/marketdata/package-info.java, backend/src/main/java/com/quantlens/portfolio/package-info.java, backend/src/main/java/com/quantlens/security/package-info.java, backend/src/main/java/com/quantlens/seed/package-info.java, backend/src/main/resources/application.yml, backend/src/test/resources/application-test.yml</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Recommended Project Structure; Code Examples § application.yml complete; Pattern 3 pgvector yaml)
    - .planning/research/ARCHITECTURE.md (Package Layout Spring Modulith; inter-module rules)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (Modulith package boundaries decision)
  </read_first>
  <action>
    Create one package-info.java per Phase-1 module package under com.quantlens: marketdata, portfolio, security, seed. Each package-info.java carries the @org.springframework.modulith.ApplicationModule annotation (with a displayName) so Spring Modulith treats each as a named module. Do NOT create analytics/ai/mcp packages this phase (only Phase-1 packages per CONTEXT). Author backend/src/main/resources/application.yml exactly as RESEARCH.md Code Examples § application.yml complete: datasource with ${SPRING_DATASOURCE_*} env placeholders defaulting to localhost/quantlens/quantlens_dev; jpa.hibernate.ddl-auto=validate; jpa.open-in-view=false; flyway.enabled=true with classpath:db/migration; spring.ai.vectorstore.pgvector with initialize-schema=true, dimensions=1536, distance-type=COSINE_DISTANCE, index-type=HNSW (comment each "EXPLICIT — default is false" / "Locked 1536"); spring.ai.chat.client.enabled=false; server.port=8080 with servlet.session.cookie.same-site=lax and tracking-modes=cookie; management health-only with show-details=never. Author backend/src/test/resources/application-test.yml for @ActiveProfiles("test"): same spring.ai.vectorstore.pgvector block (initialize-schema=true, dimensions=1536, COSINE_DISTANCE, HNSW), ddl-auto=validate, flyway enabled — datasource is supplied at runtime by Testcontainers @ServiceConnection (do not hardcode jdbc URL).
  </action>
  <verify>
    <automated>cd backend; set JAVA_HOME to Temurin 21; run .\mvnw.cmd -q -DskipTests test-compile — must exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Four package-info.java files exist under com/quantlens/{marketdata,portfolio,security,seed}, each annotated @ApplicationModule
    - No analytics/ai/mcp package directories exist under com/quantlens this phase
    - application.yml contains `initialize-schema: true`, `dimensions: 1536`, `distance-type: COSINE_DISTANCE`, `index-type: HNSW`
    - application.yml contains `ddl-auto: validate`, `open-in-view: false`, `same-site: lax`, and `spring.ai.chat.client.enabled: false`
    - application.yml management exposure includes only `health` with `show-details: never`
    - application-test.yml contains the same pgvector block and does NOT hardcode a jdbc datasource URL
  </acceptance_criteria>
  <done>Module packages declared; main and test profiles authored with locked pgvector + session config; test-compile green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Wave 0 test scaffolds (Testcontainers base + six red tests)</name>
  <files>backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java, backend/src/test/java/com/quantlens/QuantLensModulithTest.java, backend/src/test/java/com/quantlens/seed/SeedRunnerIntegrationTest.java, backend/src/test/java/com/quantlens/infra/VectorStoreSchemaTest.java, backend/src/test/java/com/quantlens/security/AuthIntegrationTest.java, backend/src/test/java/com/quantlens/security/PersonaIntegrationTest.java</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 11 Modulith test; Validation Architecture § Phase Requirements → Test Map; Observable Validation Signals)
  </read_first>
  <behavior>
    - AbstractPostgresIntegrationTest: starts a PostgreSQLContainer using image pgvector/pgvector:pg16, exposes it via @ServiceConnection so Spring Boot test context auto-wires the datasource; runs docker/db init equivalent (CREATE EXTENSION vector + uuid-ossp) via container init or an @BeforeAll exec so the vector_store schema can initialize; reused by all integration tests
    - QuantLensModulithTest: ApplicationModules.of(QuantLensApplication.class).verify() passes (asserts module boundaries) — this should pass once packages exist
    - SeedRunnerIntegrationTest (RED until Plan 02): asserts securities count >= 15, ohlcv_bars count ~ 15*504 (>= 7000), app_users count == 3, factor_returns count ~ 504 (>= 500); asserts a second context start does not duplicate (idempotence)
    - VectorStoreSchemaTest (RED until Plan 02): queries information_schema / pg type to assert a vector_store table exists and its embedding column is the pgvector type with dimension 1536
    - AuthIntegrationTest (RED until Plan 03): loginSuccess → POST /api/auth/login alice/demo1234 returns 200 JSON {"authenticated":true}; loginFailure → wrong password returns 401 JSON; sessionPersists → cookie from login lets GET /api/auth/me return 200
    - PersonaIntegrationTest (RED until Plan 03): logging in as alice vs bob vs charlie resolves three distinct portfolio identities (different portfolio id / persona) — proves AUTH-02 scoping
  </behavior>
  <action>
    Create the six files above. AbstractPostgresIntegrationTest is the shared base (annotated @SpringBootTest @ActiveProfiles("test") @Testcontainers) holding a static @Container PostgreSQLContainer<>("pgvector/pgvector:pg16") with @ServiceConnection; ensure the vector + uuid-ossp extensions are created on the container before the Spring context starts (init script via withCopyToContainer/withInitScript or container.execInContainer in @BeforeAll). QuantLensModulithTest implements RESEARCH Pattern 11. The four behavior tests extend AbstractPostgresIntegrationTest and encode the assertions in <behavior>; where the production code they target does not yet exist (repositories, /api/auth endpoints), reference the intended contracts by name and mark each test method @Disabled("RED — turns green in Plan 0X") OR write them to fail meaningfully — but the FILES MUST EXIST and test-compile. Do NOT use watch-mode flags. Use MockMvc or TestRestTemplate for the auth HTTP assertions.
  </action>
  <verify>
    <automated>cd backend; set JAVA_HOME to Temurin 21; run .\mvnw.cmd -q test-compile — must exit 0 (all six files compile). Then .\mvnw.cmd -q test -Dtest=QuantLensModulithTest — must pass.</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist at the VALIDATION.md Wave 0 paths and test-compile passes
    - AbstractPostgresIntegrationTest references image string `pgvector/pgvector:pg16` and `@ServiceConnection`
    - QuantLensModulithTest calls `ApplicationModules.of(QuantLensApplication.class).verify()` and passes
    - SeedRunnerIntegrationTest, VectorStoreSchemaTest, AuthIntegrationTest, PersonaIntegrationTest each compile and are RED (failing or @Disabled-pending), not absent
    - No `--watch` or watch-mode flag appears in any test
  </acceptance_criteria>
  <done>Six Wave 0 scaffolds exist and compile; modulith test green; the four feature tests are red placeholders ready for Plans 02 and 03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify backend scaffold + Wave 0 test infrastructure</name>
  <action>PAUSE for human verification. The executor performs no automated work in this task — present the what-built summary and the how-to-verify steps to the user, wait for the resume-signal, and do not proceed to Wave 1 until approved.</action>
  <what-built>Backend scaffold (Spring Boot 3.5.13 / Java 21 modular monolith), pinned BOMs (Spring AI 1.1.6 + Spring Modulith 1.4.11), application/test profiles, and six Wave 0 test scaffolds with a Testcontainers pgvector base class.</what-built>
  <how-to-verify>
    1. Open backend/pom.xml — confirm spring-ai-bom 1.1.6 and spring-modulith-bom 1.4.11 are imported and there are NO spring-ai-starter-* dependencies.
    2. In PowerShell: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"` then `cd backend; .\mvnw.cmd -q test -Dtest=QuantLensModulithTest` — should print BUILD SUCCESS and the build header should show Java 21.
    3. Confirm Docker Desktop is running, then optionally `cd backend; .\mvnw.cmd -q test-compile` to confirm all six test files compile.
    4. Confirm no analytics/ai/mcp packages were created (only marketdata, portfolio, security, seed).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 1 (schema/seed + auth), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → developer host | Maven wrapper downloads Maven + dependencies; Initializr zip fetched over network |
| Testcontainers → Docker daemon | Test starts a Postgres container image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Spring Initializr zip / Maven dependency download (supply chain) | mitigate | Use the official start.spring.io API and Maven Central via the generated wrapper only; pin exact versions (Boot 3.5.13, AI BOM 1.1.6, Modulith 1.4.11) so resolved artifacts are deterministic |
| T-01-02 | Tampering | Testcontainers image pull | accept | Image `pgvector/pgvector:pg16` is the same image used in production compose; pinned tag, official pgvector publisher |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (Maven-only scaffold). The npm package legitimacy checkpoint applies to Plan 04 (frontend + .mcp.json). |
</threat_model>

<verification>
- `JAVA_HOME=Temurin21 ./mvnw test-compile` exits 0 (all main + test sources compile under Java 21)
- `JAVA_HOME=Temurin21 ./mvnw test -Dtest=QuantLensModulithTest` passes (module boundaries valid)
- pom.xml: spring-ai-bom 1.1.6 + spring-modulith-bom 1.4.11 imported; zero spring-ai-starter-* deps
- Six Wave 0 test files exist at VALIDATION.md paths and compile
</verification>

<success_criteria>
- Backend scaffold compiles under JDK 21 with the Maven wrapper
- Spring AI BOM 1.1.6 and Spring Modulith 1.4.11 pinned; no AI starters present
- Four Modulith module packages declared (marketdata, portfolio, security, seed)
- application.yml + application-test.yml authored with locked pgvector (1536/COSINE/HNSW) and session config
- All six Wave 0 test scaffolds exist (red/disabled where production code is pending), modulith test green
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done
</output>
