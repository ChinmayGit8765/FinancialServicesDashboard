---
phase: 03-frontend-scaffold
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/components/PnlChart.vue
  - frontend/src/components/BenchmarkChart.vue
  - frontend/src/components/AllocationChart.vue
autonomous: true
requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "PnlChart renders the equity curve as a single accent line with gradient area fill from PortfolioPnlDto.equityCurve"
    - "BenchmarkChart renders two rebased-to-100 lines (portfolio solid accent, S&P 500 dashed secondary)"
    - "AllocationChart renders a donut by default and switches to a treemap via a segmented toggle"
    - "Each chart shows loading skeleton, empty message, and error state per UI-SPEC, wrapped in a figure/figcaption"
  artifacts:
    - path: "frontend/src/components/PnlChart.vue"
      provides: "Equity-curve line chart with category xAxis and UTC-safe date parsing"
      contains_all: ["xAxis", "type: 'category'", "T00:00:00"]
    - path: "frontend/src/components/BenchmarkChart.vue"
      provides: "Dual-line benchmark overlay chart"
      min_lines: 40
    - path: "frontend/src/components/AllocationChart.vue"
      provides: "Donut/treemap allocation chart with toggle"
      contains: "treemap"
  key_links:
    - from: "frontend/src/components/PnlChart.vue"
      to: "vue-echarts VChart"
      via: "<v-chart :option=\"option\" :autoresize=\"true\">"
      pattern: "v-chart"
    - from: "frontend/src/components/PnlChart.vue"
      to: "frontend/src/api/portfolio.ts"
      via: "import type PortfolioPnlDto"
      pattern: "PortfolioPnlDto"
    - from: "frontend/src/components/AllocationChart.vue"
      to: "AllocationSliceDto"
      via: "donut/treemap option built from props.allocation"
      pattern: "AllocationSliceDto"
---

<objective>
Build the three ECharts data-visualization components that consume the Phase 2 chart-ready DTOs: `PnlChart.vue` (equity-curve line + gradient fill), `BenchmarkChart.vue` (dual rebased-to-100 lines), and `AllocationChart.vue` (donut with treemap toggle). Each follows the shared figure/loading/empty/error scaffold and binds a reactive `computed` option to `<v-chart>`, inheriting the `quantlens-dark` theme via the THEME_KEY provided in App.vue (Plan 01).

Purpose: These are the visual heart of the screenshot-ready dashboard (UI-01). They are pure presentational components driven by props (the store resources DashboardView passes in Plan 05), so they can be built and type-checked in parallel with the table/card components in Plan 04 — no file overlap.

Output: Three chart SFCs, all building green, each handling loading/empty/error and using `xAxis.type:'category'` with ISO date strings (no UTC off-by-one).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-frontend-scaffold/03-UI-SPEC.md
@.planning/phases/03-frontend-scaffold/03-RESEARCH.md
@.planning/phases/03-frontend-scaffold/03-PATTERNS.md

<interfaces>
<!-- DTO shapes these components bind to (from Plan 02 api/portfolio.ts). All BigDecimal → number; use Number(x) when ECharts needs numeric. -->
PortfolioPnlDto.equityCurve: DateValueDto[]  where DateValueDto = { date:string "2022-09-12", value:number }
BenchmarkComparisonDto: { dates:string[], portfolioSeries:number[], benchmarkSeries:number[] }  (both series rebased to 100.0000)
AllocationSliceDto: { label:string, weight:number (0–1), marketValue:number }

Theme: <v-chart> inherits 'quantlens-dark' via THEME_KEY provided in App.vue (Plan 01) — do NOT pass a theme prop.
EChartsOption type: import type { EChartsOption } from 'echarts/types/dist/shared'
Components imported into DashboardView in Plan 05 — they receive props, emit 'retry'.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data → ECharts option | Server-sourced numbers/strings injected into chart formatters and the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (XSS) | chart tooltip/label formatters | mitigate | Tooltip/axis formatters build strings from typed numbers/ISO dates only; never inject API strings into v-html; Vue interpolation in the figcaption escapes by default (RESEARCH.md Security Domain — no v-html anywhere in Phase 3). |
| T-03-08 | Information disclosure | error state text | mitigate | Error blocks show the static copy from UI-SPEC ("Failed to load … Retry?"), never the raw error object. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PnlChart.vue — equity-curve line chart with shared figure/state scaffold</name>
  <files>frontend/src/components/PnlChart.vue</files>
  <read_first>
    - 03-RESEARCH.md Pattern 2 (full PnlChart computed option: xAxis.type:'category' with ISO date array, axisLabel formatter "Sep '22" via new Date(val+'T00:00:00'), yAxis compact-currency formatter, line smooth symbol:'none' accent color, area gradient rgba(14,165,233,0.25)→0, tooltip trigger:'axis' with DD MMM YYYY + currency) and Pitfall 1 (category not time) + Pitfall 2 (UTC off-by-one)
    - 03-PATTERNS.md "src/components/PnlChart.vue" (component scaffold: figure[aria-busy] > skeleton | chart-error[+retry emit] | chart-empty | v-chart; chart-panel scoped CSS using var(--token); .sr-only figcaption) — this is the canonical scaffold reused by Tasks 2 & 3
    - 03-UI-SPEC.md § PnlChart.vue states (loading shimmer 320px, empty "No P&L data available.", error "Failed to load P&L data. Retry?", populated accent line + gradient), § Accessibility (figure + figcaption text summary)
  </read_first>
  <action>
    Create frontend/src/components/PnlChart.vue with `<script setup lang="ts">` defining `defineProps<{ pnl: PortfolioPnlDto | null; loading: boolean; error: string | null }>()` and `defineEmits<{ retry: [] }>()`. Build a `computed<EChartsOption>` returning `{}` when `!props.pnl?.equityCurve?.length`, else the Pattern-2 option: xAxis.type:'category' with `equityCurve.map(d=>d.date)`, axisLabel.formatter using `new Date(val+'T00:00:00')` → `toLocaleDateString('en-US',{month:'short',year:'2-digit'})`; yAxis.type:'value' with compact USD formatter; one line series (smooth, symbol:'none', lineStyle accent #0ea5e9 via the theme — set width:2, color from theme), areaStyle linear gradient rgba(14,165,233,0.25)→rgba(14,165,233,0); tooltip trigger:'axis' formatter producing "DD MMM YYYY<br/>$X,XXX,XXX.XX" (date via new Date(axisValue+'T00:00:00')). Template: `<figure class="chart-panel" :aria-busy="loading">` containing v-if loading skeleton (320px), v-else-if error chart-error with a Retry `<button @click="$emit('retry')">`, v-else-if empty message, v-else `<v-chart class="chart" :option="option" :autoresize="true" />`, plus a `.sr-only` `<figcaption>` summarizing the curve. Scoped CSS uses var(--token) exclusively (T-03 styling rule) — chart-panel bg var(--color-bg-surface), radius var(--radius-lg), shadow var(--shadow-card), border var(--color-border); .chart height 320px; .chart-error color var(--color-down); .chart-empty color var(--color-text-muted). No theme prop on v-chart.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - PnlChart.vue contains an `xAxis` with `type: 'category'` AND uses `new Date(` with `'T00:00:00'` (both axisLabel and tooltip date parsing — UTC off-by-one guard)
    - Template has all four states (loading/error/empty/v-chart) and a figcaption
    - No hardcoded hex outside the documented gradient rgba values; all colors use var(--token) where a token exists
    - No `theme=` prop on v-chart; no v-html
  </acceptance_criteria>
  <done>PnlChart renders equity curve with gradient fill + all states; build green.</done>
</task>

<task type="auto">
  <name>Task 2: BenchmarkChart.vue — dual rebased-to-100 line chart</name>
  <files>frontend/src/components/BenchmarkChart.vue</files>
  <read_first>
    - 03-PATTERNS.md "src/components/BenchmarkChart.vue" (sibling of PnlChart — same figure/state scaffold; dual-series option: Portfolio accent #0ea5e9 width 2, S&P 500 secondary #94a3b8 width 1.5 dashed)
    - 03-UI-SPEC.md § BenchmarkChart.vue (legend two items top-right, baseline 100 faint horizontal rule, tooltip both series side-by-side, empty "No benchmark data available.", error "Failed to load benchmark. Retry?", 320px)
    - 03-RESEARCH.md Pattern 2 (xAxis.type:'category' with dates array; symbol:'none')
    - The PnlChart.vue scaffold created in Task 1 (copy its figure/loading/empty/error structure)
  </read_first>
  <action>
    Create frontend/src/components/BenchmarkChart.vue mirroring the PnlChart scaffold but with `defineProps<{ benchmark: BenchmarkComparisonDto | null; loading: boolean; error: string | null }>()` + retry emit. computed option returns {} when `!props.benchmark?.dates?.length`, else: xAxis.type:'category' data `props.benchmark.dates`; yAxis.type:'value' (plain numeric, ~100 baseline); legend top-right with 'Portfolio' + 'S&P 500'; a markLine or yAxis splitLine at 100 as a faint rule (var(--color-border)); two line series — {name:'Portfolio', data:portfolioSeries.map(Number), smooth, symbol:'none', lineStyle:{color:'#0ea5e9', width:2}} and {name:'S&P 500', data:benchmarkSeries.map(Number), smooth, symbol:'none', lineStyle:{color:'#94a3b8', width:1.5, type:'dashed'}}; tooltip trigger:'axis' showing both series values. Empty/error copy per UI-SPEC. Reuse the chart-panel scoped CSS pattern (var tokens, 320px). No theme prop, no v-html.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - BenchmarkChart.vue has two line series, the second with `type: 'dashed'`
    - xAxis uses `type: 'category'` with the dates array
    - All four states present + figcaption; empty copy "No benchmark data available."
  </acceptance_criteria>
  <done>BenchmarkChart renders portfolio vs S&P 500 both at 100 with all states; build green.</done>
</task>

<task type="auto">
  <name>Task 3: AllocationChart.vue — donut with treemap toggle</name>
  <files>frontend/src/components/AllocationChart.vue</files>
  <read_first>
    - 03-RESEARCH.md Pattern 3 (donutOption: pie radius ['40%','68%'] center ['50%','55%'] data {name:label, value:Number(marketValue)} label hidden + emphasis; treemapOption: type 'treemap' data {name,value} label formatter "name\nweight%" itemStyle borderWidth 2; chartType ref + computed switch)
    - 03-PATTERNS.md "src/components/AllocationChart.vue" (sibling scaffold; donut/treemap toggle ref; segmented toggle template + active CSS var(--color-accent-subtle)/var(--color-accent); height 280px not 320px)
    - 03-UI-SPEC.md § AllocationChart.vue (8-color palette comes from theme color[]; center label total market value; toggle segmented control top-right; empty "No allocation data. Add holdings to see your sector breakdown."; error "Failed to load allocation. Retry?")
    - The PnlChart.vue scaffold (Task 1) for the figure/state structure
  </read_first>
  <action>
    Create frontend/src/components/AllocationChart.vue with `defineProps<{ allocation: AllocationSliceDto[] | null; loading: boolean; error: string | null }>()` + retry emit. Add `const chartType = ref<'donut'|'treemap'>('donut')`. Build `donutOption` (pie radius ['40%','68%'], center ['50%','55%'], data from allocation mapped to {name:label, value:Number(marketValue)}, label hidden, emphasis label shown; color cycling comes from the quantlens-dark theme color[] — do not hardcode the palette) and `treemapOption` (type 'treemap', data {name:label, value:Number(marketValue)}, label.formatter showing name + `(weight*100).toFixed(1)%`, itemStyle borderWidth 2 borderColor var canvas). `const option = computed(() => chartType.value==='donut' ? donutOption.value : treemapOption.value)`. Treat empty as `!props.allocation?.length`. Template: figure/loading/empty/error scaffold (280px) + a segmented `<div class="chart-toggle" role="group" aria-label="Chart type">` with Donut/Treemap buttons toggling chartType (active class → background var(--color-accent-subtle), border var(--color-accent)) positioned top-right; v-chart bound to option. Scoped CSS var tokens, height 280px. No theme prop, no v-html.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - AllocationChart.vue contains both a pie series and a `treemap` series, switched by a `chartType` ref
    - A segmented Donut/Treemap toggle with role="group" exists
    - All four states present + empty copy "No allocation data…"; chart height 280px
  </acceptance_criteria>
  <done>AllocationChart renders donut + treemap toggle with all states; build green.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with all three chart components.
- Each chart: figure/figcaption + loading/empty/error + v-chart inheriting quantlens-dark theme.
- xAxis.type:'category' on time-series charts; UTC off-by-one avoided in formatters.
</verification>

<success_criteria>
- PnlChart (equity curve + gradient), BenchmarkChart (dual rebased lines), AllocationChart (donut/treemap toggle) all built and type-check clean.
- Shared figure/state scaffold; retry emits; tokens-only styling; no v-html, no theme prop.
</success_criteria>

<output>
Create `.planning/phases/03-frontend-scaffold/03-03-SUMMARY.md` when done.
</output>
