---
phase: 04-quant-risk-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/quantlens/analytics/package-info.java
  - backend/src/main/java/com/quantlens/analytics/api/RiskScorecardDto.java
  - backend/src/main/java/com/quantlens/analytics/api/VarResultDto.java
  - backend/src/main/java/com/quantlens/analytics/api/CorrelationMatrixDto.java
  - backend/src/main/java/com/quantlens/analytics/api/AttributionDto.java
  - backend/src/main/java/com/quantlens/analytics/api/PairResultDto.java
  - backend/src/main/java/com/quantlens/analytics/service/RiskCalculator.java
  - backend/src/main/java/com/quantlens/analytics/service/CorrelationCalculator.java
  - backend/src/main/java/com/quantlens/analytics/service/FamaFrenchCalculator.java
  - backend/src/main/java/com/quantlens/analytics/service/CointegrationScanner.java
  - backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java
  - backend/src/main/java/com/quantlens/marketdata/domain/FactorReturnRepository.java
  - backend/src/test/java/com/quantlens/analytics/RiskCalculatorTest.java
  - backend/src/test/java/com/quantlens/analytics/CorrelationCalculatorTest.java
  - backend/src/test/java/com/quantlens/analytics/FamaFrenchCalculatorTest.java
  - backend/src/test/java/com/quantlens/analytics/CointegrationScannerTest.java
  - backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java
  - backend/src/test/java/com/quantlens/analytics/AnalyticsGoldenValuePrinterTest.java
  - frontend/src/api/analytics.ts
  - frontend/src/__tests__/components/RiskScorecard.test.ts
  - frontend/src/__tests__/components/CorrelationHeatmap.test.ts
  - frontend/src/__tests__/components/AttributionChart.test.ts
  - frontend/src/__tests__/components/PairsTable.test.ts
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03, ATTR-01, ARB-01]

must_haves:
  truths:
    - "The com.quantlens.analytics Modulith module compiles and its boundary (allowedDependencies marketdata::domain, portfolio::domain) passes QuantLensModulithTest"
    - "All five analytics DTOs exist as Java records with the exact field names the frontend and tests consume"
    - "All four /api/portfolio analytics endpoints exist, are principal-scoped, and return 401 when unauthenticated"
    - "Six backend test files and four frontend test files exist as RED scaffolds (compile, reference real symbols, assertions pending golden values)"
    - "The @Disabled AnalyticsGoldenValuePrinterTest exists and can be run manually to print seed-derived constants"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/analytics/package-info.java"
      provides: "Modulith module declaration"
      contains: "ApplicationModule"
    - path: "backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java"
      provides: "Four principal-scoped read-only endpoints"
      contains: "resolvePortfolioId"
    - path: "backend/src/main/java/com/quantlens/analytics/api/RiskScorecardDto.java"
      provides: "Scorecard DTO record"
      contains: "record RiskScorecardDto"
    - path: "backend/src/main/java/com/quantlens/marketdata/domain/FactorReturnRepository.java"
      provides: "findAllByOrderByFactorDateAsc query"
      contains: "findAllByOrderByFactorDateAsc"
    - path: "backend/src/test/java/com/quantlens/analytics/AnalyticsGoldenValuePrinterTest.java"
      provides: "Disabled golden-value printer"
      contains: "@Disabled"
    - path: "frontend/src/api/analytics.ts"
      provides: "Analytics DTO TypeScript interfaces"
      contains: "RiskScorecardDto"
  key_links:
    - from: "backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java"
      to: "PortfolioRepository.findPortfolioIdByUsername"
      via: "resolvePortfolioId helper copied from PortfolioController"
      pattern: "findPortfolioIdByUsername"
    - from: "backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java"
      to: "AbstractPostgresIntegrationTest"
      via: "extends"
      pattern: "extends AbstractPostgresIntegrationTest"
---

<objective>
Establish the Phase 4 analytics foundation: a new `com.quantlens.analytics` Spring Modulith module with five DTO records, four compiling stub calculator/scanner services, a four-endpoint principal-scoped controller, the `FactorReturnRepository` ordered query, and ALL Wave 0 RED test scaffolds (6 backend + 4 frontend) including the `@Disabled` golden-value printer.

This is the Wave 0 plan. After it lands, every downstream plan implements against contracts that already compile and against tests that already exist (currently RED). No downstream plan invents a DTO field name or endpoint path.

Purpose: Lock the analytics contracts and test surface before any math is written, so RISK/ATTR/ARB calculators (Plans 02–03) and the frontend (Plan 04) build against fixed interfaces with golden-value tests waiting to turn GREEN.
Output: A compiling analytics module, the four analytics endpoints (returning stub data), the FactorReturnRepository extension, and 10 RED test files. Backend `.\mvnw.cmd compile` succeeds; frontend `npm run build` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quant-risk-engine/04-CONTEXT.md
@.planning/phases/04-quant-risk-engine/04-RESEARCH.md
@.planning/phases/04-quant-risk-engine/04-PATTERNS.md
@.planning/phases/04-quant-risk-engine/04-VALIDATION.md

<interfaces>
<!-- Exact contracts extracted from the codebase. Use these directly — no exploration needed. -->

Module boundary analog — backend/src/main/java/com/quantlens/portfolio/package-info.java:
  @org.springframework.modulith.ApplicationModule(
          displayName = "Portfolio",
          allowedDependencies = {"marketdata::domain", ...})
  package com.quantlens.portfolio;
Both portfolio::domain and marketdata::domain already declare @NamedInterface("domain") — no changes needed there.

Principal resolution — backend/src/main/java/com/quantlens/portfolio/api/PortfolioController.java lines 180-187 (copy verbatim):
  private Long resolvePortfolioId(Authentication authentication) {
      if (authentication == null || !authentication.isAuthenticated()) {
          throw new ResponseStatusException(HttpStatus.UNAUTHORIZED);
      }
      String username = authentication.getName();
      return portfolioRepository.findPortfolioIdByUsername(username)
              .orElseThrow(() -> new ResponseStatusException(HttpStatus.UNAUTHORIZED));
  }
PortfolioRepository.findPortfolioIdByUsername(String) returns Optional<Long> (portfolio::domain).

Repositories available to analytics (via marketdata::domain + portfolio::domain):
  PositionRepository.findByPortfolioIdWithSecurity(Long portfolioId) -> List<Position>   (JOIN FETCH security)
  OhlcvBarRepository.findAllBySecurityIdsOrdered(List<Long>) -> List<OhlcvBar>            (ORDER BY security.id ASC, barDate ASC)
  OhlcvBarRepository.findLatestBarBySecurityIds(List<Long>) -> List<OhlcvBar>             (one bar per security)
  SecurityRepository.findByBenchmarkTrue() -> List<Security>                              (SPX500, size 1)
  FactorReturnRepository: currently bare JpaRepository<FactorReturn, Long> — ADD findAllByOrderByFactorDateAsc()

Entity accessors (exact — do not guess):
  OhlcvBar.getClosePrice() -> BigDecimal; .getBarDate() -> LocalDate; .getSecurity() -> Security
  FactorReturn.getFactorDate() -> LocalDate; .getMktRf()/.getSmb()/.getHml()/.getRf() -> BigDecimal
  Security.isBenchmark() -> boolean; .getSector() -> String; .getTicker() -> String; .getId() -> Long
  AppUserRepository.findByUsername("alice") -> Optional<AppUser>; PortfolioRepository.findByUserId(Long) -> List<Portfolio>

Integration test base — backend/src/test/java/com/quantlens/portfolio/PortfolioControllerIntegrationTest.java:
  extends AbstractPostgresIntegrationTest; @Autowired TestRestTemplate; loginAndGetSessionCookie("alice") posts /api/auth/login with password "demo1234".

Frontend DTO interface analog — frontend/src/api/portfolio.ts: exports interfaces only, no fetch functions.
Frontend test analog — frontend/src/__tests__/components/KpiCard.test.ts: vitest + @vue/test-utils mount; props-driven loading/error/data states.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics module + five DTO records + FactorReturnRepository query</name>
  <files>backend/src/main/java/com/quantlens/analytics/package-info.java, backend/src/main/java/com/quantlens/analytics/api/RiskScorecardDto.java, backend/src/main/java/com/quantlens/analytics/api/VarResultDto.java, backend/src/main/java/com/quantlens/analytics/api/CorrelationMatrixDto.java, backend/src/main/java/com/quantlens/analytics/api/AttributionDto.java, backend/src/main/java/com/quantlens/analytics/api/PairResultDto.java, backend/src/main/java/com/quantlens/marketdata/domain/FactorReturnRepository.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/portfolio/package-info.java (Modulith @ApplicationModule analog)
    - backend/src/main/java/com/quantlens/portfolio/api/DateValueDto.java (Java record DTO style analog)
    - backend/src/main/java/com/quantlens/marketdata/domain/FactorReturnRepository.java (the bare repo to extend)
    - 04-PATTERNS.md "DTO Records" section (lines 129-191 — exact field lists)
    - 04-RESEARCH.md "DTO Shapes" section (lines 843-887)
  </read_first>
  <action>
    Create `analytics/package-info.java` with `@org.springframework.modulith.ApplicationModule(displayName = "Analytics", allowedDependencies = {"marketdata::domain", "portfolio::domain"})` and `package com.quantlens.analytics;`. Do NOT add `portfolio::service` — analytics replicates the short equity-curve logic locally (decision confirmed in 04-RESEARCH.md Open Question 1).

    Create five record DTOs in `analytics/api/`, using the compact record style of DateValueDto. Enforce the numeric-convention split: statistics as `double`, money as `BigDecimal`.
    - VarResultDto: `String method`, `double confidence`, `int horizonDays`, `BigDecimal amount`, `double percentage`. Javadoc the contract: method is "HISTORICAL" | "PARAMETRIC" | "CVaR_HISTORICAL"; confidence 0.95; horizonDays 1; amount is a POSITIVE monetary loss; percentage is a fraction (0.018 = 1.8%).
    - RiskScorecardDto: `double sharpeRatio`, `double annualizedVolatility`, `double maxDrawdown`, `double beta`, `List<VarResultDto> var`. Javadoc maxDrawdown as negative (-0.18 = 18% drawdown).
    - CorrelationMatrixDto: `List<String> tickers`, `List<List<Double>> matrix` (N×N, diagonal 1.0).
    - AttributionDto: `double alphaAnnualized`, `double betaMkt`, `double betaSmb`, `double betaHml`, `double rSquared`, `double contribMktAnnualized`, `double contribSmbAnnualized`, `double contribHmlAnnualized`.
    - PairResultDto: `String tickerY`, `String tickerX`, `double hedgeRatio`, `double adfStatistic`, `double pValue`, `double spreadZScore`, `String signal` (signal: "LONG_Y_SHORT_X" | "SHORT_Y_LONG_X" | "NEUTRAL").

    Add `List<FactorReturn> findAllByOrderByFactorDateAsc();` to FactorReturnRepository (Spring Data derived query; returns 504 rows ordered by factorDate ASC). Add the `import java.util.List;`.
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot && mvnw.cmd -q -o compile"</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd -q compile` succeeds (JAVA_HOME=Temurin 21).
    - `analytics/package-info.java` contains `allowedDependencies = {"marketdata::domain", "portfolio::domain"}` and does NOT contain `portfolio::service`.
    - All five DTO files declare `public record` with the exact field names above (grep each filename for its `record <Name>` declaration).
    - FactorReturnRepository contains `findAllByOrderByFactorDateAsc`.
  </acceptance_criteria>
  <done>Analytics module declared, five DTO records compile with exact field names, FactorReturnRepository has the ordered query. Backend compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Stub calculator services + AnalyticsController (4 principal-scoped endpoints)</name>
  <files>backend/src/main/java/com/quantlens/analytics/service/RiskCalculator.java, backend/src/main/java/com/quantlens/analytics/service/CorrelationCalculator.java, backend/src/main/java/com/quantlens/analytics/service/FamaFrenchCalculator.java, backend/src/main/java/com/quantlens/analytics/service/CointegrationScanner.java, backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/portfolio/api/PortfolioController.java (controller + resolvePortfolioId analog — lines 48-188)
    - backend/src/main/java/com/quantlens/portfolio/service/PortfolioService.java lines 56-73 (service @Service @Transactional(readOnly=true) declaration + constructor injection)
    - 04-PATTERNS.md "AnalyticsController.java" section (lines 59-126) and "RiskCalculator.java" section (lines 194-285)
  </read_first>
  <action>
    Create four `@Service @Transactional(readOnly = true)` classes in `analytics/service/`. In this plan they are STUBS that compile and wire DI but return placeholder data (real math lands in Plans 02–03). Inject only repositories from allowed modules.
    - RiskCalculator: constructor-inject PositionRepository, OhlcvBarRepository, SecurityRepository, FactorReturnRepository. Method `public RiskScorecardDto computeRiskScorecard(Long portfolioId)` returning a stub (e.g. `new RiskScorecardDto(0, 0, 0, 0, List.of())`). Add a `// STUB: real implementation in Plan 04-02` comment.
    - CorrelationCalculator: inject PositionRepository, OhlcvBarRepository. Method `public CorrelationMatrixDto computeCorrelationMatrix(Long portfolioId)` returning `new CorrelationMatrixDto(List.of(), List.of())`. Comment `// STUB: Plan 04-02`.
    - FamaFrenchCalculator: inject PositionRepository, OhlcvBarRepository, FactorReturnRepository. Method `public AttributionDto computeAttribution(Long portfolioId)` returning a zeroed AttributionDto. Comment `// STUB: Plan 04-03`.
    - CointegrationScanner: inject PositionRepository, OhlcvBarRepository, SecurityRepository. Method `public List<PairResultDto> scanPairs(Long portfolioId)` returning `List.of()`. Comment `// STUB: Plan 04-03`.

    Create AnalyticsController in `analytics/api/`: `@RestController @RequestMapping("/api/portfolio")`. Constructor-inject PortfolioRepository (for principal resolution) + the four services. Copy the `resolvePortfolioId(Authentication)` private helper VERBATIM from PortfolioController (IDOR control — never accept portfolioId from request). Four endpoints, each `@GetMapping @Transactional(readOnly = true)`, each calling `resolvePortfolioId(authentication)` then the matching service:
    - `GET /risk` -> `ResponseEntity<RiskScorecardDto>` via riskCalculator.computeRiskScorecard
    - `GET /correlation` -> `ResponseEntity<CorrelationMatrixDto>` via correlationCalculator.computeCorrelationMatrix
    - `GET /attribution` -> `ResponseEntity<AttributionDto>` via ffCalculator.computeAttribution
    - `GET /pairs` -> `ResponseEntity<List<PairResultDto>>` via cointegrationScanner.scanPairs
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot && mvnw.cmd -q -o compile"</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd -q compile` succeeds.
    - AnalyticsController contains `resolvePortfolioId` and the literal route strings `/risk`, `/correlation`, `/attribution`, `/pairs`; contains NO `@RequestParam` or `@PathVariable` referencing portfolioId/userId (grep confirms absence).
    - All four service classes are annotated `@Service` and `@Transactional(readOnly = true)`.
  </acceptance_criteria>
  <done>Four stub services and the four-endpoint controller compile; principal resolution copied verbatim; no IDOR vector.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 RED test scaffolds — 6 backend + 4 frontend + @Disabled printer + analytics.ts</name>
  <files>backend/src/test/java/com/quantlens/analytics/RiskCalculatorTest.java, backend/src/test/java/com/quantlens/analytics/CorrelationCalculatorTest.java, backend/src/test/java/com/quantlens/analytics/FamaFrenchCalculatorTest.java, backend/src/test/java/com/quantlens/analytics/CointegrationScannerTest.java, backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java, backend/src/test/java/com/quantlens/analytics/AnalyticsGoldenValuePrinterTest.java, frontend/src/api/analytics.ts, frontend/src/__tests__/components/RiskScorecard.test.ts, frontend/src/__tests__/components/CorrelationHeatmap.test.ts, frontend/src/__tests__/components/AttributionChart.test.ts, frontend/src/__tests__/components/PairsTable.test.ts</files>
  <read_first>
    - backend/src/test/java/com/quantlens/portfolio/GoldenValuePrinterTest.java (the @Disabled printer analog — full file, alice resolution pattern)
    - backend/src/test/java/com/quantlens/portfolio/PortfolioServiceTest.java (pure-unit test analog — no Spring context)
    - backend/src/test/java/com/quantlens/portfolio/PortfolioControllerIntegrationTest.java (TestRestTemplate + loginAndGetSessionCookie analog)
    - frontend/src/__tests__/components/KpiCard.test.ts (vitest mount analog)
    - frontend/src/api/portfolio.ts (DTO-interfaces-only file analog)
    - 04-VALIDATION.md "Per-Req Verification Map" + "Wave 0 Requirements"; 04-RESEARCH.md "Phase Requirements -> Test Map" (lines 1192-1216)
  </read_first>
  <action>
    Create `frontend/src/api/analytics.ts` exporting TypeScript interfaces mirroring the five Java records exactly (VarResultDto, RiskScorecardDto, CorrelationMatrixDto, AttributionDto, PairResultDto) per 04-PATTERNS.md lines 553-597. Interfaces only, no fetch functions (mirror portfolio.ts). `signal` typed as the literal union.

    Create the five RED backend test files in package `com.quantlens.analytics`. These must COMPILE and reference real symbols, with golden-value constants declared as `0.0 // FILL from printer` and assertions present but expected to fail until Plans 02-03 fill values. Map every Test-Map row from 04-RESEARCH.md to a `@Test` method using the exact method names listed there:
    - RiskCalculatorTest (no @SpringBootTest — pure unit; construct calculator inputs by hand or mark methods that need seed data with a TODO referencing the integration test): sharpe_alice_matchesGoldenValue, annualizedVol_alice_matchesGoldenValue, maxDrawdown_alice_isNonPositive, maxDrawdown_alice_matchesGoldenValue, beta_alice_isAboveOne, beta_alice_matchesGoldenValue, historicalVar_aliceIsPositive, parametricVar_aliceIsPositive. Use AssertJ `isCloseTo(expected, within(tol))` with tolerances from the 04-RESEARCH.md tolerance table.
    - CorrelationCalculatorTest: correlationMatrix_isSymmetric, correlationMatrix_diagonalIsOne, aapl_msft_correlation_matchesGolden.
    - FamaFrenchCalculatorTest: ffRegression_paramsLengthIsFour, ffRegression_rSquaredMatchesGolden, betaMkt_aliceIsPositive, contributions_sumToPortfolioReturn.
    - CointegrationScannerTest: adfPValue_knownStatistic_matches (mackinnonp(-3.0) ≈ 0.034 ±0.01), spreadZScore_formulaCorrect.
    - AnalyticsControllerIntegrationTest extends AbstractPostgresIntegrationTest: copy loginAndGetSessionCookie + authenticatedGet helpers verbatim. Methods: getRisk_unauthenticated_returns401, risk_endpoint_returns200_alice, risk_endpoint_varMethodsLabelled, risk_endpoint_varAmountsMatchGoldenValues, correlation_endpoint_tickersMatchHoldings, attribution_endpoint_hasRequiredFields, pairs_responseHasRequiredFields. Parse JSON with ObjectMapper.

    Create AnalyticsGoldenValuePrinterTest extends AbstractPostgresIntegrationTest, annotated `@Disabled("Golden-value capture — enable once to print seed constants, then re-disable")` on the @Test method. @Autowired the three real services (RiskCalculator, FamaFrenchCalculator, CointegrationScanner). Resolve alice's portfolio via AppUserRepository.findByUsername("alice") + PortfolioRepository.findByUserId (same as GoldenValuePrinterTest). Print SHARPE, ANNUAL_VOL, MAX_DRAWDOWN, BETA, HIST_VAR_PCT, PARAM_VAR_PCT, ALPHA_ANNUALIZED, BETA_MKT, R_SQUARED with `%.8f`, plus the AAPL-MSFT correlation and a sanity print of the MacKinnon p-value for tau=-3.0. (The printer references service methods that return stubs now; it becomes meaningful after Plans 02-03 implement the math.)

    Create the four frontend RED component tests mirroring KpiCard.test.ts: loading/populated/error states for RiskScorecard, CorrelationHeatmap, AttributionChart, PairsTable. Import the (not-yet-created) component from '../../components/<Name>.vue' — these will fail to resolve until Plan 04 creates the components; that is the intended RED state. Use the prop shapes from 04-PATTERNS.md frontend test scaffold.
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot && mvnw.cmd -q -o test-compile"</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd -q test-compile` succeeds (all backend test files compile against real module symbols).
    - AnalyticsGoldenValuePrinterTest contains `@Disabled` and `extends AbstractPostgresIntegrationTest`.
    - AnalyticsControllerIntegrationTest contains `extends AbstractPostgresIntegrationTest` and a method asserting `HttpStatus.UNAUTHORIZED` for an unauthenticated /risk call.
    - frontend/src/api/analytics.ts exports `RiskScorecardDto`, `CorrelationMatrixDto`, `AttributionDto`, `PairResultDto`, `VarResultDto` (grep each).
    - The four frontend test files exist and import their target component path (RED until Plan 04 — documented as intended).
    - `npm run build` in frontend/ succeeds (analytics.ts type-checks; test files are excluded from the build).
  </acceptance_criteria>
  <done>All 10 RED test scaffolds and analytics.ts exist; backend test sources compile; the @Disabled printer is ready to capture golden values; frontend builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/HTTP client -> AnalyticsController | Authenticated session crosses here; principal is the only trusted identity source |
| AnalyticsController -> portfolio::domain / marketdata::domain | In-process Modulith boundary; analytics reads only via allowed named interfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege (IDOR) | AnalyticsController all 4 endpoints | mitigate | portfolioId derived ONLY from `authentication.getName()` via `resolvePortfolioId` (copied verbatim from PortfolioController); no @RequestParam/@PathVariable accepts portfolio/user id |
| T-04-02 | Spoofing | analytics endpoints | mitigate | Unauthenticated requests return 401 (resolvePortfolioId throws ResponseStatusException UNAUTHORIZED); AnalyticsControllerIntegrationTest#getRisk_unauthenticated_returns401 proves it |
| T-04-03 | Tampering | Modulith boundary | mitigate | allowedDependencies limited to marketdata::domain + portfolio::domain; QuantLensModulithTest enforces; no portfolio::service dependency |
| T-04-SC | Tampering | npm/maven installs | accept | No new packages this phase (Hipparchus already in pom, frontend deps already installed — per 04-RESEARCH.md Package Legitimacy Audit: none added) |
</threat_model>

<verification>
- `.\mvnw.cmd -q compile` and `.\mvnw.cmd -q test-compile` both succeed (JAVA_HOME=Temurin 21).
- `.\mvnw.cmd -q -o test -Dtest=QuantLensModulithTest` passes (analytics module boundary valid).
- In frontend/: `npm run build` succeeds.
- Six backend + four frontend test files exist; AnalyticsGoldenValuePrinterTest is @Disabled.
</verification>

<success_criteria>
- analytics Modulith module compiles and passes the module boundary test.
- Five DTO records, four stub services, four-endpoint controller, and the FactorReturnRepository ordered query all exist and compile.
- Ten RED test scaffolds exist (compile on the backend side; frontend component tests intentionally RED pending Plan 04).
- The @Disabled golden-value printer is wired to the three services and ready to run manually.
</success_criteria>

<output>
Create `.planning/phases/04-quant-risk-engine/04-01-SUMMARY.md` when done.
</output>
