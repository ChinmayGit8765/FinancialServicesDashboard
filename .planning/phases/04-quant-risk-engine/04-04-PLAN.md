---
phase: 04-quant-risk-engine
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - frontend/src/stores/portfolio.ts
  - frontend/src/components/RiskScorecard.vue
  - frontend/src/components/CorrelationHeatmap.vue
  - frontend/src/components/AttributionChart.vue
  - frontend/src/components/PairsTable.vue
  - frontend/src/views/DashboardView.vue
  - frontend/src/__tests__/components/RiskScorecard.test.ts
  - frontend/src/__tests__/components/CorrelationHeatmap.test.ts
  - frontend/src/__tests__/components/AttributionChart.test.ts
  - frontend/src/__tests__/components/PairsTable.test.ts
autonomous: false
requirements: [RISK-01, RISK-02, RISK-03, ATTR-01, ARB-01]

must_haves:
  truths:
    - "The Pinia portfolio store exposes risk/correlation/attribution/pairs AsyncState plus fetchRisk/fetchCorrelation/fetchAttribution/fetchPairs, wired into refreshAll and $reset"
    - "RiskScorecard.vue renders Sharpe/vol/maxDrawdown/beta KPI cards plus a side-by-side Historical vs Parametric VaR table with method/confidence/horizon labels"
    - "CorrelationHeatmap.vue renders an ECharts heatmap with visualMap -1..+1 (blue->white->red) across holdings"
    - "AttributionChart.vue renders a contribution bar chart (Alpha, Mkt-RF, SMB, HML)"
    - "PairsTable.vue renders candidate pairs with p-value, hedge ratio, Z-score, and signal badge"
    - "DashboardView replaces the Phase-4 KPI placeholders, the Risk Scorecard slot, and the Correlation Heatmap slot with the real components; attribution + pairs panels are placed in the grid without restructuring existing rows"
    - "All four frontend component tests pass and the full backend suite + frontend build/test stay green"
  artifacts:
    - path: "frontend/src/components/RiskScorecard.vue"
      provides: "Risk KPI cards + dual-VaR table"
      contains: "VaR"
    - path: "frontend/src/components/CorrelationHeatmap.vue"
      provides: "ECharts correlation heatmap"
      contains: "heatmap"
    - path: "frontend/src/components/AttributionChart.vue"
      provides: "FF contribution bar chart"
      contains: "bar"
    - path: "frontend/src/components/PairsTable.vue"
      provides: "Cointegration pairs table"
      contains: "signal"
    - path: "frontend/src/stores/portfolio.ts"
      provides: "4 new analytics AsyncState + fetch actions"
      contains: "fetchRisk"
  key_links:
    - from: "frontend/src/stores/portfolio.ts"
      to: "/api/portfolio/risk"
      via: "fetchRisk axios.get"
      pattern: "api/portfolio/risk"
    - from: "frontend/src/views/DashboardView.vue"
      to: "RiskScorecard.vue / CorrelationHeatmap.vue"
      via: "component import replacing SlotPlaceholder"
      pattern: "RiskScorecard|CorrelationHeatmap"
    - from: "frontend/src/components/CorrelationHeatmap.vue"
      to: "store.correlation"
      via: "props/computed binding"
      pattern: "correlation"
---

<objective>
Build the Phase 4 frontend: extend the Pinia portfolio store with four analytics resources, create the four dashboard panels (RiskScorecard, CorrelationHeatmap, AttributionChart, PairsTable), and wire them into DashboardView by replacing the reserved Phase-4 SlotPlaceholders — turning the four RED frontend component tests GREEN.

This completes the vertical slice: the user can now SEE the golden-tested risk scorecard, correlation heatmap, factor attribution, and pairs scanner in the dashboard, scoped to the selected persona, in the dark theme.

Purpose: Render the credibility core. After this plan, all five Phase-4 success criteria are user-observable in the running app.
Output: Store extension + four Vue components + DashboardView wiring + four passing component tests; `npm run build` and `npm run test` green; full backend suite green. Final human-verify checkpoint confirms the panels render correctly under `docker compose up`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quant-risk-engine/04-CONTEXT.md
@.planning/phases/04-quant-risk-engine/04-RESEARCH.md
@.planning/phases/04-quant-risk-engine/04-PATTERNS.md
@.planning/phases/03-frontend-scaffold/03-UI-SPEC.md
@.planning/phases/04-quant-risk-engine/04-02-SUMMARY.md
@.planning/phases/04-quant-risk-engine/04-03-SUMMARY.md

<interfaces>
<!-- Frontend contracts. analytics.ts interfaces already created in Plan 04-01. -->
frontend/src/api/analytics.ts exports: VarResultDto, RiskScorecardDto, CorrelationMatrixDto, AttributionDto, PairResultDto.

Store pattern — frontend/src/stores/portfolio.ts:
  asyncState<T>(null) -> reactive {data, loading, error}; module-scoped refreshVersion race guard.
  fetchBenchmark (lines 134-147) is the exact template: set loading true, error null; axios.get<T>(url); version-guard; on catch 401 -> 'Session expired' else 'Failed to load <resource>'; finally loading false.
  refreshAll (lines 163-172) Promise.allSettled of all fetches with myVersion.
  $reset (lines 180-186) nulls every resource.
  return block (lines 188-203) exposes state + actions.

Component analogs:
  KpiCard.vue — props label/primary/secondary/delta/loading; RiskScorecard composes multiple KpiCard.
  AllocationChart.vue — VChart (no theme prop; THEME_KEY provided globally in App.vue), three-state loading/error/empty, .chart-panel/.skeleton/.chart-error/.retry-btn/.chart-empty/.sr-only CSS, emit('retry').
  HoldingsTable.vue — table-panel three-state pattern; PairsTable mirrors it.

DashboardView.vue wiring points (current file):
  lines 101-115: two `.kpi-placeholder` KpiCards ("Risk Score — Phase 4", "Sharpe Ratio — Phase 4") -> replace with real RiskScorecard KPI values.
  line 146: `<SlotPlaceholder label="Risk Scorecard — Phase 4" minHeight="280px" />` (col-6) -> RiskScorecard.vue.
  line 151: `<SlotPlaceholder label="Correlation Heatmap — Phase 4" minHeight="240px" />` (col-12) -> CorrelationHeatmap.vue.
  Attribution + Pairs have no reserved slot — add two new grid rows (col-6 + col-6, or col-12 each) between the correlation row (line 152) and the Monte Carlo slot (line 154) WITHOUT altering existing rows.
</interfaces>

<echarts_options>
<!-- From 04-RESEARCH.md / 04-PATTERNS.md — exact. -->
Heatmap: data = [[j, i, matrix[i][j]] ...]; xAxis category dto.tickers (rotate 45); yAxis category [...tickers].reverse(); visualMap min -1 max 1 color ['#ef4444','#f8fafc','#3b82f6']; series type 'heatmap' label show toFixed(2) color '#1e293b'.
Bar: labels ['Alpha','Mkt-RF','SMB','HML']; values [alphaAnnualized, contribMkt, contribSmb, contribHml]*100; per-bar color v>=0 ? '#22c55e' : '#ef4444'; yAxis '{value}%'; label position 'top' '{value}%'.
</echarts_options>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend portfolio store with 4 analytics resources + components RiskScorecard & PairsTable</name>
  <files>frontend/src/stores/portfolio.ts, frontend/src/components/RiskScorecard.vue, frontend/src/components/PairsTable.vue, frontend/src/__tests__/components/RiskScorecard.test.ts, frontend/src/__tests__/components/PairsTable.test.ts</files>
  <read_first>
    - frontend/src/stores/portfolio.ts (full — extend it)
    - frontend/src/components/KpiCard.vue (RiskScorecard composes these)
    - frontend/src/components/HoldingsTable.vue (PairsTable analog — table three-state)
    - frontend/src/api/analytics.ts (DTO interfaces)
    - frontend/src/__tests__/components/RiskScorecard.test.ts + PairsTable.test.ts (RED scaffolds from Plan 01)
    - 04-PATTERNS.md store-extension (lines 603-671) + RiskScorecard.vue (675-743) + PairsTable.vue (870-916)
    - 03-UI-SPEC.md RiskScorecard slot, VaR side-by-side, number formatting, error copy
  </read_first>
  <action>
    Extend portfolio.ts: import the 4 analytics DTO types from '../api/analytics'. Add four `asyncState<...>(null)` declarations (risk, correlation, attribution, pairs). Add four fetch functions (fetchRisk, fetchCorrelation, fetchAttribution, fetchPairs) copying the fetchBenchmark template exactly, hitting '/api/portfolio/risk', '/correlation', '/attribution', '/pairs'; error fallbacks 'Failed to load risk metrics' / '...correlation data' / '...attribution' / '...pairs data'. Add all four to refreshAll's Promise.allSettled (with myVersion) and to $reset. Add all 8 symbols to the return block.

    Create RiskScorecard.vue (`<script setup lang="ts">`): accept props `{ risk: RiskScorecardDto | null, loading: boolean, error: string | null }` and `emit('retry')` (props-driven so the RED test mounts it directly). Compose KpiCard instances: Sharpe (toFixed(2)), Ann. Volatility (formatPercent(annualizedVolatility*100)), Max Drawdown (formatSignedPercent(maxDrawdown*100) with delta), Beta (toFixed(2)). Render a VaR comparison sub-table iterating `risk.var`: columns Method / Confidence / Horizon / Amount / %, using the method/confidence/horizonDays/amount/percentage fields, formatting amount as currency and percentage as percent. Loading: show `.skeleton`. Error: `.chart-error` role="alert" with static copy "Failed to load risk metrics. Check your connection and try again." + Retry button emitting 'retry'. Scoped CSS uses only var(--token) (grid-template-columns auto-fit minmax(160px,1fr)).

    Create PairsTable.vue mirroring HoldingsTable three-state: props `{ pairs: PairResultDto[] | null, loading, error }`, emit('retry'). Table columns Pair (tickerY / tickerX) | p-value (toFixed(3)) | Hedge β (toFixed(3)) | Z-score (toFixed(2)) | Signal (badge classed by signal value). Empty copy "No cointegrated pairs found in current holdings."; error copy + Retry. Scoped CSS tokens only; signal badge styled like the BUY/SELL badge pattern.

    Fill the two RED component tests (loading shows .skeleton; populated shows a metric value e.g. '0.42'; error shows [role="alert"] + .retry-btn) per 04-PATTERNS.md test scaffold.
  </action>
  <verify>
    <automated>cd frontend && cmd /c "npm run test -- RiskScorecard PairsTable"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- RiskScorecard PairsTable` passes (loading/populated/error for both components).
    - portfolio.ts return block exposes risk, correlation, attribution, pairs, fetchRisk, fetchCorrelation, fetchAttribution, fetchPairs; refreshAll contains all four new fetch calls; $reset nulls all four.
    - RiskScorecard.vue references `risk.var` and emits 'retry'; PairsTable.vue renders a signal badge.
    - `npm run build` succeeds (type-checks against analytics.ts DTOs).
  </acceptance_criteria>
  <done>Store exposes 4 analytics resources wired into refreshAll/$reset; RiskScorecard + PairsTable built and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Chart components CorrelationHeatmap + AttributionChart; wire all four into DashboardView</name>
  <files>frontend/src/components/CorrelationHeatmap.vue, frontend/src/components/AttributionChart.vue, frontend/src/views/DashboardView.vue, frontend/src/__tests__/components/CorrelationHeatmap.test.ts, frontend/src/__tests__/components/AttributionChart.test.ts</files>
  <read_first>
    - frontend/src/components/AllocationChart.vue (VChart + three-state + scoped CSS analog — full file)
    - frontend/src/views/DashboardView.vue (full — wiring points at lines 101-115, 146, 151-152)
    - frontend/src/__tests__/components/CorrelationHeatmap.test.ts + AttributionChart.test.ts (RED scaffolds)
    - 04-PATTERNS.md CorrelationHeatmap.vue (747-829) + AttributionChart.vue (833-866) + DashboardView wiring notes
    - 03-UI-SPEC.md ECharts theming, heatmap visualMap, extension-slots reference (lines 569-580)
  </read_first>
  <action>
    Create CorrelationHeatmap.vue (analog AllocationChart.vue): props `{ correlation: CorrelationMatrixDto | null, loading, error }`, emit('retry'). Build the ECharts heatmap `option` computed from the heatmap spec in <echarts_options> (data [j,i,value]; xAxis tickers rotate 45; yAxis tickers reversed; visualMap -1..1 with the three theme colors; heatmap series with label toFixed(2)). VChart with NO theme prop (THEME_KEY is global). Three-state template (loading skeleton 320px height; error .chart-error + Retry; empty "No correlation data available."; else <v-chart>). figure/figcaption per UI-SPEC accessibility. Scoped CSS copied from AllocationChart.

    Create AttributionChart.vue (analog AllocationChart.vue): props `{ attribution: AttributionDto | null, loading, error }`, emit('retry'). Build the bar `option` from the bar spec (Alpha/Mkt-RF/SMB/HML; values *100; green/red per sign; yAxis '{value}%'; top labels). Three-state template; empty "No attribution data available." Scoped CSS tokens only.

    Wire DashboardView.vue:
    - Import RiskScorecard, CorrelationHeatmap, AttributionChart, PairsTable.
    - Add retry handlers retryRisk/retryCorrelation/retryAttribution/retryPairs calling the store fetches.
    - Replace the two `.kpi-placeholder` KpiCards (lines 101-115) with real values bound from store.risk.data (Risk/VaR summary card + Sharpe card) — keep them inside the kpi-strip; show '—' when data null.
    - Replace the Risk Scorecard SlotPlaceholder (line 146, col-6) with `<RiskScorecard :risk="portfolioStore.risk.data" :loading="portfolioStore.risk.loading" :error="portfolioStore.risk.error" @retry="retryRisk" />`.
    - Replace the Correlation Heatmap SlotPlaceholder (line 151, col-12) with `<CorrelationHeatmap ... @retry="retryCorrelation" />`.
    - Add two NEW grid rows between the correlation row and the Monte Carlo slot: an Attribution panel (col-6) and a Pairs panel (col-6), each bound to its store resource. Do NOT modify the Monte Carlo / holdings / transactions / Phase-6 rows.
    Fill the two RED chart component tests (loading skeleton; populated renders v-chart/option without error; error role="alert" + retry) per the scaffold.
  </action>
  <verify>
    <automated>cd frontend && cmd /c "npm run test && npm run build"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` passes all four Phase-4 component tests (RiskScorecard, PairsTable, CorrelationHeatmap, AttributionChart) plus all prior Phase-3 tests.
    - `npm run build` succeeds.
    - DashboardView imports all four components and no longer renders SlotPlaceholder for "Risk Scorecard — Phase 4" or "Correlation Heatmap — Phase 4" (grep confirms those two labels are gone; Monte Carlo / Phase-6 SlotPlaceholders remain).
    - CorrelationHeatmap.vue `option` contains a `'heatmap'` series and visualMap min -1 max 1; AttributionChart.vue `option` contains a `'bar'` series.
  </acceptance_criteria>
  <done>All four panels render in DashboardView replacing the reserved slots; full frontend test + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Phase-4 dashboard panels under docker compose</name>
  <action>Checkpoint task — no code. After Tasks 1-2 land and all automated gates pass, pause for the user to visually verify the four Phase-4 panels in the running app (see how-to-verify). Do NOT auto-advance; this gate is blocking.</action>
  <what-built>The full Phase-4 vertical slice: backend analytics endpoints (/risk, /correlation, /attribution, /pairs) returning golden-tested data, and four dashboard panels (RiskScorecard with dual-VaR table, CorrelationHeatmap, AttributionChart, PairsTable) rendered in DashboardView. Backend suite + frontend build/test are green.</what-built>
  <how-to-verify>
    1. From the repo root run `docker compose up --build` and wait for db (healthy), backend (healthy), frontend (up).
    2. Open http://localhost:8080 (or the frontend port from docker-compose) and log in as Alice (alice / demo1234).
    3. Confirm the KPI strip shows real Risk Score / Sharpe values (no "Phase 4" placeholders).
    4. Confirm the Risk Scorecard panel shows Sharpe, Ann. Volatility, Max Drawdown, Beta, and a side-by-side Historical vs Parametric VaR table with method/confidence/horizon labels and positive amounts.
    5. Confirm the Correlation Heatmap renders with the blue->white->red scale (-1..+1), tickers on both axes, cell values shown.
    6. Confirm the Attribution bar chart shows Alpha / Mkt-RF / SMB / HML contributions (green/red bars) and the Pairs table lists candidate pairs with p-value, hedge β, Z-score, and a signal badge.
    7. Switch persona to Bob, then Charlie — confirm all four panels re-scope without a full page reload and show no console errors.
  </how-to-verify>
  <verify><human-check>User confirms all four panels render correctly, persona switch re-scopes without reload, no console errors.</human-check></verify>
  <resume-signal>Type "approved" or describe any rendering/data issues (e.g. wrong colors, empty panel, console error).</resume-signal>
  <done>User has visually verified the four Phase-4 panels under docker compose and approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/portfolio/* analytics endpoints | Session cookie crosses here; backend resolves portfolio from principal |
| store -> rendered DOM | API data rendered; error objects must never reach the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege (IDOR) | analytics fetches | mitigate | store fetches call /api/portfolio/<metric> with NO id param; backend derives portfolio from principal (Plan 01 control); persona switch re-auths via existing flow |
| T-04-08 | Information Disclosure | Vue error states | mitigate | components show static error copy + Retry; never render the raw error object (T-03-08 pattern); 401 maps to 'Session expired' + global interceptor redirect |
| T-04-09 | Spoofing (stale persona data) | refreshAll race | mitigate | refreshVersion guard on all four new fetches (copied from fetchBenchmark) discards stale writes on rapid persona switch |
| T-04-SC | Tampering | npm installs | accept | No new packages (echarts/vue-echarts already installed; 04-RESEARCH.md Package Legitimacy Audit: none added) |
</threat_model>

<verification>
- In frontend/: `npm run test` (all Phase-3 + 4 Phase-4 component tests green) and `npm run build` succeed.
- Backend: `.\mvnw.cmd verify` full suite green (JAVA_HOME=Temurin 21); AnalyticsGoldenValuePrinterTest remains @Disabled.
- Human-verify checkpoint approved: all four panels render correctly under `docker compose up`, persona switch re-scopes without reload, no console errors.
</verification>

<success_criteria>
- RISK-01/02/03 + ATTR-01 + ARB-01 are user-observable: scorecard (with dual VaR), correlation heatmap, attribution bar chart, and pairs table all render in the dashboard from the golden-tested endpoints.
- Phase-4 SlotPlaceholders replaced without restructuring existing DashboardView rows.
- Full backend suite + frontend build/test green; analytics Modulith boundary intact.
</success_criteria>

<output>
Create `.planning/phases/04-quant-risk-engine/04-04-SUMMARY.md` when done.
</output>
