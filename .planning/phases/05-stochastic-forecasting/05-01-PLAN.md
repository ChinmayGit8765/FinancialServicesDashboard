---
phase: 05-stochastic-forecasting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/java/com/quantlens/analytics/api/ModelType.java
  - backend/src/main/java/com/quantlens/analytics/api/ForecastDto.java
  - backend/src/main/java/com/quantlens/analytics/api/ForecastController.java
  - backend/src/main/java/com/quantlens/analytics/service/ForecastService.java
  - backend/src/test/java/com/quantlens/analytics/ForecastMathHandComputedTest.java
  - backend/src/test/java/com/quantlens/analytics/ForecastStructuralTest.java
  - backend/src/test/java/com/quantlens/analytics/ForecastFinmathIntegrationTest.java
  - backend/src/test/java/com/quantlens/analytics/ForecastControllerIntegrationTest.java
  - frontend/src/__tests__/components/MonteCarloFanChart.test.ts
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "finmath-lib 6.1.7 is on the backend classpath and the project compiles on Java 21"
    - "ForecastDto and ModelType contracts exist and are importable by service, controller, and tests"
    - "All five Phase-5 test files exist and FAIL for the right reason (RED — no implementation yet)"
  artifacts:
    - path: "backend/pom.xml"
      provides: "net.finmath:finmath-lib:6.1.7 dependency (no commons-math3 exclusion)"
      contains: "finmath-lib"
    - path: "backend/src/main/java/com/quantlens/analytics/api/ModelType.java"
      provides: "GBM, JUMP_DIFFUSION, HESTON, BOOTSTRAP enum"
      contains: "enum ModelType"
    - path: "backend/src/main/java/com/quantlens/analytics/api/ForecastDto.java"
      provides: "Forecast result record (model, horizonDays, p5..p95 double[])"
      contains: "record ForecastDto"
    - path: "backend/src/main/java/com/quantlens/analytics/service/ForecastService.java"
      provides: "ForecastService bean stub (Feller guard, calibration wiring, throws for un-implemented models)"
      contains: "class ForecastService"
    - path: "backend/src/main/java/com/quantlens/analytics/api/ForecastController.java"
      provides: "GET /api/portfolio/forecast endpoint (principal-scoped, horizon clamp)"
      contains: "/forecast"
    - path: "backend/src/test/java/com/quantlens/analytics/ForecastMathHandComputedTest.java"
      provides: "HC-11 GBM analytic-mean / Ito-median anchor (no Spring context)"
      contains: "hc11"
    - path: "backend/src/test/java/com/quantlens/analytics/ForecastStructuralTest.java"
      provides: "percentile-ordering, band-widening, seed=42 reproducibility, bootstrap-moment tests"
      contains: "class ForecastStructuralTest"
    - path: "backend/src/test/java/com/quantlens/analytics/ForecastFinmathIntegrationTest.java"
      provides: "finmath instantiation + path-generation smoke (no Spring context)"
      contains: "class ForecastFinmathIntegrationTest"
    - path: "backend/src/test/java/com/quantlens/analytics/ForecastControllerIntegrationTest.java"
      provides: "4-model endpoint integration test extending AbstractPostgresIntegrationTest"
      contains: "class ForecastControllerIntegrationTest"
    - path: "frontend/src/__tests__/components/MonteCarloFanChart.test.ts"
      provides: "Vitest scaffold: renders 5 series, selector switches model"
      contains: "MonteCarloFanChart"
  key_links:
    - from: "backend/pom.xml"
      to: "net.finmath:finmath-lib:6.1.7"
      via: "Maven dependency (transitive commons-math3:3.6.1 kept)"
      pattern: "finmath-lib"
    - from: "ForecastFinmathIntegrationTest"
      to: "net.finmath.montecarlo classes"
      via: "compile-time import (fails if finmath absent)"
      pattern: "net\\.finmath"
---

<objective>
Wave 0 for Phase 5. Add the finmath-lib dependency, create the backend contracts (ModelType enum, ForecastDto record, stub ForecastService, ForecastController endpoint), and lay down all five Phase-5 test scaffolds in RED state.

Purpose: This is the interface-and-test foundation. Plans 05-02 (backend engine) and 05-03 (frontend) build against the contracts created here and turn these RED tests GREEN. The finmath dependency is a hard compile gate — nothing in the backend MC engine compiles until it is on the classpath. HC-11 (hand-computed GBM analytic-mean anchor) is created here so correctness validation is never seed-circular.

Output: finmath-lib in pom.xml; ModelType, ForecastDto, ForecastController, ForecastService stub compiled; five test files present and failing for the right reason (assertions unmet / model not yet implemented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stochastic-forecasting/05-RESEARCH.md
@.planning/phases/05-stochastic-forecasting/05-PATTERNS.md
@.planning/phases/05-stochastic-forecasting/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Verified from the codebase — no exploration required. -->

Reusable backend helpers (com.quantlens.analytics.service.RiskCalculator):
- `List<DateValueDto> buildEquityCurveLocal(List<Position> positions)` — INSTANCE method (package-accessible). Inject RiskCalculator into ForecastService.
- `static double[] logReturns(List<DateValueDto> curve)` — STATIC helper (line 261).
- RiskCalculator constructor deps: PositionRepository, OhlcvBarRepository, SecurityRepository, FactorReturnRepository.
- Current-portfolio-value pattern uses `latestCloseBySecurityId(secIds)` (private in RiskCalculator) which delegates to `OhlcvBarRepository.findLatestBarBySecurityIds(securityIds)`.

Reusable controller pattern (com.quantlens.analytics.api.AnalyticsController):
- `@RequestMapping("/api/portfolio")` on the class.
- `private Long resolvePortfolioId(Authentication authentication)` at lines 141-148 — copy VERBATIM. Throws 401 if unauthenticated; resolves portfolioId via `PortfolioRepository.findPortfolioIdByUsername(username)`. NEVER accepts portfolioId from request.

Analytics Modulith module boundary (package-info.java):
- `allowedDependencies = {"marketdata::domain", "portfolio::domain"}` — ForecastService may import com.quantlens.portfolio.domain.* and the local com.quantlens.analytics.service.DateValueDto. Do NOT import portfolio::service.

Test infra:
- `com.quantlens.AbstractPostgresIntegrationTest` — base class for Testcontainers integration tests (seeded Postgres: alice/bob/charlie portfolios).
- AnalyticsControllerIntegrationTest lines 214-238: `loginAndGetSessionCookie(String username)` + `authenticatedGet(String path, String cookie)` — copy verbatim.
- RiskMathHandComputedTest — analog for no-Spring-context hand-computed tests (Derivation javadoc block + assertThat(...).as(...).isCloseTo(expected, within(tol))).
</interfaces>

<finmath_api>
<!-- From 05-RESEARCH.md §finmath-lib 6.1.7 Exact API Reference. Constants below are LOCKED. -->
Constants (CONTEXT.md locked): MC_SEED=42, NUM_PATHS=5000.
Heston illustrative: KAPPA=2.0, THETA=0.04, XI=0.3, RHO=-0.7, V0=0.04 (Feller: 2·2·0.04=0.16 > 0.09=0.3²).
Jump illustrative: LAMBDA=0.10, MU_J=-0.10, SIGMA_J=0.15.
finmath chain: TimeDiscretizationFromArray(0.0, horizonDays, 1.0/252.0) → BrownianMotionFromMersenneRandomNumbers(td, nFactors, 5000, 42) → model → EulerSchemeFromProcessModel(model, bm) → MonteCarloAssetModel → getAssetValue(t,0).getRealizations().
GBM nFactors=1; Heston nFactors=2 + Scheme.FULL_TRUNCATION; Merton via MonteCarloMertonModel(td, 5000, 42, S0, mu, sigma, lambda, muJ, sigmaJ, RandomVariableFromArrayFactory).
OPEN QUESTIONS to verify at compile time (Assumptions Log A4/A5 + Open Questions 1): HestonModel constructor theta/kappa parameter ORDER; MonteCarloMertonModel constructor arity. The finmath smoke test in this plan is the verification gate.
</finmath_api>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add finmath-lib dependency and verify classpath</name>
  <files>backend/pom.xml</files>
  <read_first>
    - backend/pom.xml — existing &lt;dependencies&gt; block (hipparchus-stat / hipparchus-core entries are the insertion neighbors)
    - PATTERNS analog: 05-PATTERNS.md §backend/pom.xml (config — new dependency)
    - RESEARCH excerpt: 05-RESEARCH.md §Standard Stack — New Addition; §Pitfall 3 (commons-math3 coexistence)
  </read_first>
  <action>
    Add a single dependency to backend/pom.xml inside &lt;dependencies&gt;: groupId net.finmath, artifactId finmath-lib, version 6.1.7. Add a comment above it noting that the transitive commons-math3:3.6.1 is harmless and MUST NOT be excluded (finmath's MertonModel Poisson process needs it; Hipparchus uses a different groupId org.hipparchus so there is zero class collision). Do NOT add any &lt;exclusions&gt; block. Do NOT change any other dependency. The version is LOCKED at 6.1.7 per CONTEXT.md and the Package Legitimacy Audit (approved — academic library by Prof. Christian Fries, verified on Maven Central).
  </action>
  <acceptance_criteria>
    - `dependency:tree` shows exactly one commons-math3 entry at version 3.6.1, sourced from finmath-lib.
    - Backend compiles on Java 21 with finmath classes resolvable.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q dependency:tree | grep -c "finmath-lib:jar:6.1.7"</automated>
  </verify>
  <done>finmath-lib:6.1.7 present in dependency tree; exactly one commons-math3:3.6.1 transitive entry; no exclusions added.</done>
</task>

<task type="auto">
  <name>Task 2: Create ModelType enum, ForecastDto record, ForecastService stub, and ForecastController endpoint</name>
  <files>backend/src/main/java/com/quantlens/analytics/api/ModelType.java, backend/src/main/java/com/quantlens/analytics/api/ForecastDto.java, backend/src/main/java/com/quantlens/analytics/service/ForecastService.java, backend/src/main/java/com/quantlens/analytics/api/ForecastController.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/analytics/api/RiskScorecardDto.java — record style analog (javadoc @param per field, no @JsonProperty)
    - backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java — controller analog (class @RequestMapping, resolvePortfolioId lines 141-148, IDOR javadoc lines 19-43)
    - backend/src/main/java/com/quantlens/analytics/service/RiskCalculator.java — service analog (lines 55-92 declaration + calibration; logReturns line 261; buildEquityCurveLocal line 281; latestCloseBySecurityId line 385)
    - PATTERNS analog: 05-PATTERNS.md §ForecastDto, §ModelType, §ForecastController, §ForecastService
    - RESEARCH excerpt: 05-RESEARCH.md §Calibration; §Architectural Responsibility Map; §Security Domain
  </read_first>
  <action>
    Create four files in com.quantlens.analytics. (1) ModelType enum in the api package with exactly GBM, JUMP_DIFFUSION, HESTON, BOOTSTRAP (used as a @RequestParam — Spring converts the query string by name and emits 400 MethodArgumentTypeMismatchException for unknown values; document this in the enum javadoc — this is the SIM-02 DoS/Tampering mitigation T-05-02).
    (2) ForecastDto record in the api package: fields (ModelType model, int horizonDays, double[] p5, double[] p25, double[] p50, double[] p75, double[] p95), javadoc @param per field, no @JsonProperty.
    (3) ForecastService @Service @Transactional(readOnly=true) in the service package. Constructor injects PositionRepository, RiskCalculator, OhlcvBarRepository (needed for current portfolio value). Declare the LOCKED constants as private static final: MC_SEED=42, NUM_PATHS=5000, HESTON_KAPPA=2.0, HESTON_THETA=0.04, HESTON_XI=0.3, HESTON_RHO=-0.7, HESTON_V0=0.04, JUMP_LAMBDA=0.10, JUMP_MU_J=-0.10, JUMP_SIGMA_J=0.15. In the constructor, enforce the Feller condition fail-fast: if 2·HESTON_KAPPA·HESTON_THETA &lt;= HESTON_XI·HESTON_XI throw IllegalStateException. Implement a public `ForecastDto forecast(Long portfolioId, ModelType model, int horizonDays)` that performs calibration (buildEquityCurveLocal + RiskCalculator.logReturns → DescriptiveStatistics → annualizedMu = mean·252, annualizedSigma = stdev·√252; guard fewer than 2 returns with IllegalArgumentException) and computes initialValue (sum of qty·latestClose for long positions, mirroring RiskCalculator's latestCloseBySecurityId pattern), then `throw new UnsupportedOperationException("model engine implemented in 05-02")` for all models. This is a STUB — the engine arrives in Plan 05-02. Do NOT inline finmath simulation here. Stay within the analytics module boundary (import portfolio::domain + local service.DateValueDto only; no portfolio::service).
    (4) ForecastController @RestController @RequestMapping("/api/portfolio") in the api package. Constructor injects PortfolioRepository + ForecastService. GET /forecast with @Transactional(readOnly=true): params Authentication, @RequestParam(defaultValue="GBM") ModelType model, @RequestParam(defaultValue="252") int horizon. Resolve portfolioId via a VERBATIM copy of resolvePortfolioId from AnalyticsController (IDOR T-05-01 mitigation — portfolioId NEVER from request). Clamp horizon to [1,504] via Math.max(1, Math.min(504, horizon)) (DoS T-05-03). Return ResponseEntity.ok(forecastService.forecast(portfolioId, model, clampedHorizon)). Copy the IDOR-prevention class javadoc from AnalyticsController, adapted to explain the model + horizon params.
  </action>
  <acceptance_criteria>
    - All four files compile.
    - ModelType has exactly the four enum constants.
    - ForecastService constructor throws nothing for the locked Feller-satisfying params; forecast() reaches model dispatch and throws UnsupportedOperationException (stub state).
    - ForecastController.getForecast clamps horizon to [1,504] and never reads portfolioId from a request param.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q compile 2>&1 | grep -v '^#' | grep -ci "BUILD FAILURE" </automated>
  </verify>
  <done>Contracts and stub compile; Modulith boundary respected; resolvePortfolioId copied verbatim; horizon clamp + ModelType enum mitigations in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lay down all five RED test scaffolds (HC-11, structural, finmath smoke, controller integration, frontend)</name>
  <files>backend/src/test/java/com/quantlens/analytics/ForecastMathHandComputedTest.java, backend/src/test/java/com/quantlens/analytics/ForecastStructuralTest.java, backend/src/test/java/com/quantlens/analytics/ForecastFinmathIntegrationTest.java, backend/src/test/java/com/quantlens/analytics/ForecastControllerIntegrationTest.java, frontend/src/__tests__/components/MonteCarloFanChart.test.ts</files>
  <read_first>
    - backend/src/test/java/com/quantlens/analytics/RiskMathHandComputedTest.java — HC analog (no-Spring derivation javadoc + isCloseTo/within)
    - backend/src/test/java/com/quantlens/analytics/RiskCalculatorTest.java — structural analog
    - backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java — integration analog (auth helpers lines 214-238; JSON-shape assertions)
    - frontend/src/__tests__ existing component tests + frontend/src/components/PnlChart.vue / AllocationChart.vue (component-test analogs)
    - PATTERNS analog: 05-PATTERNS.md §ForecastMathHandComputedTest, §ForecastStructuralTest, §ForecastFinmathIntegrationTest, §ForecastControllerIntegrationTest, §MonteCarloFanChart.vue
    - RESEARCH excerpt: 05-RESEARCH.md §Hand-Computed GBM Analytic-Mean Anchor (HC-11); §Validation Architecture; §Complete GBM/Heston/Bootstrap Setup
  </read_first>
  <behavior>
    - HC-11 (ForecastMathHandComputedTest, no Spring context): with S₀=100, μ=0.10, σ=0.20, T=1y over NUM_PATHS paths, mean(S_T) ≈ 100·e^0.10 = 110.517 within 1%; median(S_T) ≈ 100·e^0.08 = 108.329 within 0.5%; median &lt; mean (log-normal sign-flip guard). Derivation is in the javadoc, computed externally — NOT from ForecastService output.
    - Structural (ForecastStructuralTest, extends AbstractPostgresIntegrationTest): for all 4 models, p5≤p25≤p50≤p75≤p95 at every step; GBM band width at t=252 &gt; at t=1; two seed=42 runs produce byte-identical p50 arrays; bootstrap output mean log-return within 5% of historical mean.
    - finmath smoke (ForecastFinmathIntegrationTest, no Spring context): GBM finmath chain instantiates and getAssetValue(1,0).getRealizations().length == 5000 without exception; Heston Feller assertion (2κθ&gt;ξ²) holds and the model instantiates; (verifies Open Questions: Heston theta/kappa order via a mean-reversion-direction check E[variance_t] trends toward THETA; MonteCarloMertonModel constructor arity compiles).
    - Controller integration (ForecastControllerIntegrationTest, extends AbstractPostgresIntegrationTest): unauthenticated GET /api/portfolio/forecast → 401; authenticated GET ?model=GBM&horizon=252 → 200 with body.model=="GBM", body.horizonDays==252, body.p50 is array of size 252; the four models each return 200 with distinct band shapes.
    - Frontend (MonteCarloFanChart.test.ts): component mounts with a forecast prop and renders 5 ECharts series; clicking a model toggle button changes selectedModel and emits update:model.
  </behavior>
  <action>
    Create the five test files in RED state. They MUST compile but FAIL (HC-11/structural/integration assertions unmet because the engine throws UnsupportedOperationException; finmath smoke fails only if finmath APIs are wrong — that is the intended verification of Open Questions). For each backend test, copy the class header / auth-helper / derivation-javadoc conventions from the named analog. In ForecastControllerIntegrationTest copy loginAndGetSessionCookie + authenticatedGet VERBATIM from AnalyticsControllerIntegrationTest. Write real assertions per the &lt;behavior&gt; block — no `assertTrue(true)` placeholders. The HC-11 test wires the actual GBM simulation call (via ForecastService or a direct finmath GBM chain per RESEARCH §Complete GBM Setup) and asserts against the hand-derived 110.517 / 108.329 constants. Mark nothing @Disabled. Do NOT inline production simulation logic into tests beyond what HC-11/finmath-smoke require to exercise the API. The frontend test follows the existing __tests__ component-test pattern and mounts MonteCarloFanChart (which does not yet exist — the test import is RED until Plan 05-03; this is expected and is the frontend Wave-0 gate).
  </action>
  <acceptance_criteria>
    - All four backend test files compile (finmath-smoke compiling proves finmath API names from RESEARCH are correct or surfaces the exact mismatch to fix).
    - Backend Forecast* tests FAIL with engine-not-implemented / assertion errors (RED), not compile errors, except where finmath API verification legitimately requires a name fix.
    - Frontend test exists and references MonteCarloFanChart + a model-selector interaction.
    - HC-11 constants 110.517 and 108.329 appear in ForecastMathHandComputedTest.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q test-compile 2>&1 | grep -v '^#' | grep -ci "BUILD FAILURE"</automated>
  </verify>
  <done>Five test files exist; backend tests compile and are RED for the right reason (engine stub); HC-11 anchors to externally-derived constants; finmath smoke compiles, confirming the finmath API surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/portfolio/forecast | Untrusted query params (model, horizon) cross here; session cookie authenticates |
| ForecastService → finmath/Hipparchus | Internal compute; no external input beyond calibrated portfolio data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ForecastController.getForecast | mitigate | resolvePortfolioId(authentication) copied verbatim from AnalyticsController; portfolioId NEVER read from @RequestParam/@PathVariable. Unauthenticated → 401. |
| T-05-02 | Tampering | model @RequestParam | mitigate | Typed as ModelType enum; unknown value → 400 MethodArgumentTypeMismatchException automatically (no string switch on raw input). |
| T-05-03 | Denial of Service | horizon @RequestParam | mitigate | Clamp to [1,504] via Math.max(1, Math.min(504, horizon)); path count is server-fixed at 5000 (not client-controllable). |
| T-05-SC | Tampering | finmath-lib install | mitigate | Single new package net.finmath:finmath-lib:6.1.7 — Package Legitimacy Audit verdict Approved (academic library, Maven Central verified, 14-yr provenance). No [ASSUMED]/[SUS]/[SLOP]; no human checkpoint required. dependency:tree verifies exactly one commons-math3:3.6.1. |
</threat_model>

<verification>
- `dependency:tree` shows finmath-lib:6.1.7 and exactly one commons-math3:3.6.1.
- `./mvnw compile` succeeds (contracts + stub).
- `./mvnw test-compile` succeeds (all backend test scaffolds compile — finmath API surface confirmed).
- Backend Forecast* tests are RED (engine not implemented) — confirms Wave 0 leaves real failing tests for 05-02 to turn green.
- frontend MonteCarloFanChart.test.ts exists referencing the not-yet-built component (RED for 05-03).
</verification>

<success_criteria>
- finmath-lib 6.1.7 on classpath; project compiles on Java 21; no commons-math3 exclusion.
- ModelType, ForecastDto, ForecastController, ForecastService stub compile within the analytics Modulith boundary.
- All five Phase-5 test files exist; backend tests compile and fail for the right reason; HC-11 anchors to externally-derived constants (not seed-circular).
- Open finmath questions (Heston param order, Merton arity) are surfaced/resolved at test-compile time.
</success_criteria>

<output>
Create `.planning/phases/05-stochastic-forecasting/05-01-SUMMARY.md` when done. Record: confirmed finmath API signatures (especially HestonModel theta/kappa order and MonteCarloMertonModel arity), any name corrections made vs RESEARCH, and the exact RED failure list handed to 05-02.
</output>
