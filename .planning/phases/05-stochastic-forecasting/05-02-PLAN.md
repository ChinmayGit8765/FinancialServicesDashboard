---
phase: 05-stochastic-forecasting
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/src/main/java/com/quantlens/analytics/service/ForecastService.java
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "GBM forecast E[S_T] matches the analytic mean S₀·e^(μT) within 1% and the median is Ito-correct (HC-11 green)"
    - "All four models (GBM, JUMP_DIFFUSION, HESTON, BOOTSTRAP) return monotone p5≤p25≤p50≤p75≤p95 bands at every step"
    - "Two seed=42 runs of the same forecast produce byte-identical bands (reproducible for README screenshots)"
    - "GET /api/portfolio/forecast returns 200 with the correct JSON shape for all four models"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/analytics/service/ForecastService.java"
      provides: "4-model Monte Carlo engine + per-step percentile extraction returning ForecastDto"
      contains: "extractPercentiles"
      min_lines: 150
  key_links:
    - from: "ForecastService.forecast"
      to: "RiskCalculator.buildEquityCurveLocal + RiskCalculator.logReturns"
      via: "calibration of annualized μ, σ from portfolio daily log returns"
      pattern: "logReturns"
    - from: "ForecastService GBM/Heston/Merton runners"
      to: "finmath MonteCarloAssetModel.getAssetValue(t,0).getRealizations()"
      via: "EulerSchemeFromProcessModel over BrownianMotionFromMersenneRandomNumbers(seed=42)"
      pattern: "getRealizations"
    - from: "ForecastService bootstrap runner"
      to: "Hipparchus MersenneTwister(42)"
      via: "block resample of historical log returns, L=max(10,√H)"
      pattern: "MersenneTwister"
---

<objective>
Implement the four Monte Carlo model runners and per-step percentile extraction inside ForecastService, replacing the stub from Plan 05-01. This turns the backend RED tests (HC-11, structural, finmath smoke, controller integration) GREEN.

Purpose: This is the quant core — the credibility-bearing math. GBM must be Ito-correct (the headline pitfall), Heston must satisfy Feller and produce stochastic-vol behavior, Merton must add left-tail jump risk, and the block bootstrap must preserve historical moments and volatility clustering. All four must be reproducible under seed=42 and return monotone percentile bands.

Output: A complete ForecastService whose `forecast(portfolioId, model, horizonDays)` dispatches to one of four runners, extracts p5/p25/p50/p75/p95 per step, and returns a ForecastDto. Backend suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-stochastic-forecasting/05-RESEARCH.md
@.planning/phases/05-stochastic-forecasting/05-PATTERNS.md
@.planning/phases/05-stochastic-forecasting/05-VALIDATION.md
@.planning/phases/05-stochastic-forecasting/05-01-SUMMARY.md

<interfaces>
<!-- Contracts already created in Plan 05-01 (read its SUMMARY for confirmed finmath signatures). -->
ForecastService stub (from 05-01): constructor injects PositionRepository, RiskCalculator, OhlcvBarRepository; locked constants declared; Feller guard in constructor; `forecast(Long, ModelType, int)` does calibration + initialValue then throws UnsupportedOperationException at model dispatch.
ForecastDto record: (ModelType model, int horizonDays, double[] p5, double[] p25, double[] p50, double[] p75, double[] p95). Accessors: model(), horizonDays(), p5()..p95().
ModelType enum: GBM, JUMP_DIFFUSION, HESTON, BOOTSTRAP.

Reusable (com.quantlens.analytics.service.RiskCalculator):
- instance `buildEquityCurveLocal(List<Position>)`; static `logReturns(List<DateValueDto>)`.
- current-value pattern: OhlcvBarRepository.findLatestBarBySecurityIds(securityIds).
</interfaces>

<finmath_chain>
<!-- LOCKED from 05-RESEARCH.md §finmath API + confirmed in 05-01-SUMMARY. Use the signatures SUMMARY records. -->
Common: TimeDiscretizationFromArray(0.0, horizonDays, 1.0/252.0); RandomVariableFromArrayFactory rvf; BrownianMotionFromMersenneRandomNumbers(td, nFactors, 5000, 42); MonteCarloAssetModel(new EulerSchemeFromProcessModel(model, bm)); loop t=1..horizonDays: sim.getAssetValue(t,0).getRealizations() → double[5000].
GBM: nFactors=1; BlackScholesModel(initialValue, annualizedMu, annualizedSigma, rvf). finmath evolves log-space with (μ−σ²/2)dt automatically — Ito-correct by construction; do NOT hand-add the drift term.
Merton: MonteCarloMertonModel(td, 5000, 42, initialValue, annualizedMu, annualizedSigma, JUMP_LAMBDA, JUMP_MU_J, JUMP_SIGMA_J, rvf) — it IS an AssetModelMonteCarloSimulationModel; use directly (verify arity from 05-01-SUMMARY).
Heston: nFactors=2; HestonModel(rvf-wrapped: initialValue, annualizedMu, Math.sqrt(HESTON_V0), 0.0, THETA, KAPPA, XI, RHO ... in the ORDER confirmed by 05-01-SUMMARY), HestonModel.Scheme.FULL_TRUNCATION, rvf.
Bootstrap (pure Java, Hipparchus MersenneTwister(42)): L=Math.max(10,(int)Math.sqrt(H)); blockStart=rng.nextInt(H-L+1); compound exp(logReturn) over blocks until horizonDays filled.
Percentile extraction (nearest-rank): sorted=clone+Arrays.sort; index=clamp(ceil(p·n)-1,0,n-1) for p in {.05,.25,.50,.75,.95}. Monotone by construction.
</finmath_chain>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GBM, Merton, and Heston finmath runners + percentile extraction</name>
  <files>backend/src/main/java/com/quantlens/analytics/service/ForecastService.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/analytics/service/ForecastService.java — the stub from 05-01 (calibration + initialValue already wired; dispatch throws)
    - .planning/phases/05-stochastic-forecasting/05-01-SUMMARY.md — CONFIRMED finmath signatures (Heston theta/kappa order, Merton arity)
    - PATTERNS analog: 05-PATTERNS.md §ForecastService (percentile helper, error handling)
    - RESEARCH excerpt: 05-RESEARCH.md §Step 3a/3b/3c, §Step 4, §Step 5, §Complete GBM Setup, §Complete Heston Setup, §Pitfall 1 (Ito), §Pitfall 2 (Feller), §Pitfall 4 (n/a backend), §Pitfall 5 (dt scaling)
  </read_first>
  <behavior>
    - HC-11 (ForecastMathHandComputedTest): GBM mean(S_T) within 1% of S₀·e^(μT); median within 0.5% of S₀·e^((μ−σ²/2)T); median &lt; mean.
    - finmath smoke (ForecastFinmathIntegrationTest): GBM/Heston/Merton instantiate; getAssetValue(t,0).getRealizations().length == 5000; Heston E[variance] reverts toward THETA (correct param order).
    - Structural (GBM/HESTON/JUMP_DIFFUSION portions): p5≤p25≤p50≤p75≤p95 at every step; GBM band widens with horizon; seed=42 reproducibility (byte-identical p50).
  </behavior>
  <action>
    Replace the model-dispatch in forecast() with a switch on ModelType delegating to private runners. Implement runGbm, runMerton, runHeston using the finmath chain in &lt;finmath_chain&gt; with the EXACT signatures recorded in 05-01-SUMMARY (use that SUMMARY as the source of truth for Heston parameter order and Merton arity — do not re-guess). All three build a double[horizonDays][5] band matrix: for t=1..horizonDays call sim.getAssetValue(t,0).getRealizations() and pass to a private static extractPercentiles(double[]) (nearest-rank, clone+sort+clamped index) returning {p5,p25,p50,p75,p95}; then transpose into five double[horizonDays] arrays for the DTO. GBM uses BlackScholesModel — rely on finmath's built-in (μ−σ²/2) log-space drift; do NOT add the Ito term manually (Pitfall 1). Heston uses 2 Brownian factors + FULL_TRUNCATION; the Feller guard is already in the constructor. Merton uses MonteCarloMertonModel with the illustrative jump constants. Add the private static clamp(int,int,int) helper. Return new ForecastDto(model, horizonDays, p5, p25, p50, p75, p95). Do NOT implement BOOTSTRAP in this task (next task). Keep the bootstrap branch throwing UnsupportedOperationException until Task 2.
  </action>
  <acceptance_criteria>
    - ForecastMathHandComputedTest passes (HC-11 green — Ito-correct mean + median).
    - ForecastFinmathIntegrationTest passes (3 finmath models instantiate + generate 5000-path realizations; Heston reverts to THETA).
    - GBM/HESTON/JUMP_DIFFUSION structural assertions (ordering, widening, reproducibility) pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q test -Dtest=ForecastMathHandComputedTest,ForecastFinmathIntegrationTest 2>&1 | grep -v '^#' | grep -ci "BUILD SUCCESS"</automated>
  </verify>
  <done>Three finmath runners implemented with confirmed signatures; HC-11 + finmath smoke green; percentile bands monotone and reproducible under seed=42.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement historical block bootstrap runner; full backend suite green</name>
  <files>backend/src/main/java/com/quantlens/analytics/service/ForecastService.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/analytics/service/ForecastService.java — runners + extractPercentiles from Task 1
    - PATTERNS analog: 05-PATTERNS.md §ForecastService (Key Accessor Notes — MersenneTwister)
    - RESEARCH excerpt: 05-RESEARCH.md §Historical Block Bootstrap (algorithm, L≈max(10,√H), boundary), §Pitfall 6 (block boundary), §Complete Block Bootstrap
  </read_first>
  <behavior>
    - Bootstrap output mean log-return within 5% of the historical mean log-return (moment preservation).
    - Bootstrap bands are monotone p5≤p25≤p50≤p75≤p95 at every step and reproducible under seed=42.
    - Controller integration (ForecastControllerIntegrationTest): all 4 models return 200 with correct JSON shape (model, horizonDays==252, p50 array size 252); 401 when unauthenticated; the four models give distinct band shapes.
  </behavior>
  <action>
    Implement runBootstrap as pure Java using Hipparchus MersenneTwister(MC_SEED). Compute H = dailyLogReturns.length and L = Math.max(10, (int)Math.sqrt(H)). For each of NUM_PATHS paths, start at initialValue and repeatedly pick blockStart = rng.nextInt(H - L + 1) (boundary-safe per Pitfall 6), then compound currentValue *= Math.exp(dailyLogReturns[i]) over the block until horizonDays steps are filled, recording the per-step value. Build the step-major value buffer and pass each step's 5000 values to extractPercentiles (reuse the Task-1 helper). Wire the BOOTSTRAP branch in the forecast() switch to runBootstrap. Document the block-length choice (preserves ~monthly autocorrelation / volatility clustering vs i.i.d.) in a code comment for the MODELS.md author to reference. After this task, the full backend suite (including ForecastStructuralTest bootstrap-moment + ForecastControllerIntegrationTest 4-model) must be green.
  </action>
  <acceptance_criteria>
    - ForecastStructuralTest passes (all 4 models monotone + widening + reproducibility + bootstrap moment within 5%).
    - ForecastControllerIntegrationTest passes (4 models → 200 with correct shape; 401 unauthenticated; distinct bands).
    - Full backend test suite green.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q verify 2>&1 | grep -v '^#' | grep -ci "BUILD SUCCESS"</automated>
  </verify>
  <done>Block bootstrap runner implemented (L=max(10,√H), boundary-safe, seed=42); ForecastStructuralTest + ForecastControllerIntegrationTest green; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ForecastService → finmath/Hipparchus compute | Internal; inputs are calibrated portfolio data only (no raw client input reaches the simulation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ForecastService.forecast | mitigate | portfolioId arrives already resolved from Authentication by ForecastController (05-01); service never accepts a client-supplied id. @Transactional(readOnly=true). |
| T-05-03 | Denial of Service | runner path/step counts | mitigate | NUM_PATHS server-fixed at 5000; horizonDays already clamped to [1,504] at the controller; no client value scales the loop bounds. |
| T-05-04 | Tampering (correctness) | GBM drift | mitigate | Rely on finmath BlackScholesModel built-in (μ−σ²/2) log-space drift; HC-11 guard test fails the build if the Ito correction is ever lost. |
| T-05-05 | Tampering (correctness) | Heston variance process | mitigate | FULL_TRUNCATION scheme + constructor Feller guard (2κθ>ξ²); finmath-smoke asserts variance reverts to THETA (param-order guard). |
</threat_model>

<verification>
- `./mvnw test -Dtest=ForecastMathHandComputedTest,ForecastFinmathIntegrationTest` green (Ito + finmath API).
- `./mvnw verify` green (structural + controller integration + full suite).
- Manual sanity (not gating): a high-vol low-drift portfolio shows GBM median below base (Ito sign check).
</verification>

<success_criteria>
- Four model runners implemented; GBM Ito-correct (HC-11 within 1%/0.5%, median<mean).
- All models return monotone p5..p95 bands that widen with horizon and are byte-identical across two seed=42 runs.
- Bootstrap preserves historical mean log-return within 5%.
- GET /api/portfolio/forecast returns 200 with correct shape for all four models; 401 unauthenticated.
- Full backend suite green.
</success_criteria>

<output>
Create `.planning/phases/05-stochastic-forecasting/05-02-SUMMARY.md` when done. Record: any deviation from RESEARCH finmath signatures, measured per-model timing, and confirmation that HC-11 + reproducibility pass.
</output>
