---
phase: 06-demo-mode-ai-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.yml
  - backend/src/test/resources/application-test.yml
  - backend/src/main/resources/db/migration/V4__ai_seed_content.sql
  - backend/src/main/java/com/quantlens/ai/package-info.java
  - backend/src/main/java/com/quantlens/ai/seed/AiSeedContent.java
  - backend/src/main/java/com/quantlens/ai/seed/AiSeedContentRepository.java
  - backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java
  - backend/src/main/java/com/quantlens/ai/session/LlmKeySessionHolder.java
  - backend/src/main/java/com/quantlens/ai/chat/DemoModeAdvisor.java
  - backend/src/main/java/com/quantlens/ai/chat/ChatClientStrategy.java
  - backend/src/main/java/com/quantlens/ai/api/AiStatusDto.java
  - backend/src/main/java/com/quantlens/ai/api/AiKeyRequest.java
  - backend/src/main/java/com/quantlens/ai/api/ExplainResponseDto.java
  - backend/src/main/java/com/quantlens/ai/api/CommentaryDto.java
  - backend/src/main/java/com/quantlens/ai/api/AiKeyController.java
  - backend/src/main/java/com/quantlens/ai/api/AiController.java
  - backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java
  - backend/src/main/java/com/quantlens/ai/service/CommentaryService.java
  - backend/src/test/java/com/quantlens/ai/DemoModeAdvisorTest.java
  - backend/src/test/java/com/quantlens/ai/LlmKeySessionHolderTest.java
  - backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java
  - backend/src/test/java/com/quantlens/ai/AiKeyControllerTest.java
  - backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java
  - backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java
  - frontend/src/__tests__/components/BYOKeyModal.test.ts
  - frontend/src/__tests__/components/AiModeBadge.test.ts
  - frontend/src/__tests__/components/ExplainDrawer.test.ts
  - frontend/src/__tests__/components/CommentaryCard.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-07, AI-08]
user_setup: []

must_haves:
  truths:
    - "App boots key-less: Spring context loads with both anthropic/openai api-key = sentinel (no real key), no NoUniqueBeanDefinitionException for ChatClient"
    - "Backend compiles; all new AI tests present; the DemoModeAdvisor + LlmKeySessionHolder + KeyLeakage tests PASS, feature-content tests are RED (assertion failures, not compile errors)"
    - "Frontend vue-tsc build succeeds; the new AI component test scaffolds excluded from prod build and discoverable by vitest (no 'Cannot find module' for the test files themselves)"
    - "ai_seed_content table exists after Flyway runs on a fresh Testcontainers Postgres"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/ai/package-info.java"
      provides: "AI Modulith module declaration (allowedDependencies portfolio::domain)"
      contains: "ApplicationModule"
    - path: "backend/src/main/java/com/quantlens/ai/seed/AiSeedContent.java"
      provides: "JPA entity for seeded AI fixtures (type, subjectId, content)"
      contains: "ai_seed_content"
    - path: "backend/src/main/resources/db/migration/V4__ai_seed_content.sql"
      provides: "Flyway DDL for ai_seed_content with uq_ai_seed unique constraint"
      contains: "uq_ai_seed"
    - path: "backend/src/main/java/com/quantlens/ai/chat/DemoModeAdvisor.java"
      provides: "CallAdvisor implementing adviseCall + getOrder + getName"
      contains: "CallAdvisor"
    - path: "backend/src/main/java/com/quantlens/ai/session/LlmKeySessionHolder.java"
      provides: "@SessionScope key holder (hasKey/getProvider/getApiKey/setKey/clear)"
      contains: "SessionScope"
    - path: "backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java"
      provides: "Security gate test — key never in response/log; hits /api/ai/explain/AAPL (seeded ticker)"
      contains: "TEST-SENTINEL-KEY"
  key_links:
    - from: "backend/src/main/resources/application.yml"
      to: "spring-ai anthropic/openai starters"
      via: "api-key sentinel ${ANTHROPIC_API_KEY:DEMO_NO_KEY}"
      pattern: "DEMO_NO_KEY"
    - from: "backend/src/main/java/com/quantlens/ai/seed/AiSeedContentRepository.java"
      to: "ai_seed_content table"
      via: "findByTypeAndSubjectId derived query"
      pattern: "findByTypeAndSubjectId"
    - from: "backend/src/main/java/com/quantlens/ai/session/LlmKeySessionHolder.java"
      to: "AiKeyController + DemoModeAdvisor + services (com.quantlens.ai.session)"
      via: "@SessionScope bean injected across the ai module"
      pattern: "com\\.quantlens\\.ai\\.session"
---

<objective>
Stand up the entire Phase 6 AI scaffold in one pass: add the two Spring AI starters, configure key-less (sentinel) startup, create the `com.quantlens.ai` Modulith module with the `ai_seed_content` table + entity + repository + idempotent seeder stub, create the `LlmKeySessionHolder` session bean (in `com.quantlens.ai.session`), and create ALL backend + frontend test scaffolds including the `KeyLeakageIntegrationTest` security gate. Every production class consumed by later plans (DemoModeAdvisor, ChatClientStrategy, AiKeyController, AiController, ExplainPositionService, CommentaryService, the four DTOs) is created here as a compiling implementation or stub so downstream plans implement against fixed contracts.

This is the Wave 0 foundation. It VERIFIES the four research assumptions (A1-A4) at compile/startup time and records their resolutions in the SUMMARY for downstream plans.

Purpose: Lock contracts and prove key-less startup before any seam wiring depends on it.
Output: Compiling backend with starters + sentinel config, Flyway V4 table, AI module skeleton, all test files (security/advisor/holder tests GREEN; feature-content tests RED), frontend component test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-mode-ai-seam/06-RESEARCH.md
@.planning/phases/06-demo-mode-ai-seam/06-PATTERNS.md
@.planning/phases/06-demo-mode-ai-seam/06-VALIDATION.md

<interfaces>
<!-- Contracts established here, consumed by 06-02/03/04. -->

LlmKeySessionHolder (com.quantlens.ai.session) — @Component @SessionScope:
- boolean hasKey()              // apiKey != null && !apiKey.isBlank()
- String  getProvider()        // "anthropic" | "openai" | null
- String  getApiKey()          // never logged/echoed/serialized
- void    setKey(String provider, String apiKey)
- void    clear()

DemoModeAdvisor (com.quantlens.ai.chat) implements org.springframework.ai.chat.client.advisor.api.CallAdvisor:
- ChatClientResponse adviseCall(ChatClientRequest, CallAdvisorChain)
- String getName()  -> "DemoModeAdvisor"
- int    getOrder() -> Ordered.HIGHEST_PRECEDENCE
- context keys read from request.context(): "AI_SEED_TYPE", "AI_SEED_SUBJECT"

ChatClientStrategy (com.quantlens.ai.chat) @Service:
- ChatClient forSession(LlmKeySessionHolder keyHolder)

AiSeedContentRepository (com.quantlens.ai.seed) extends JpaRepository<AiSeedContent, Long>:
- Optional<AiSeedContent> findByTypeAndSubjectId(String type, String subjectId)

DTO records (com.quantlens.ai.api):
- AiStatusDto(String mode, String provider)         // provider null in demo; @JsonInclude(NON_NULL)
- AiKeyRequest(String provider, String apiKey)      // request body only; @NotBlank both
- ExplainResponseDto(String narrative)
- CommentaryDto(String headline, String body, List<String> bulletPoints)

Endpoints:
- POST   /api/ai/key          -> AiStatusDto (live)   never returns key
- DELETE /api/ai/key          -> AiStatusDto (demo)
- GET    /api/ai/status       -> AiStatusDto
- GET    /api/ai/explain/{ticker} -> ExplainResponseDto   (path var = TICKER, not numeric id)
- GET    /api/ai/commentary   -> CommentaryDto

Seed table rows:
- type "EXPLAIN_POSITION", subjectId = ticker (e.g. "AAPL")
- type "DAILY_COMMENTARY", subjectId = persona key ("GROWTH" | "INCOME" | "BALANCED")
</interfaces>

# Backend analogs (read for style):
@backend/src/main/java/com/quantlens/analytics/package-info.java
@backend/src/main/java/com/quantlens/portfolio/domain/Position.java
@backend/src/main/java/com/quantlens/seed/SeedLogRepository.java
@backend/src/main/java/com/quantlens/seed/SeedRunner.java
@backend/src/main/java/com/quantlens/analytics/api/VarResultDto.java
@backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java
@backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java
@backend/src/main/resources/db/migration/V1__schema.sql
@backend/pom.xml

# Frontend analogs:
@frontend/src/__tests__/components/RiskScorecard.test.ts
@frontend/src/__tests__/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Spring AI starters + key-less sentinel config + Flyway V4 table + AI module data layer</name>
  <files>backend/pom.xml, backend/src/main/resources/application.yml, backend/src/test/resources/application-test.yml, backend/src/main/resources/db/migration/V4__ai_seed_content.sql, backend/src/main/java/com/quantlens/ai/package-info.java, backend/src/main/java/com/quantlens/ai/seed/AiSeedContent.java, backend/src/main/java/com/quantlens/ai/seed/AiSeedContentRepository.java, backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java, backend/src/main/java/com/quantlens/ai/session/LlmKeySessionHolder.java</files>
  <read_first>
    - RESEARCH.md "pom.xml additions", "Pattern 4: Booting Starters with No Real Key", "Pattern 6: LlmKeySessionHolder" (note: holder package is com.quantlens.ai.session), "Pattern 8: Seeded Content", Assumption A4 + Open Questions 2-3
    - PATTERNS.md package-info analog, AiSeedContent via Position.java, AiSeedContentRepository, AiSeedRunner via SeedRunner, V4 migration via V1, LlmKeySessionHolder (com.quantlens.ai.session), application.yml additions
    - backend/src/main/java/com/quantlens/seed/SeedRunner.java (idempotency guard; persona tick lists; static Logger pattern)
  </read_first>
  <action>
Add to backend/pom.xml `<dependencies>` the two starters (no `<version>` — governed by the already-pinned spring-ai-bom:1.1.6): `org.springframework.ai:spring-ai-starter-model-anthropic` and `org.springframework.ai:spring-ai-starter-model-openai`. Both are Approved per RESEARCH Package Legitimacy Audit (official org.springframework.ai group) — no legitimacy checkpoint needed.

Add to backend/src/main/resources/application.yml exactly per RESEARCH Pattern 4: `spring.ai.anthropic.api-key=${ANTHROPIC_API_KEY:DEMO_NO_KEY}`, `spring.ai.anthropic.chat.options.model=claude-sonnet-4-6`, `spring.ai.anthropic.chat.options.max-tokens=2048`; `spring.ai.openai.api-key=${OPENAI_API_KEY:DEMO_NO_KEY}`, `spring.ai.openai.chat.options.model=gpt-4o`, `spring.ai.openai.chat.options.max-tokens=2048`; `spring.ai.chat.client.enabled=false` (Pitfall 6); `logging.level.org.springframework.ai=WARN` (Pitfall 4). VERIFY A4: confirm `spring.ai.chat.client.enabled` is the correct 1.1.6 property by booting with both providers and checking no NoUniqueBeanDefinitionException for ChatClient; if that property is ignored on this BOM, ALSO set `spring.ai.model.chat=none` (Open Questions 2-3) so a key-less context boots cleanly, and record the exact working combination in SUMMARY.

Add to backend/src/test/resources/application-test.yml: `spring.ai.anthropic.api-key=TEST_SENTINEL`, `spring.ai.openai.api-key=TEST_SENTINEL`, `spring.ai.chat.client.enabled=false` (mirror prod so the test context loads key-less). If A4 required `spring.ai.model.chat=none` in prod, mirror it here too so the Task 3 integration tests boot identically.

Create db/migration/V4__ai_seed_content.sql per RESEARCH and V1 analog: leading filename+purpose comment; `CREATE TABLE IF NOT EXISTS ai_seed_content(id BIGSERIAL PRIMARY KEY, type VARCHAR(64) NOT NULL, subject_id VARCHAR(32) NOT NULL, content TEXT NOT NULL, CONSTRAINT uq_ai_seed UNIQUE (type, subject_id))`. This is V4 (V1-V3 exist).

Create `com.quantlens.ai` package-info.java mirroring analytics: `@org.springframework.modulith.ApplicationModule(displayName="AI", allowedDependencies={"portfolio::domain"})`. ALL AI classes including LlmKeySessionHolder live INSIDE this module — the holder is in the `com.quantlens.ai.session` subpackage, NOT in security/auth (keeps the ai module's only cross-module read to portfolio::domain and avoids exposing a session bean across modules).

Create AiSeedContent entity (com.quantlens.ai.seed) mirroring Position.java: `@Entity @Table(name="ai_seed_content")`; id IDENTITY; type `@Column(nullable=false,length=64)`; subjectId `@Column(name="subject_id",nullable=false,length=32)`; content `@Column(nullable=false,columnDefinition="TEXT")`; protected no-arg ctor + public (type,subjectId,content) ctor + getters only. No Lombok, no @ToString.

Create AiSeedContentRepository (com.quantlens.ai.seed) extends JpaRepository<AiSeedContent,Long> with `Optional<AiSeedContent> findByTypeAndSubjectId(String type, String subjectId)` (derived, no @Query).

Create LlmKeySessionHolder (com.quantlens.ai.session) per RESEARCH Pattern 6 — FULL implementation (it is trivial): `@Component @SessionScope`; private String provider + private String apiKey; hasKey/getProvider/getApiKey/setKey/clear. The package is `com.quantlens.ai.session` (confirmed against the aligned RESEARCH/PATTERNS docs). NO @ToString, NO @JsonInclude, no logging annotations; apiKey is a plain non-static non-volatile field.

Create AiSeedRunner (com.quantlens.ai.seed) as a STUB: `@Component @Order(2) implements ApplicationRunner`; static final Logger (LoggerFactory.getLogger); constructor-inject AiSeedContentRepository + SeedLogRepository; an idempotency guard checking `seedLogRepository.findById("ai-v1")` per SeedRunner but with an EMPTY seed body and a TODO pointing to 06-02. Do NOT write the "ai-v1" completion row yet (so 06-02 can run the real seed). With an empty body it is a no-op that must not break startup.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q compile *>&1 | Select-String -Pattern 'BUILD SUCCESS','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd compile` prints BUILD SUCCESS (starters resolve from BOM; all new classes compile)
    - WAVE-0 ASSUMPTION GATE (A1-A4) — A4 is gated here at the key-less boot; A1-A3 are gated at Task 2 compile but their import/API surface is locked here:
      - A4: key-less sentinel boot raises NO `NoUniqueBeanDefinitionException` for ChatClient. `spring.ai.chat.client.enabled: false` is the correct 1.1.6 property; if it is ignored on this BOM, ALSO set `spring.ai.model.chat=none` so the context boots key-less. The Task 3 integration tests booting with TEST_SENTINEL prove this at runtime — this acceptance criterion is the precondition for those tests booting at all. Record the exact working property combination in SUMMARY.
      - A1: only ChatClientRequest/ChatClientResponse types will be referenced downstream (no AdvisedRequest/AdvisedResponse) — locked by the interface block here, confirmed at Task 2 compile.
      - A2/A3: synthetic ChatClientResponse construction + AnthropicChatModel.Builder min fields — confirmed at Task 2 compile/build().
    - application.yml contains `DEMO_NO_KEY` for both providers, `claude-sonnet-4-6`, `gpt-4o`, `spring.ai.chat.client.enabled: false`, `org.springframework.ai: WARN`
    - V4__ai_seed_content.sql contains `uq_ai_seed UNIQUE (type, subject_id)`
    - LlmKeySessionHolder is in package `com.quantlens.ai.session`, annotated `@SessionScope`, no @ToString/@JsonInclude
    - AiSeedContentRepository declares `findByTypeAndSubjectId`
    - A4 resolution (chat.client.enabled property name + whether spring.ai.model.chat=none was also required for a clean key-less boot) recorded in SUMMARY
  </acceptance_criteria>
  <done>Starters added, sentinel config in place, V4 table + AI module entity/repo/seeder stub + LlmKeySessionHolder (com.quantlens.ai.session) compile cleanly; A4 key-less boot gated (proven at runtime by Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Implement DemoModeAdvisor + ChatClientStrategy + 4 DTOs + AiKeyController; create AiController + service stubs — verify A1/A2/A3</name>
  <files>backend/src/main/java/com/quantlens/ai/chat/DemoModeAdvisor.java, backend/src/main/java/com/quantlens/ai/chat/ChatClientStrategy.java, backend/src/main/java/com/quantlens/ai/api/AiStatusDto.java, backend/src/main/java/com/quantlens/ai/api/AiKeyRequest.java, backend/src/main/java/com/quantlens/ai/api/ExplainResponseDto.java, backend/src/main/java/com/quantlens/ai/api/CommentaryDto.java, backend/src/main/java/com/quantlens/ai/api/AiKeyController.java, backend/src/main/java/com/quantlens/ai/api/AiController.java, backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java, backend/src/main/java/com/quantlens/ai/service/CommentaryService.java</files>
  <read_first>
    - RESEARCH.md Pattern 2 DemoModeAdvisor, Pattern 5 ChatClientStrategy, Key Injection table, Pattern 7 AiKeyController, Pattern 9 ExplainPositionService, Assumptions A1/A2/A3, Open Question 1, Pitfall 5
    - PATTERNS.md DemoModeAdvisor, ChatClientStrategy, AiKeyController via AuthController, AiController via AnalyticsController, DTO records
    - backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java (resolvePortfolioId — copy verbatim)
    - backend/src/main/java/com/quantlens/security/api/AuthController.java (controller/constructor/ResponseEntity shape)
  </read_first>
  <action>
Create four DTO records (com.quantlens.ai.api), VarResultDto style (public record, no body, Javadoc @param per field): AiStatusDto(String mode, String provider) annotated `@JsonInclude(JsonInclude.Include.NON_NULL)` so null provider is omitted; AiKeyRequest(String provider, String apiKey) with `@NotBlank` on both (jakarta.validation; validation starter present); ExplainResponseDto(String narrative); CommentaryDto(String headline, String body, List<String> bulletPoints).

Create DemoModeAdvisor (com.quantlens.ai.chat) per RESEARCH Pattern 2 — FULL implementation (its tests are in Task 3 of this plan). Implement `CallAdvisor`: getName() returns "DemoModeAdvisor"; getOrder() returns Ordered.HIGHEST_PRECEDENCE; adviseCall(ChatClientRequest, CallAdvisorChain) — if keyHolder.hasKey() delegate chain.nextCall(request); else read "AI_SEED_TYPE"/"AI_SEED_SUBJECT" from request.context(), look up seedRepo.findByTypeAndSubjectId, build a synthetic ChatClientResponse. VERIFY A2: confirm `new ChatClientResponse(new ChatResponse(List.of(new Generation(new AssistantMessage(content)))), ctx)` compiles (imports: org.springframework.ai.chat.model.ChatResponse/Generation, org.springframework.ai.chat.messages.AssistantMessage, org.springframework.ai.chat.client.ChatClientResponse). VERIFY A1: use ChatClientRequest/ChatClientResponse only — never AdvisedRequest/AdvisedResponse. Constructor-inject LlmKeySessionHolder (from com.quantlens.ai.session) + AiSeedContentRepository. Record A1/A2 resolutions in SUMMARY.

Create ChatClientStrategy (com.quantlens.ai.chat) @Service per RESEARCH Pattern 5. Constructor-inject the auto-configured AnthropicChatModel, OpenAiChatModel, and DemoModeAdvisor. `forSession(LlmKeySessionHolder)` selects the per-request model (demo → base anthropic model, never actually called because the advisor short-circuits; live → switch on provider, build a new model with the session key) and returns `ChatClient.builder(model).defaultAdvisors(demoAdvisor).build()`. Use the VERIFIED key-injection API: Anthropic via `AnthropicChatModel.builder().anthropicApi(AnthropicApi.builder().apiKey(key).build()).defaultOptions(AnthropicChatOptions.builder().model("claude-sonnet-4-6").maxTokens(2048).build()).build()` (NO mutate on Anthropic); OpenAI via `baseOpenAiModel.mutate().openAiApi(OpenAiApi.builder().apiKey(key).build()).build()`. VERIFY A3: confirm `AnthropicChatModel.builder().anthropicApi(...).defaultOptions(...).build()` succeeds without toolCallingManager/retryTemplate/observationRegistry; if build() requires them, copy them from the injected base AnthropicChatModel and record the workaround in SUMMARY.

Create AiKeyController (com.quantlens.ai.api) per RESEARCH Pattern 7 — FULL implementation: `@RestController @RequestMapping("/api/ai")`, constructor-inject LlmKeySessionHolder (com.quantlens.ai.session). POST /key(@RequestBody @Valid AiKeyRequest) validates provider whitelist (anthropic|openai) and non-blank key, calls keyHolder.setKey, returns ResponseEntity.ok(new AiStatusDto("live", provider)) — NEVER returns the key. DELETE /key calls keyHolder.clear(), returns AiStatusDto("demo", null). GET /status returns AiStatusDto from hasKey(). Add `@ExceptionHandler(IllegalArgumentException.class)` returning a generic body (no exception message, no key — Pitfall 5).

Create AiController (com.quantlens.ai.api) near-stub: `@RestController @RequestMapping("/api/ai")`, constructor-inject PortfolioRepository + ExplainPositionService + CommentaryService. Copy `resolvePortfolioId(Authentication)` VERBATIM from AnalyticsController (IDOR-safe — never accept portfolioId from the request). GET /explain/{ticker}(@PathVariable String ticker, Authentication) @Transactional(readOnly=true) → explainService.explain(portfolioId, ticker) → ExplainResponseDto. GET /commentary(Authentication) @Transactional(readOnly=true) → commentaryService.commentary(portfolioId) → CommentaryDto. NOTE: the path variable is the TICKER, not a numeric holdingId — HoldingDto is keyed by ticker (no id field) and the seed subject_id is ticker; this resolves the RESEARCH Pattern 9 mismatch (PortfolioService exposes getHoldings(portfolioId), not getHolding(id)). Record this contract decision in SUMMARY.

Create ExplainPositionService + CommentaryService (com.quantlens.ai.service) as STUBS that compile and return placeholder DTOs (explain → `new ExplainResponseDto("")`, commentary → `new CommentaryDto("","",List.of())`) with a TODO pointing to 06-03. Constructor-inject ChatClientStrategy + LlmKeySessionHolder (com.quantlens.ai.session) + PortfolioService. @Service; @Transactional(readOnly=true) on read methods. These stubs let the controller + integration tests compile in RED state.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q compile *>&1 | Select-String -Pattern 'BUILD SUCCESS','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd compile` prints BUILD SUCCESS — confirms A1 (ChatClientRequest/Response), A2 (ChatClientResponse construction), A3 (AnthropicChatModel.builder min fields) all compile
    - DemoModeAdvisor implements CallAdvisor; getOrder() returns Ordered.HIGHEST_PRECEDENCE; no AdvisedRequest/AdvisedResponse references
    - ChatClientStrategy uses AnthropicChatModel.builder() (no mutate) and baseOpenAiModel.mutate() for OpenAI
    - AiKeyController POST /key returns AiStatusDto without a key field; @ExceptionHandler present
    - AiController.resolvePortfolioId is the verbatim IDOR-safe copy; explain path is /explain/{ticker}
    - All injection of the key holder imports com.quantlens.ai.session.LlmKeySessionHolder
    - A1/A2/A3 resolutions + ticker-vs-holdingId contract recorded in SUMMARY
  </acceptance_criteria>
  <done>All seam/feature production classes compile against the real Spring AI 1.1.6 API; contracts fixed for 06-02/03/04; assumptions A1-A3 verified at compile.</done>
</task>

<task type="auto">
  <name>Task 3: Create all test scaffolds (backend unit/integration + frontend component) and prove key-less startup + security gate</name>
  <files>backend/src/test/java/com/quantlens/ai/DemoModeAdvisorTest.java, backend/src/test/java/com/quantlens/ai/LlmKeySessionHolderTest.java, backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java, backend/src/test/java/com/quantlens/ai/AiKeyControllerTest.java, backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java, backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java, frontend/src/__tests__/components/BYOKeyModal.test.ts, frontend/src/__tests__/components/AiModeBadge.test.ts, frontend/src/__tests__/components/ExplainDrawer.test.ts, frontend/src/__tests__/components/CommentaryCard.test.ts</files>
  <read_first>
    - RESEARCH.md DemoModeAdvisorTest, KeyLeakageIntegrationTest body, Test Map, log-capture note, Pitfall 3 session isolation
    - PATTERNS.md KeyLeakageIntegrationTest + login helpers, AiControllerIntegrationTest, AiKeyControllerTest via AuthIntegrationTest, DemoModeAdvisorTest
    - VALIDATION.md Per-Req Verification Map, demo-mode-no-network rule
    - backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java (AbstractPostgresIntegrationTest base + loginAndGetSessionCookie/authenticatedGet helpers — copy verbatim)
    - frontend/src/__tests__/components/RiskScorecard.test.ts (RED scaffold style), frontend/src/__tests__/setup.ts
  </read_first>
  <action>
Backend unit tests (pure Mockito, no Spring context):
- DemoModeAdvisorTest per RESEARCH: mock AiSeedContentRepository, LlmKeySessionHolder, CallAdvisorChain; `shortCircuitsInDemoMode` (hasKey false → verifyNoInteractions(chain); content equals the seeded value); `passesThroughWhenKeyPresent` (hasKey true → verify(chain).nextCall(request); verifyNoInteractions(seedRepo)). Provide buildRequest/extractContent helpers building a ChatClientRequest with context params AI_SEED_TYPE/AI_SEED_SUBJECT. These PASS now (advisor is fully implemented in Task 2) — they are the executable spec.
- LlmKeySessionHolderTest: setKey then hasKey true + getProvider/getApiKey correct; clear() resets to hasKey false; fresh instance starts demo. PASSES now.

Backend integration tests (extend AbstractPostgresIntegrationTest; copy loginAndGetSessionCookie + authenticatedGet verbatim from AnalyticsControllerIntegrationTest):
- KeyLeakageIntegrationTest (SECURITY GATE) per RESEARCH: login alice; `testKey="TEST-SENTINEL-KEY-"+UUID.randomUUID()`; POST /api/ai/key {provider:anthropic, apiKey:testKey} → 200, assert `$.mode==live`, `$.apiKey` does not exist, body does NOT contain testKey; GET /api/ai/status → body must not contain testKey; **GET /api/ai/explain/AAPL** (a REAL seeded ticker — NOT /api/ai/explain/1 numeric; a 404 would make the leakage assertion vacuously pass) → 200, body must not contain testKey; GET /api/ai/commentary → 200, body must not contain testKey. CRITICAL: AAPL must be a seeded holding in alice's portfolio so the endpoint returns 200, not 404 — confirm AAPL is in the Phase-1 seeded universe (it is, per the holdings reference). Attach a logback ListAppender<ILoggingEvent> to the root logger for the test duration; assert no captured log line contains testKey. PASSES now (controller returns {mode,provider} only; key never logged). If it fails, the build is blocked — this is the mandatory gate.
- AiKeyControllerTest: setKey_returns_mode_live (POST APPLICATION_JSON → AiStatusDto mode live, provider matches); clearKey_returns_mode_demo; sessionIsolation (two distinct session cookies, set different providers each, assert GET /status returns each session's own provider — proves @SessionScope isolation, Pitfall 3). PASSES now.
- AiControllerIntegrationTest: explain_unauthenticated_returns401 (no cookie → 401) PASSES now; explainReturnsSeededContent (**GET /api/ai/explain/AAPL** — ticker path, NOT numeric → 200 with non-blank narrative) and commentaryReturnsSeededContent (GET /api/ai/commentary → 200 with headline/body/bulletPoints) — mark clearly as RED-for-06-02/03 (placeholder service returns empty content until then).
- AiDemoModeIntegrationTest: demo mode performs NO provider call — login, no key set (demo), GET explain/AAPL + commentary return seeded DB content with 200; the proof is structural (no real provider key configured; sentinel never sent because the advisor short-circuits). Add a comment: this test must NOT mock a live provider — the demo path reads the DB only. RED until 06-02 seeds content. (Note: an EXECUTABLE no-network assertion — spy on the advisor chain to assert nextCall() is never invoked in demo — is added in 06-02 once content is seeded; here the structural form is acceptable as the RED scaffold.)

Frontend component test scaffolds (frontend/src/__tests__/components/, .test.ts; excluded from prod build per tsconfig.app.json), RiskScorecard.test.ts RED style:
- BYOKeyModal.test.ts: import from '../../components/ai/BYOKeyModal.vue' (RED). Tests: renders provider radios + password input when open=true; `keyNotInLocalStorage` — after a submit interaction, localStorage contains no entry whose value equals the typed key; emits 'submitted' and 'close'.
- AiModeBadge.test.ts: import from '../../components/ai/AiModeBadge.vue'. Tests: shows "Demo" when status mode is demo; shows "Live" when mode is live (mount with a Pinia test store seeded to each state).
- ExplainDrawer.test.ts: import from '../../components/ai/ExplainDrawer.vue'. Tests: shimmer skeleton when loading=true; narrative text when populated; not rendered when open=false.
- CommentaryCard.test.ts: import from '../../components/ai/CommentaryCard.vue'. Tests: skeleton when loading=true; renders headline+body+bulletPoints when populated; error state shows static copy.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q test-compile *>&1 | Select-String -Pattern 'BUILD SUCCESS','BUILD FAILURE'; .\mvnw.cmd -q test -Dtest=DemoModeAdvisorTest,LlmKeySessionHolderTest,KeyLeakageIntegrationTest,AiKeyControllerTest *>&1 | Select-String -Pattern 'BUILD SUCCESS','Tests run','BUILD FAILURE'; cd ..\frontend; npm run test -- --run *>&1 | Select-String -Pattern 'Test Files','passed','failed','Cannot find module'</automated>
  </verify>
  <acceptance_criteria>
    - `.\mvnw.cmd test-compile` prints BUILD SUCCESS — ALL 6 backend AI test files compile, INCLUDING the RED AiControllerIntegrationTest + AiDemoModeIntegrationTest (catches compile errors in the RED scaffolds, not just the GREEN ones)
    - DemoModeAdvisorTest, LlmKeySessionHolderTest, KeyLeakageIntegrationTest, AiKeyControllerTest all PASS (BUILD SUCCESS, 0 failures)
    - KeyLeakageIntegrationTest hits /api/ai/explain/AAPL (a 200-returning seeded ticker), and asserts `$.apiKey` does not exist AND body/logs never contain the test key across all four endpoints
    - AiKeyControllerTest sessionIsolation proves two sessions hold independent providers
    - Frontend `npm run test -- --run` discovers all 4 AI component test files — assertion FAILURES are OK (components not built yet), but "Cannot find module" for a *.test.ts file path is NOT ok (the scaffold itself must be syntactically valid and importable). Positive signal: the test runner reports the 4 files as collected/run.
    - Feature-content backend tests (AiControllerIntegrationTest seeded cases, AiDemoModeIntegrationTest) present and marked RED-for-06-02/03
  </acceptance_criteria>
  <done>All 6 backend AI test files test-COMPILE; security/advisor/holder/key tests GREEN; feature-content tests RED-and-ready; the 4 frontend component scaffolds are collected by vitest (RED assertions, no missing-module errors); key-less startup proven by the integration tests booting the context with the sentinel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/ai/key | Untrusted provider + API key cross into the server (one-way) |
| session bean ↔ singletons | @SessionScope key holder must not be promoted to singleton state |
| app → LLM provider | Only crossed in live mode; sentinel must never reach a provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | LlmKeySessionHolder | mitigate | No @ToString/@JsonInclude; apiKey never serialized; field non-static (verified by KeyLeakageIntegrationTest + LlmKeySessionHolderTest) |
| T-06-02 | Information Disclosure | application.yml logging | mitigate | `logging.level.org.springframework.ai=WARN`; no SimpleLoggerAdvisor registered anywhere |
| T-06-03 | Tampering | startup with no key | mitigate | Sentinel `DEMO_NO_KEY` boots key-less; DemoModeAdvisor short-circuits before any network call; sentinel never sent |
| T-06-04 | Elevation of Privilege | @SessionScope key holder | mitigate | Constructor injection only (scoped proxy); AiKeyControllerTest#sessionIsolation asserts no cross-session bleed |
| T-06-SC | Tampering | spring-ai starter installs | mitigate | Both starters Approved (official org.springframework.ai, BOM-resolved) — no [ASSUMED]/[SUS] packages; no blocking checkpoint required |
</threat_model>

<verification>
- `.\mvnw.cmd compile` BUILD SUCCESS (key-less startup config + all AI classes compile; A4 boot gated)
- `.\mvnw.cmd test-compile` BUILD SUCCESS (all 6 backend AI test files compile, incl. RED scaffolds)
- `.\mvnw.cmd test -Dtest=DemoModeAdvisorTest,LlmKeySessionHolderTest,KeyLeakageIntegrationTest,AiKeyControllerTest` all PASS
- `npm run test -- --run` (frontend) collects all 4 AI component scaffolds (RED assertions OK; no "Cannot find module")
- `npm run build` (frontend, vue-tsc) succeeds with the 4 new test scaffolds excluded
- `.\mvnw.cmd test` runs all AI tests: advisor/holder/key/leakage GREEN; feature-content tests RED with assertion errors (not compile errors)
</verification>

<success_criteria>
- Spring AI Anthropic + OpenAI starters added; app boots with the sentinel and no real key (no NoUniqueBeanDefinitionException)
- ai_seed_content table created by Flyway V4 on a fresh Testcontainers Postgres
- com.quantlens.ai module skeleton + LlmKeySessionHolder (com.quantlens.ai.session) + DemoModeAdvisor + ChatClientStrategy + DTOs + controllers + service stubs compile
- All 6 backend AI test files test-COMPILE; KeyLeakageIntegrationTest (security gate, hits /explain/AAPL) PASSES
- All RED feature-content scaffolds and the 4 frontend component scaffolds present and collected by the test runners
- Assumptions A1-A4 resolutions recorded in SUMMARY for downstream plans
</success_criteria>

<output>
Create `.planning/phases/06-demo-mode-ai-seam/06-01-SUMMARY.md` when done. MUST record: A1 (sentinel boots key-less), A2 (ChatClientResponse construction), A3 (AnthropicChatModel.builder min fields), A4 (chat.client.enabled property name + whether spring.ai.model.chat=none was also needed for a clean key-less boot), and the explain path-variable contract (ticker, not numeric holdingId).
</output>
</content>
</invoke>
