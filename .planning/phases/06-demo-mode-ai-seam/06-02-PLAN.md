---
phase: 06-demo-mode-ai-seam
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java
  - backend/src/main/java/com/quantlens/security/config/SecurityConfig.java
  - backend/src/test/java/com/quantlens/ai/AiSeedRunnerTest.java
  - backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java
autonomous: true
requirements: [AI-01, AI-02]
user_setup: []

must_haves:
  truths:
    - "On a fresh DB, AiSeedRunner populates ai_seed_content with one EXPLAIN_POSITION row per ticker (15) and one DAILY_COMMENTARY row per persona (3)"
    - "Re-running the app does not duplicate seed rows (idempotency via seed_log 'ai-v1')"
    - "Seeded content references the persona's real tickers/approximate share counts so it matches the dashboard"
    - "POST /api/ai/key succeeds without a CSRF token rejection (endpoint exempted or XSRF-token-driven)"
    - "In demo mode the advisor chain's nextCall() is never invoked (EXECUTABLE no-network proof via spy/subclass)"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java"
      provides: "Idempotent @Order(2) seeder writing authored EXPLAIN_POSITION + DAILY_COMMENTARY fixtures"
      contains: "ai-v1"
    - path: "backend/src/test/java/com/quantlens/ai/AiSeedRunnerTest.java"
      provides: "Integration test: row counts + idempotency on re-run"
      contains: "AbstractPostgresIntegrationTest"
    - path: "backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java"
      provides: "Demo-mode seam test incl. EXECUTABLE no-network proof (chain.nextCall never invoked)"
      contains: "nextCall"
  key_links:
    - from: "backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java"
      to: "ai_seed_content table"
      via: "AiSeedContentRepository.save per fixture"
      pattern: "save"
    - from: "backend/src/main/java/com/quantlens/security/config/SecurityConfig.java"
      to: "/api/ai/key"
      via: "CSRF ignoringRequestMatchers OR documented XSRF-token path"
      pattern: "/api/ai/key"
---

<objective>
Complete the demo↔live seam by authoring the seeded fixtures and wiring CSRF for the key endpoint. The DemoModeAdvisor short-circuit logic and ChatClientStrategy were implemented in 06-01; this plan fills `ai_seed_content` with convincing, persona-accurate content via an idempotent `AiSeedRunner`, makes `POST /api/ai/key` accepted by the security layer, and upgrades AiDemoModeIntegrationTest from a structural to an EXECUTABLE no-network proof. After this plan, demo mode returns real authored narratives end-to-end with zero key and zero network — and the no-network claim is proven by asserting the advisor chain's nextCall() is never invoked.

Purpose: Make demo mode genuinely convincing (AI-01) and the key endpoint usable (AI-02) — the seed content is the screenshot artifact; the no-network proof is executable, not asserted by absence.
Output: Populated `ai_seed_content` (18 rows), idempotent seeder, SecurityConfig CSRF wiring for `/api/ai/key`, seeder integration test, executable demo-mode no-network test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-mode-ai-seam/06-RESEARCH.md
@.planning/phases/06-demo-mode-ai-seam/06-PATTERNS.md
@.planning/phases/06-demo-mode-ai-seam/06-01-SUMMARY.md

<interfaces>
<!-- From 06-01 (already created). -->
AiSeedContent(type, subjectId, content) — entity; AiSeedContentRepository.findByTypeAndSubjectId + save
LlmKeySessionHolder (com.quantlens.ai.session) .setKey/clear (already wired into AiKeyController)
DemoModeAdvisor (com.quantlens.ai.chat) — short-circuits via the CallAdvisorChain; adviseCall delegates chain.nextCall(request) ONLY when hasKey() is true
Seed contract:
- type "EXPLAIN_POSITION", subjectId = ticker (AAPL, MSFT, NVDA, AMZN, TSLA, JPM, BAC, XOM, CVX, PG, KO, WMT, JNJ + any others in the seeded universe)
- type "DAILY_COMMENTARY", subjectId = "GROWTH" | "INCOME" | "BALANCED"
seed_log idempotency key for this seeder: "ai-v1"
</interfaces>

# Analogs:
@backend/src/main/java/com/quantlens/seed/SeedRunner.java
@backend/src/main/java/com/quantlens/seed/SeedLog.java
@backend/src/main/java/com/quantlens/seed/SeedLogRepository.java
@backend/src/main/java/com/quantlens/security/config/SecurityConfig.java
@backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java
@backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement idempotent AiSeedRunner with authored persona-accurate fixtures</name>
  <files>backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java, backend/src/test/java/com/quantlens/ai/AiSeedRunnerTest.java</files>
  <read_first>
    - RESEARCH.md Pattern 8 seeder, Seeded Content Quality Guide, holdings reference table, authored content strategy
    - PATTERNS.md AiSeedRunner via SeedRunner: @Order(2), idempotency guard, static Logger, persona tick lists, idempotent guard shared pattern
    - backend/src/main/java/com/quantlens/seed/SeedRunner.java (full idempotency + transaction + logging structure)
    - 06-01-SUMMARY.md (confirm the exact ticker universe seeded by Phase 1 SeedRunner)
  </read_first>
  <action>
Replace the 06-01 stub body of AiSeedRunner with the real seeding logic. Keep `@Component @Order(2) implements ApplicationRunner`, static final Logger, constructor-injected AiSeedContentRepository + SeedLogRepository. In `run(ApplicationArguments)` (@Transactional): check `seedLogRepository.findById("ai-v1")` idempotency guard per SeedRunner — return early if completed. Otherwise insert fixtures, then write the SeedLog("ai-v1") completed=true LAST (so a mid-seed failure rolls back).

Fixtures to author (RESEARCH Quality Guide):
- One EXPLAIN_POSITION row per ticker in the seeded universe (the universe Phase 1 SeedRunner created — confirm from 06-01-SUMMARY; per the holdings reference these include AAPL, MSFT, NVDA, AMZN, TSLA, JPM, BAC, XOM, CVX, PG, KO, WMT, JNJ). subjectId = ticker. content = 2-4 paragraphs of LLM-looking narrative that names the ticker, its sector, and approximate role in a portfolio, with mild hedging language ("appears well-positioned", "has historically demonstrated"). Reference the approximate share-count context where natural so it does not contradict the displayed holdings (Pitfall 12 — seeded vs live drift).
- Three DAILY_COMMENTARY rows: subjectId "GROWTH" (Alice — AAPL/MSFT/NVDA/AMZN/TSLA tilt), "INCOME" (Bob — JPM/BAC/XOM/CVX/PG/KO/WMT dividend tilt), "BALANCED" (Charlie — mixed). content = CommentaryDto-shaped narrative: a headline-worthy first line, a body paragraph, and 2-4 bullet-style observations consistent with that persona's holdings and the risk/forecast metrics already shown on the dashboard. Each persona's commentary must read distinctly (growth ≠ income ≠ balanced).

Use idempotent upsert semantics: for each fixture, only insert if `findByTypeAndSubjectId` is empty (defensive — the seed_log guard already prevents re-runs, but this avoids unique-constraint violations on uq_ai_seed if the log row is missing). Log a summary line (count of rows seeded) via the static Logger — NEVER log content bodies at INFO if they are long; a count is enough. Never System.out.println.

NOTE on CommentaryDto shape: the DemoModeAdvisor returns the raw stored `content` string as the assistant message. The CommentaryService (06-03) maps that string into CommentaryDto(headline, body, bulletPoints). Store the DAILY_COMMENTARY content in a parseable shape that 06-03 can split (e.g. first line = headline, blank-line-separated body, lines prefixed "- " = bullets). Document the exact storage convention in this plan's SUMMARY so 06-03 parses it consistently.

Create AiSeedRunnerTest (extends AbstractPostgresIntegrationTest): after context start, assert `ai_seed_content` contains exactly the expected EXPLAIN_POSITION count (one per ticker) and 3 DAILY_COMMENTARY rows; assert each persona key exists; assert a sample EXPLAIN_POSITION row content is non-blank and contains its ticker substring. Idempotency: invoke the runner a second time (or assert the seed_log "ai-v1" row exists and row counts are unchanged) and confirm no duplicate rows.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q test -Dtest=AiSeedRunnerTest *>&1 | Select-String -Pattern 'BUILD SUCCESS','Tests run','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - AiSeedRunnerTest PASSES (BUILD SUCCESS, 0 failures)
    - ai_seed_content has one EXPLAIN_POSITION per seeded ticker + 3 DAILY_COMMENTARY rows
    - Re-run does not duplicate rows; seed_log "ai-v1" completed=true written last
    - Each EXPLAIN_POSITION content names its ticker; each persona commentary is distinct
    - DAILY_COMMENTARY storage convention (headline/body/bullets parse format) recorded in SUMMARY for 06-03
  </acceptance_criteria>
  <done>Demo content is authored, persona-accurate, idempotently seeded, and verified by an integration test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CSRF for /api/ai/key and prove the demo-mode seam is no-network (executable)</name>
  <files>backend/src/main/java/com/quantlens/security/config/SecurityConfig.java, backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java</files>
  <read_first>
    - PATTERNS.md SecurityConfig update note: add "/api/ai/key" to ignoringRequestMatchers alongside login/logout; /api/ai/* already covered by .anyRequest().authenticated()
    - RESEARCH.md Pattern 7 SecurityConfig note: exempt /api/ai/key from CSRF OR ensure frontend sends X-XSRF-TOKEN (axios interceptor in auth.ts already does)
    - backend/src/main/java/com/quantlens/security/config/SecurityConfig.java (existing csrf block; .anyRequest().authenticated())
    - backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java (the structural RED scaffold from 06-01 — upgrade it to executable)
    - RESEARCH.md DemoModeAdvisor / Pitfall on no-network proof (spy the chain)
  </read_first>
  <action>
Update SecurityConfig.filterChain csrf block. The existing axios interceptor (auth.ts) already attaches X-XSRF-TOKEN to mutating requests, so the cleanest choice is to RELY on the XSRF token (no exemption needed). However, to match the existing project pattern for session-mutating endpoints and avoid a token-timing edge case on the very first key submission, add `"/api/ai/key"` to the existing `.ignoringRequestMatchers("/api/auth/login", "/api/auth/logout")` so it becomes `.ignoringRequestMatchers("/api/auth/login", "/api/auth/logout", "/api/ai/key")`. This covers both POST and DELETE on /key. Do NOT add any permitAll rule — /api/ai/* must remain authenticated (the key endpoint requires a logged-in session so the key binds to that session). Add a brief comment explaining the exemption mirrors the login/logout rationale (the key POST is session-bound and CSRF-defended by SameSite=Lax + authenticated session). GET /api/ai/status and the AI feature GETs need no CSRF handling (CSRF applies to mutating methods only). Record in SUMMARY that the choice was exemption-for-consistency, not a security relaxation (the endpoint stays authenticated).

Upgrade AiDemoModeIntegrationTest (the structural RED scaffold from 06-01) into an EXECUTABLE no-network proof, now that content is seeded:
- The previous version relied on the structural absence of a real key. Replace that weak proof with a positive assertion that the advisor chain's `nextCall()` is NEVER invoked in demo mode. Use a spy/subclass approach: register (via @TestConfiguration / @MockBean or a test-only @Primary bean) a spy or counting subclass around the CallAdvisorChain path so the test can assert nextCall() invocation count == 0 after demo-mode explain + commentary calls. If the chain is not directly injectable, spy the DemoModeAdvisor's collaborators or use a Mockito spy on a test-exposed seam (the cleanest hook is a spy CallAdvisor inserted AFTER DemoModeAdvisor that increments a counter in nextCall — in demo mode DemoModeAdvisor short-circuits before reaching it, so the counter stays 0; in a control assertion with a key set, the spy IS reached). Document the chosen hook in SUMMARY.
- Keep the existing assertions: login alice, no key set (demo), GET /api/ai/explain/AAPL + GET /api/ai/commentary return 200 with seeded, non-blank content. Add the executable assertion: the downstream-chain counter is 0 across both demo calls (proves no provider call / no network).
- Add a comment: this test must NOT mock a live provider — the demo path reads the DB only; the no-network claim is now executable, not structural.

Run the full AI suite to confirm the seam works end-to-end in demo mode now that content is seeded.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q test -Dtest=AiKeyControllerTest,KeyLeakageIntegrationTest,AiDemoModeIntegrationTest,AiSeedRunnerTest *>&1 | Select-String -Pattern 'BUILD SUCCESS','Tests run','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - SecurityConfig csrf block lists "/api/ai/key" in ignoringRequestMatchers
    - /api/ai/* remains under .anyRequest().authenticated() (no new permitAll)
    - AiKeyControllerTest + KeyLeakageIntegrationTest still PASS (key POST accepted, key never leaks)
    - AiDemoModeIntegrationTest now PASSES with an EXECUTABLE no-network proof: a spy/counting CallAdvisor placed after DemoModeAdvisor records nextCall() invocation count == 0 across demo-mode explain + commentary calls (NOT relying on structural absence-of-key)
    - AiDemoModeIntegrationTest returns 200 + seeded non-blank content for /explain/AAPL and /commentary
  </acceptance_criteria>
  <done>The key endpoint is accepted by the security layer; demo-mode seam returns seeded content end-to-end with executable proof of zero network; security gate still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/ai/key | CSRF-relevant mutating endpoint; must stay authenticated |
| seeder → DB | Authored content written once, idempotently |
| app → LLM provider | In demo mode the chain's nextCall() must never fire (executable proof) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing/CSRF | POST/DELETE /api/ai/key | mitigate | Endpoint stays authenticated; CSRF exemption mirrors login/logout (session-bound + SameSite=Lax); never permitAll |
| T-06-06 | Information Disclosure | AiSeedRunner logging | mitigate | Logger emits row counts only, never long content bodies; never System.out |
| T-06-01 | Information Disclosure | key handling (regression) | mitigate | KeyLeakageIntegrationTest re-run after CSRF change confirms key still never leaks |
| T-06-07 | Tampering | seed idempotency | mitigate | seed_log "ai-v1" written last in transaction; per-fixture findByTypeAndSubjectId guard prevents uq_ai_seed violation |
| T-06-03b | Tampering | demo-mode network call | mitigate | AiDemoModeIntegrationTest asserts chain.nextCall() invocation count == 0 in demo mode (executable, not structural) |
</threat_model>

<verification>
- `.\mvnw.cmd test -Dtest=AiSeedRunnerTest` PASS (18 rows, idempotent)
- `.\mvnw.cmd test -Dtest=AiKeyControllerTest,KeyLeakageIntegrationTest,AiDemoModeIntegrationTest` PASS (incl. executable no-network proof)
- `.\mvnw.cmd test` full backend suite green (explain/commentary feature-content tests remain RED for 06-03)
</verification>

<success_criteria>
- ai_seed_content populated with persona-accurate EXPLAIN_POSITION + DAILY_COMMENTARY fixtures, idempotently
- Demo mode returns authored seeded content end-to-end with zero key and an EXECUTABLE zero-network proof (chain.nextCall never invoked)
- POST/DELETE /api/ai/key accepted by the security layer; key never leaks (gate still green)
- DAILY_COMMENTARY storage convention documented for 06-03 parsing
</success_criteria>

<output>
Create `.planning/phases/06-demo-mode-ai-seam/06-02-SUMMARY.md` when done. MUST record the DAILY_COMMENTARY content storage convention (how headline/body/bullets are encoded in the single content string), the seeded ticker universe count, and the test hook used for the executable no-network proof (the spy/counting CallAdvisor placement).
</output>
</content>
