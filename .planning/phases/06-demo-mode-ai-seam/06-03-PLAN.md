---
phase: 06-demo-mode-ai-seam
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java
  - backend/src/main/java/com/quantlens/ai/service/CommentaryService.java
  - backend/src/main/java/com/quantlens/ai/api/AiController.java
autonomous: true
requirements: [AI-07, AI-08]
user_setup: []

must_haves:
  truths:
    - "GET /api/ai/explain/{ticker} returns a non-blank narrative drawn from the seeded fixture in demo mode"
    - "GET /api/ai/commentary returns CommentaryDto with headline+body+bulletPoints for the session persona in demo mode"
    - "Both endpoints route through ChatClientStrategy.forSession → DemoModeAdvisor (single demo/live switch); no if(demoMode) in services"
    - "explain rejects a ticker not in the principal's portfolio (IDOR-safe, principal-scoped)"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java"
      provides: "explain(portfolioId, ticker) → ExplainResponseDto via ChatClient + AI_SEED_TYPE/SUBJECT params"
      contains: "AI_SEED_TYPE"
    - path: "backend/src/main/java/com/quantlens/ai/service/CommentaryService.java"
      provides: "commentary(portfolioId) → CommentaryDto via ChatClient; maps stored content to headline/body/bullets"
      contains: "DAILY_COMMENTARY"
  key_links:
    - from: "backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java"
      to: "ChatClientStrategy.forSession"
      via: "prompt().advisors(param AI_SEED_TYPE/AI_SEED_SUBJECT).call().content()"
      pattern: "forSession"
    - from: "backend/src/main/java/com/quantlens/ai/service/CommentaryService.java"
      to: "persona resolution"
      via: "portfolioId → persona key (GROWTH|INCOME|BALANCED)"
      pattern: "GROWTH"
---

<objective>
Implement the two AI feature services so the explain-this-position (AI-07) and daily-commentary (AI-08) endpoints return real seeded content in demo mode by routing through `ChatClientStrategy.forSession()` + `DemoModeAdvisor`. Both services set the `AI_SEED_TYPE`/`AI_SEED_SUBJECT` advisor params and a live-mode prompt; in demo mode the advisor short-circuits to the seeded fixture. This turns the `AiControllerIntegrationTest` seeded-content cases GREEN and completes the backend feature surface.

Purpose: Deliver explain + commentary through the seam so they are seeded-by-default and live-ready.
Output: Real ExplainPositionService + CommentaryService; AiController wired; backend suite fully green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-mode-ai-seam/06-RESEARCH.md
@.planning/phases/06-demo-mode-ai-seam/06-PATTERNS.md
@.planning/phases/06-demo-mode-ai-seam/06-01-SUMMARY.md
@.planning/phases/06-demo-mode-ai-seam/06-02-SUMMARY.md

<interfaces>
<!-- From 06-01/02 (already created). -->
ChatClientStrategy.forSession(LlmKeySessionHolder) -> ChatClient
DemoModeAdvisor reads context params AI_SEED_TYPE, AI_SEED_SUBJECT
ExplainResponseDto(String narrative)
CommentaryDto(String headline, String body, List<String> bulletPoints)
PortfolioService.getHoldings(Long portfolioId) -> List<HoldingDto>   // HoldingDto keyed by ticker (no id)
HoldingDto fields: ticker, name, sector, quantity, avgCostBasis, currentPrice, currentMarketValue, portfolioWeight, unrealizedPnlAbs, unrealizedPnlPct
PortfolioRepository.findPortfolioIdByUsername(String) -> Optional<Long>
seed subjectId for commentary: persona key (GROWTH|INCOME|BALANCED) — derive from portfolioId/holdings
DAILY_COMMENTARY content storage convention: see 06-02-SUMMARY (headline/body/bullets encoding in the single content string)
</interfaces>

# Analogs:
@backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java
@backend/src/main/java/com/quantlens/portfolio/service/PortfolioService.java
@backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ExplainPositionService and CommentaryService routing through the seam</name>
  <files>backend/src/main/java/com/quantlens/ai/service/ExplainPositionService.java, backend/src/main/java/com/quantlens/ai/service/CommentaryService.java</files>
  <read_first>
    - RESEARCH.md Pattern 9 ExplainPositionService (lines 562-595), Pattern 3 per-call advisor params (lines 296-321), Pitfall 12 seeded/live consistency (lines 554-558, 595)
    - PATTERNS.md no-analog services (lines 1042-1043), principal resolution shared pattern (lines 963-976)
    - 06-02-SUMMARY.md (DAILY_COMMENTARY storage convention — the exact split format for headline/body/bullets)
    - backend/src/main/java/com/quantlens/portfolio/service/PortfolioService.java (getHoldings signature + HoldingDto fields)
  </read_first>
  <action>
ExplainPositionService.explain(Long portfolioId, String ticker) → ExplainResponseDto:
- Resolve the holding: call portfolioService.getHoldings(portfolioId), filter by ticker. If the ticker is not in the principal's holdings, throw ResponseStatusException(HttpStatus.NOT_FOUND) — this is the IDOR-safe guard (a user cannot explain a position they do not hold). Do NOT trust the ticker blindly for the seed lookup.
- Build a metrics summary string from the matched HoldingDto (ticker, sector, quantity, currentMarketValue, portfolioWeight, unrealizedPnlPct) for the LIVE prompt only.
- Call strategy.forSession(keyHolder).prompt() with a system prompt (concise financial-analyst persona) and a user prompt template referencing ticker + metrics; set advisor params via `.advisors(spec -> spec.param("AI_SEED_TYPE","EXPLAIN_POSITION").param("AI_SEED_SUBJECT", ticker))`; `.call().content()` returns the String. In demo mode the DemoModeAdvisor short-circuits and returns the seeded EXPLAIN_POSITION fixture for that ticker; in live mode the real model generates a structurally-similar narrative (Pitfall 12).
- Wrap the content in new ExplainResponseDto(content). Wrap the ChatClient call in a try/catch that, on any provider exception, throws a generic ResponseStatusException(HttpStatus.BAD_GATEWAY) WITHOUT including exception.getMessage() (Pitfall 5 — never echo a provider error that could carry the key). @Transactional(readOnly=true).

CommentaryService.commentary(Long portfolioId) → CommentaryDto:
- Derive the persona key (GROWTH|INCOME|BALANCED) from the portfolio. Determine the mapping from the seeded portfolios — read 06-01/06-02 SUMMARY for how persona maps to portfolioId/username, or infer from holdings composition. Document the chosen mapping in SUMMARY.
- Call strategy.forSession(keyHolder).prompt() with a commentary system+user prompt; set `.advisors(spec -> spec.param("AI_SEED_TYPE","DAILY_COMMENTARY").param("AI_SEED_SUBJECT", personaKey))`; `.call().content()` returns the stored content string (demo) or a live generation.
- Parse the content string into CommentaryDto(headline, body, bulletPoints) using the EXACT storage convention recorded in 06-02-SUMMARY (e.g. first line = headline, blank-line-separated body, "- "-prefixed lines = bullets). Keep the parser tolerant: if the content does not match the convention (e.g. a free-form live response), fall back to headline = first sentence, body = remainder, bulletPoints = empty. Same try/catch generic-error handling as explain. @Transactional(readOnly=true).

Neither service contains any `if (demoMode)` branch — the demo/live switch lives ONLY in DemoModeAdvisor. The services always go through strategy.forSession().
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q compile *>&1 | Select-String -Pattern 'BUILD SUCCESS','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - Both services compile and route through strategy.forSession(keyHolder).prompt()...call().content()
    - explain sets AI_SEED_TYPE=EXPLAIN_POSITION, AI_SEED_SUBJECT=ticker; commentary sets DAILY_COMMENTARY + persona key
    - explain throws NOT_FOUND for a ticker not in the principal's holdings (IDOR-safe)
    - Neither service has an if(demoMode) branch
    - Provider exceptions wrapped as generic BAD_GATEWAY (no message echo)
    - Persona-key mapping + commentary parse fallback documented in SUMMARY
  </acceptance_criteria>
  <done>Both feature services deliver seeded content in demo mode through the single DemoModeAdvisor seam.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AiController to the real services and turn the feature integration tests GREEN</name>
  <files>backend/src/main/java/com/quantlens/ai/api/AiController.java</files>
  <read_first>
    - PATTERNS.md AiController via AnalyticsController (lines 169-228), readOnly endpoint pattern (lines 218-227), principal resolution (lines 205-216)
    - RESEARCH.md Validation Test Map (lines 954-955) — explainReturnsSeededContent + commentaryReturnsSeededContent expectations
    - backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java (the RED feature-content cases from 06-01)
  </read_first>
  <action>
Confirm AiController (created as a near-stub in 06-01) now correctly delegates to the real services: GET /explain/{ticker} resolves portfolioId via the verbatim resolvePortfolioId(Authentication), calls explainService.explain(portfolioId, ticker), returns ResponseEntity.ok(ExplainResponseDto); GET /commentary resolves portfolioId, calls commentaryService.commentary(portfolioId), returns ResponseEntity.ok(CommentaryDto). Both @Transactional(readOnly=true). The path variable for explain is the ticker String. No portfolioId from request (IDOR-safe). If 06-01 already wired these delegations, this task verifies and adjusts only the return/exception handling (e.g. let the service's ResponseStatusException propagate to a 404/502).

Run the AiControllerIntegrationTest seeded-content cases (explainReturnsSeededContent for /explain/AAPL, commentaryReturnsSeededContent for /commentary) — they must now be GREEN because the services return seeded content. Also confirm AiDemoModeIntegrationTest is GREEN (no provider call). Run the full backend suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd backend; $env:JAVA_HOME='C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot'; .\mvnw.cmd -q test -Dtest=AiControllerIntegrationTest,AiDemoModeIntegrationTest,KeyLeakageIntegrationTest *>&1 | Select-String -Pattern 'BUILD SUCCESS','Tests run','BUILD FAILURE'</automated>
  </verify>
  <acceptance_criteria>
    - AiControllerIntegrationTest#explainReturnsSeededContent and #commentaryReturnsSeededContent PASS
    - AiDemoModeIntegrationTest PASSES (seeded content, no provider call)
    - KeyLeakageIntegrationTest still PASSES (no regression)
    - explain path variable is ticker; resolvePortfolioId is the verbatim IDOR-safe copy
  </acceptance_criteria>
  <done>Both feature endpoints return seeded content end-to-end; backend feature surface complete and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/ai/explain/{ticker} | Untrusted ticker path var; must be validated against the principal's holdings |
| service → LLM provider (live) | Provider errors must not echo back (could carry the key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure (IDOR) | ExplainPositionService | mitigate | Ticker validated against portfolioService.getHoldings(principalPortfolioId); NOT_FOUND if not held; portfolioId never from request |
| T-06-09 | Information Disclosure | provider error responses | mitigate | ChatClient calls wrapped in try/catch; generic BAD_GATEWAY; exception.getMessage() never forwarded (Pitfall 5) |
| T-06-01 | Information Disclosure | key leakage (regression) | mitigate | KeyLeakageIntegrationTest re-run; explain/commentary return only narrative/commentary DTOs |
| T-06-10 | Tampering | demo/live branching | mitigate | Single switch in DemoModeAdvisor; services have no if(demoMode); prevents accidental live call in demo |
</threat_model>

<verification>
- `.\mvnw.cmd test -Dtest=AiControllerIntegrationTest,AiDemoModeIntegrationTest,KeyLeakageIntegrationTest` PASS
- `.\mvnw.cmd test` full backend suite green (all AI tests GREEN)
</verification>

<success_criteria>
- explain + commentary return real seeded content in demo mode via the single seam
- explain is principal-scoped and IDOR-safe (ticker validated against holdings)
- No if(demoMode) in services; provider errors never echo
- Full backend suite green
</success_criteria>

<output>
Create `.planning/phases/06-demo-mode-ai-seam/06-03-SUMMARY.md` when done. Record the persona-key mapping (portfolioId/username → GROWTH|INCOME|BALANCED) and the commentary parse fallback behavior for the frontend team in 06-04.
</output>
