---
phase: 06-demo-mode-ai-seam
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - frontend/src/api/ai.ts
  - frontend/src/stores/ai.ts
  - frontend/public/ai-structured-demo.json
  - frontend/src/components/ai/BYOKeyModal.vue
  - frontend/src/components/ai/AiModeBadge.vue
  - frontend/src/components/ai/ExplainDrawer.vue
  - frontend/src/components/ai/CommentaryCard.vue
  - frontend/src/components/ai/StructuredOutputChart.vue
  - frontend/src/__tests__/components/StructuredOutputChart.test.ts
  - frontend/src/components/TopBar.vue
  - frontend/src/components/HoldingsTable.vue
  - frontend/src/views/DashboardView.vue
autonomous: false
requirements: [AI-01, AI-02, AI-07, AI-08]
user_setup: []

must_haves:
  truths:
    - "With no key, the dashboard shows a Demo mode badge, a seeded daily commentary card, a seeded structured-output chart, and clicking a holding opens an explain drawer with seeded narrative"
    - "Opening the BYO-key popup, selecting a provider, and entering a key flips the badge to Live; the key is never written to localStorage, sessionStorage, or store state"
    - "Returning to demo (clear key) flips the badge back to Demo"
    - "The structured-output chart renders from a seeded/typed JSON fixture (demo) — demonstrating the structured-output seam with NO live LLM wiring (live AI-06 lands in Phase 8)"
    - "vue-tsc production build succeeds; all AI component tests pass (incl. StructuredOutputChart)"
  artifacts:
    - path: "frontend/src/stores/ai.ts"
      provides: "Pinia ai store (status/explanation/commentary/structured asyncState + setKey/clearKey/fetchExplanation/fetchCommentary/fetchStructured)"
      contains: "setKey"
    - path: "frontend/src/components/ai/BYOKeyModal.vue"
      provides: "Screenshot-worthy BYO-key popup; password input; key never persisted"
      contains: "type=\"password\""
    - path: "frontend/src/components/ai/AiModeBadge.vue"
      provides: "Demo/Live badge in top bar driven by /api/ai/status"
      contains: "useAiStore"
    - path: "frontend/src/components/ai/CommentaryCard.vue"
      provides: "Daily commentary card (headline+body+bullets) with loading/error states"
      contains: "headline"
    - path: "frontend/src/components/ai/ExplainDrawer.vue"
      provides: "Slide-out explain-this-position drawer with loading/error states"
      contains: "narrative"
    - path: "frontend/src/components/ai/StructuredOutputChart.vue"
      provides: "Seeded structured-output chart rendering a typed JSON fixture (demo-only seam stub)"
      contains: "VChart"
    - path: "frontend/public/ai-structured-demo.json"
      provides: "Seeded typed JSON fixture driving the structured-output chart in demo mode"
      contains: "series"
  key_links:
    - from: "frontend/src/stores/ai.ts"
      to: "/api/ai/* endpoints + /ai-structured-demo.json"
      via: "axios singleton (session + XSRF) for /api/ai/*; plain fetch/axios for the static seeded JSON"
      pattern: "axios\\.(get|post|delete)"
    - from: "frontend/src/components/HoldingsTable.vue"
      to: "ExplainDrawer"
      via: "row click emits explain(ticker) → DashboardView opens drawer"
      pattern: "emit"
    - from: "frontend/src/views/DashboardView.vue"
      to: "CommentaryCard / BYOKeyModal / ExplainDrawer / StructuredOutputChart"
      via: "SlotPlaceholder replacement + onMounted fetchStatus/fetchCommentary/fetchStructured"
      pattern: "aiStore"
---

## Phase Goal

**As a** demo viewer of QuantLens, **I want to** see AI-authored commentary, a structured-output chart, and per-position explanations with zero setup and flip everything to live by pasting my own key in a popup, **so that** the dashboard is instantly convincing for screenshots yet genuinely live on demand.

<objective>
Build the frontend vertical slice that makes the demo↔live seam visible: the `ai` Pinia store + typed `api/ai.ts` interfaces, the four AI components (BYOKeyModal, AiModeBadge, ExplainDrawer, CommentaryCard), the seeded structured-output chart stub (StructuredOutputChart), and the wiring into TopBar/HoldingsTable/DashboardView replacing the Phase-6 SlotPlaceholders. After this plan a real user can, with no key, read seeded commentary, see a seeded structured-output chart, and click a holding for a seeded explanation; then open the popup, paste a key, and watch the mode badge flip to Live — with the key never touching localStorage, sessionStorage, or store state.

Purpose: Deliver the user-facing AI panels (including the structured-output chart required by ROADMAP Success Criterion #1) and the screenshot-worthy BYO-key popup (the exact artifact the user asked for).
Output: ai store + components (incl. StructuredOutputChart) wired into the dashboard; frontend build + component tests green; human-verified screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-mode-ai-seam/06-RESEARCH.md
@.planning/phases/06-demo-mode-ai-seam/06-PATTERNS.md
@.planning/phases/06-demo-mode-ai-seam/06-03-SUMMARY.md

<interfaces>
<!-- Backend endpoints (from 06-01/02/03), all session-authenticated, axios singleton handles cookie + XSRF. -->
GET    /api/ai/status            -> { mode: "demo"|"live", provider?: "anthropic"|"openai" }
POST   /api/ai/key               body { provider, apiKey } -> { mode:"live", provider }  (key never echoed)
DELETE /api/ai/key               -> { mode:"demo" }
GET    /api/ai/explain/{ticker}  -> { narrative: string }   // PATH VAR IS TICKER (string)
GET    /api/ai/commentary        -> { headline: string, body: string, bulletPoints: string[] }

# Structured-output stub (demo only — NO backend endpoint, NO live LLM): a static seeded JSON the store fetches.
GET    /ai-structured-demo.json  -> { title: string, subtitle?: string, series: { label: string, value: number }[] }
# Live structured output (AI-06, BeanOutputConverter → typed record → chart) lands in Phase 8. This stub renders the SAME DTO shape so Phase 8 swaps the source (static JSON → live typed record) without touching the chart.

# Frontend test scaffolds (RED, from 06-01) these components must satisfy:
frontend/src/__tests__/components/BYOKeyModal.test.ts   (keyNotInLocalStorage, renders radios+password, emits)
frontend/src/__tests__/components/AiModeBadge.test.ts   (Demo vs Live text)
frontend/src/__tests__/components/ExplainDrawer.test.ts (skeleton/populated/hidden)
frontend/src/__tests__/components/CommentaryCard.test.ts(skeleton/populated/error)
# New in this plan: frontend/src/__tests__/components/StructuredOutputChart.test.ts (skeleton/populated/error)
</interfaces>

# Frontend analogs (read for style):
@frontend/src/stores/portfolio.ts
@frontend/src/api/analytics.ts
@frontend/src/components/RiskScorecard.vue
@frontend/src/components/KpiCard.vue
@frontend/src/components/AllocationChart.vue
@frontend/src/components/TopBar.vue
@frontend/src/components/HoldingsTable.vue
@frontend/src/views/DashboardView.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create api/ai.ts types + ai Pinia store (key never persisted, structured-output fetch)</name>
  <files>frontend/src/api/ai.ts, frontend/src/stores/ai.ts</files>
  <behavior>
    - fetchStatus() populates status.data from GET /api/ai/status
    - setKey(provider, apiKey) POSTs to /api/ai/key and assigns the returned {mode,provider} to status.data; the apiKey argument is NEVER assigned to any reactive state, localStorage, or sessionStorage
    - clearKey() DELETEs /api/ai/key and resets status to {mode:'demo'}, clears explanation+commentary
    - fetchExplanation(ticker: string) populates explanation.data from GET /api/ai/explain/{ticker}
    - fetchCommentary() populates commentary.data from GET /api/ai/commentary
    - fetchStructured() populates structured.data from GET /ai-structured-demo.json (static seeded fixture)
  </behavior>
  <read_first>
    - RESEARCH.md ai.ts store, setKey security invariant, Frontend ai.ts notes
    - PATTERNS.md ai.ts store via portfolio.ts, api/ai.ts via analytics.ts, asyncState shared factory
    - frontend/src/stores/portfolio.ts (asyncState factory; defineStore setup + fetch pattern; $reset)
    - frontend/src/api/analytics.ts (module header rule: DTO interfaces only, no axios here)
    - 06-03-SUMMARY.md (explain is keyed by TICKER string — confirm fetchExplanation signature)
  </read_first>
  <action>
Create frontend/src/api/ai.ts following analytics.ts: DTO INTERFACES ONLY, no axios calls, no interceptors (the auth.ts singleton owns withCredentials + XSRF + 401). Export `AiStatus { mode: 'demo'|'live'; provider?: 'anthropic'|'openai' }`, `ExplainResponse { narrative: string }`, `CommentaryDto { headline: string; body: string; bulletPoints: string[] }`, and `StructuredChartDto { title: string; subtitle?: string; series: { label: string; value: number }[] }` (the typed shape the structured-output stub renders — mirrors the Phase-8 live record so the chart is source-agnostic). Include the same header comment as analytics.ts explaining axios calls live in the store.

Create frontend/src/stores/ai.ts following portfolio.ts: copy the AsyncState<T> interface + asyncState<T>() factory VERBATIM (do not import from portfolio.ts). defineStore('ai', setup): state `status = asyncState<AiStatus>()`, `explanation = asyncState<ExplainResponse>()`, `commentary = asyncState<CommentaryDto>()`, `structured = asyncState<StructuredChartDto>()`. Actions per RESEARCH: fetchStatus, setKey(provider, apiKey), clearKey, fetchExplanation(ticker), fetchCommentary, fetchStructured, plus $reset.

CRITICAL signature note: `fetchExplanation(ticker: string)` — the endpoint is GET /api/ai/explain/{ticker}. IGNORE any `holdingId: number` signature that appears in RESEARCH/PATTERNS samples; the contract is a ticker STRING (confirmed in 06-03-SUMMARY). fetchExplanation must take and forward a string.

fetchStructured: GET the static seeded fixture at `/ai-structured-demo.json` (served from frontend/public) into structured.data using the same asyncState loading/error pattern. This is demo-only; there is NO backend AI call and NO live LLM here — the stub demonstrates the structured-output seam by rendering a typed JSON fixture. Document in SUMMARY that Phase 8 will swap this source for a live BeanOutputConverter-typed record (AI-06) without changing the chart.

CRITICAL security invariant: setKey accepts apiKey as a parameter, POSTs it to /api/ai/key immediately, and assigns ONLY the returned {mode,provider} to status.data. The apiKey must NEVER be assigned to status/explanation/commentary/structured state, NEVER written to localStorage OR sessionStorage, NEVER stored in a module-level variable. $reset must NOT include any apiKey field (the key is never in store state).
  </action>
  <verify>
    <automated>cd frontend; npm run test -- --run ai *>&1 | Select-String -Pattern 'passed','failed'</automated>
  </verify>
  <acceptance_criteria>
    - api/ai.ts exports AiStatus, ExplainResponse, CommentaryDto, StructuredChartDto interfaces and contains no axios import
    - stores/ai.ts setKey POSTs the key and never assigns it to reactive state, localStorage, or sessionStorage
    - $reset has no apiKey field
    - fetchExplanation signature takes a ticker string (NOT a numeric holdingId)
    - fetchStructured loads /ai-structured-demo.json into structured.data (demo-only, no backend AI call)
  </acceptance_criteria>
  <done>Typed ai store and DTO module exist; key-handling security invariant holds at the store layer; structured-output fetch wired to the seeded fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the AI components (BYOKeyModal, AiModeBadge, ExplainDrawer, CommentaryCard, StructuredOutputChart) + seeded fixture</name>
  <files>frontend/src/components/ai/BYOKeyModal.vue, frontend/src/components/ai/AiModeBadge.vue, frontend/src/components/ai/ExplainDrawer.vue, frontend/src/components/ai/CommentaryCard.vue, frontend/src/components/ai/StructuredOutputChart.vue, frontend/src/__tests__/components/StructuredOutputChart.test.ts, frontend/public/ai-structured-demo.json</files>
  <behavior>
    - BYOKeyModal: when open, renders two provider radios + a password input; submit calls aiStore.setKey then clears the local key ref + emits 'submitted'; cancel emits 'close'; the key is never written to localStorage OR sessionStorage; the local key ref is cleared after submit
    - AiModeBadge: renders "Demo" (muted) when status.mode==demo, "Live" (up-color) when status.mode==live
    - ExplainDrawer: shimmer skeleton when loading; renders narrative when populated; static error copy on error; hidden when open=false
    - CommentaryCard: shimmer skeleton when loading; renders headline + body + bulletPoints when populated; static error copy on error
    - StructuredOutputChart: shimmer skeleton when loading; renders a small chart (title + series) from a StructuredChartDto when populated; static error copy on error
  </behavior>
  <read_first>
    - RESEARCH.md BYOKeyModal UX, password safety, setKey invariant
    - PATTERNS.md BYOKeyModal via AllocationChart/KpiCard (three-state template, CSS tokens, defineProps/defineEmits, password input safety, focus-visible); AiModeBadge via TopBar (pill/badge CSS, colors); ExplainDrawer via RiskScorecard (three-state + shimmer + slide-in panel z-index 200); CommentaryCard via KpiCard+RiskScorecard (card container + bullets); shimmer shared pattern; CSS token rule
    - frontend/src/components/RiskScorecard.vue (three-state + shimmer), frontend/src/components/KpiCard.vue (card + tokens), frontend/src/components/TopBar.vue (badge CSS), frontend/src/components/AllocationChart.vue (panel + retry + VChart usage), frontend/src/components/AttributionChart.vue (bar chart VChart option for a label/value series)
    - frontend/src/__tests__/components/{BYOKeyModal,AiModeBadge,ExplainDrawer,CommentaryCard}.test.ts (the RED specs to satisfy)
    - frontend/src/plugins/chart-colors.ts (CHART_COLORS palette for the structured-output chart)
  </read_first>
  <action>
First, create the seeded fixture frontend/public/ai-structured-demo.json: a small StructuredChartDto-shaped object — e.g. `{ "title": "AI-Detected Sector Exposure", "subtitle": "Structured output (demo)", "series": [ { "label": "Technology", "value": 42.5 }, { "label": "Financials", "value": 21.0 }, { "label": "Energy", "value": 14.3 }, { "label": "Consumer Staples", "value": 12.7 }, { "label": "Healthcare", "value": 9.5 } ] }`. Make the labels/values plausible against the seeded growth/balanced personas (don't contradict the allocation chart). This is the "structured output" the demo seam returns; keep it lightweight.

All components: `<script setup>`, scoped CSS using ONLY var(--color-*)/var(--space-*)/var(--radius-*)/var(--shadow-*) tokens (zero hardcoded hex), focus-visible on interactive elements, shimmer skeleton copied verbatim from the shared pattern.

BYOKeyModal.vue: defineProps<{ open: boolean }>(), defineEmits<{ close: []; submitted: [] }>(). When open, render a centered modal (overlay + dialog) with: a title "Connect Your Own AI Key", two provider radios (Anthropic Claude / OpenAI GPT), an `<input type="password" autocomplete="new-password">` bound to a local `keyInput` ref, the reassurance line "Your key is used only for this session and is never saved or transmitted to us.", a Cancel button (emit close) and a "Connect Live AI" submit button. On submit: call `useAiStore().setKey(provider, keyInput.value)`, then set keyInput.value = '' (clear immediately), emit 'submitted', emit 'close'. On error (caught from setKey): show a generic "Key rejected — check provider and key format" message; never display key-related detail. Match the RESEARCH ASCII mockup — this is the README screenshot artifact, make it polished.

AiModeBadge.vue: no props — reads `useAiStore().status.data`. Render a read-only `<span>` (not a button): "Demo" with var(--color-text-muted) when mode is demo/undefined, "Live" with var(--color-up) when mode is live; small pill padding per TopBar .persona-pill style. Optionally show the provider name when live.

ExplainDrawer.vue: defineProps<{ open: boolean; explanation: ExplainResponse | null; loading: boolean; error: string | null }>(), defineEmits<{ close: [] }>(). A fixed-position slide-in panel (position: fixed, right, var(--color-bg-elevated), z-index 200 above the top bar) shown when open=true with an overlay; three states inside (shimmer skeleton when loading, static error copy + retry when error, narrative paragraph(s) when populated). A close affordance emits 'close'. Hidden entirely when open=false.

CommentaryCard.vue: defineProps<{ commentary: CommentaryDto | null; loading: boolean; error: string | null }>(), defineEmits<{ retry: [] }>(). Full-width card (KpiCard container style): headline as primary text, body as a secondary-color paragraph, bulletPoints as a <ul> with var(--color-text-secondary); shimmer skeleton (~120px, matching the SlotPlaceholder it replaces) when loading; static error copy + retry on error.

StructuredOutputChart.vue: defineProps<{ structured: StructuredChartDto | null; loading: boolean; error: string | null }>(), defineEmits<{ retry: [] }>(). Card container (KpiCard/AllocationChart style). Three states: shimmer skeleton when loading; static error copy + retry on error; when populated, render a small VChart (vue-echarts) — a horizontal bar chart of series label→value using CHART_COLORS, with props.structured.title as the card heading and subtitle as muted secondary text. Mirror AttributionChart.vue's VChart option shape for a label/value series (typed EChartsOption, no v-html, label formatters use typed numbers only). Keep it lightweight — one bar series. Add a small "Demo" / "structured output" affordance so the screenshot reads as the structured-output panel. NOTE: this is a DEMO STUB — it renders the seeded JSON only; there is NO live LLM call here (live structured output AI-06 lands in Phase 8 and will feed the SAME StructuredChartDto shape).

Create frontend/src/__tests__/components/StructuredOutputChart.test.ts (RiskScorecard.test.ts RED style → GREEN here): mount with loading:true asserts a `.skeleton` exists; mount with a populated StructuredChartDto asserts the title text renders and the VChart is present (stub vue-echarts as in setup.ts / other chart tests); mount with error asserts a [role="alert"] + retry affordance. Follow the existing chart-component test approach (vue-echarts is stubbed in __tests__/setup.ts — confirm and reuse).
  </action>
  <verify>
    <automated>cd frontend; npm run test -- --run BYOKeyModal AiModeBadge ExplainDrawer CommentaryCard StructuredOutputChart *>&1 | Select-String -Pattern 'passed','failed'</automated>
  </verify>
  <acceptance_criteria>
    - All five component tests PASS (BYOKeyModal, AiModeBadge, ExplainDrawer, CommentaryCard, StructuredOutputChart)
    - BYOKeyModal#keyNotInLocalStorage extended: after submit, NEITHER localStorage NOR sessionStorage contains any entry whose value equals the typed key, AND the local key ref is cleared (keyInput is '' after submit)
    - BYOKeyModal uses type="password" autocomplete="new-password"
    - StructuredOutputChart renders title + a VChart from a StructuredChartDto; three states covered; it is a demo stub with NO live LLM call
    - frontend/public/ai-structured-demo.json is a valid StructuredChartDto (title + series array)
    - Zero hardcoded hex/rgb in any component (tokens only); ExplainDrawer panel z-index above the top bar
  </acceptance_criteria>
  <done>All five AI components exist, pass their tests, and follow the design-token system; the structured-output seam stub renders the seeded fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Wire components into TopBar, HoldingsTable, and DashboardView (replace Phase-6 SlotPlaceholders)</name>
  <files>frontend/src/components/TopBar.vue, frontend/src/components/HoldingsTable.vue, frontend/src/views/DashboardView.vue</files>
  <read_first>
    - PATTERNS.md DashboardView modification: SlotPlaceholder replacement map, imports + onMounted, TopBar AiModeBadge placement
    - frontend/src/views/DashboardView.vue (Phase-6 SlotPlaceholders: AI Daily Commentary col-12, AI Q&A col-8, LLM Key col-4; HoldingsTable usage)
    - frontend/src/components/TopBar.vue (.user-area div), frontend/src/components/HoldingsTable.vue (defineEmits currently { retry: [] }; row markup)
    - 06-03-SUMMARY.md (commentary/explain endpoint shapes; ticker is the explain key)
  </read_first>
  <action>
TopBar.vue: import AiModeBadge and place `<AiModeBadge />` inside `.user-area` before the username display (per PATTERNS).

HoldingsTable.vue: add an `explain: [ticker: string]` event to defineEmits (alongside the existing retry). Make each holding row clickable (an accessible affordance — a button or row with role + keydown Enter/Space) that emits `explain` with that row's ticker. Keep the existing sort behavior on header cells intact; the row click for explain must not be swallowed by the sort handlers (the sort handlers are on header cells, not body rows, so no conflict). Add focus-visible styling on the clickable row affordance.

DashboardView.vue: add imports `useAiStore`, CommentaryCard, ExplainDrawer, BYOKeyModal, StructuredOutputChart; `const aiStore = useAiStore()`. In onMounted add `void aiStore.fetchStatus()`, `void aiStore.fetchCommentary()`, and `void aiStore.fetchStructured()`. Add local refs: `explainOpen` (boolean), `keyModalOpen` (boolean). Replace/extend the Phase-6 SlotPlaceholders:
- AI Daily Commentary col-12 → `<CommentaryCard :commentary="aiStore.commentary.data" :loading="aiStore.commentary.loading" :error="aiStore.commentary.error" @retry="aiStore.fetchCommentary()" />`
- AI Q&A col-8 → render the structured-output chart here (this is a legitimate Phase-6 AI panel per ROADMAP Success Criterion #1) AND keep a small relabeled "AI Q&A — Phase 7" placeholder for the RAG feature. Concretely: split this region so `<StructuredOutputChart :structured="aiStore.structured.data" :loading="aiStore.structured.loading" :error="aiStore.structured.error" @retry="aiStore.fetchStructured()" />` occupies the col-8 AI panel slot, and add a clearly-labeled `SlotPlaceholder label="AI Q&A — Phase 7"` in an adjacent/sub region (RAG Q&A lands in Phase 7 — do NOT build Q&A here). The structured-output chart is the Phase-6 deliverable that fills this AI panel; the Q&A placeholder is the one slot that legitimately stays a placeholder, relabeled for Phase 7.
- LLM Key col-4 → a "Connect Live AI" trigger button (opens the popup) plus `<BYOKeyModal :open="keyModalOpen" @close="keyModalOpen = false" @submitted="aiStore.fetchStatus(); aiStore.fetchCommentary()" />` so submitting refreshes the badge + commentary.
Wire HoldingsTable's new explain event: `@explain="(ticker) => { explainOpen = true; aiStore.fetchExplanation(ticker) }"`. Render `<ExplainDrawer :open="explainOpen" :explanation="aiStore.explanation.data" :loading="aiStore.explanation.loading" :error="aiStore.explanation.error" @close="explainOpen = false" />`.

Run the full frontend test suite + vue-tsc build to confirm green and no type errors.
  </action>
  <verify>
    <automated>cd frontend; npm run test -- --run *>&1 | Select-String -Pattern 'passed','failed'; npm run build *>&1 | Select-String -Pattern 'built in','error'</automated>
  </verify>
  <acceptance_criteria>
    - AiModeBadge appears in TopBar .user-area
    - HoldingsTable emits explain(ticker) on a row click; existing sort still works
    - DashboardView renders CommentaryCard + StructuredOutputChart + ExplainDrawer + BYOKeyModal; AI Q&A slot relabeled "Phase 7"
    - StructuredOutputChart fills the col-8 AI panel and renders the seeded fixture (ROADMAP Success Criterion #1 — structured-output chart renders seeded content in demo)
    - onMounted calls fetchStatus + fetchCommentary + fetchStructured; submitted refreshes status + commentary
    - Full frontend test suite PASSES; `npm run build` (vue-tsc) succeeds with no type errors
  </acceptance_criteria>
  <done>All AI panels (commentary, structured-output chart, explain drawer, BYO-key popup, mode badge) are wired into the dashboard; the demo/live seam is fully visible; frontend build + tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the demo-mode AI seam and capture README screenshots</name>
  <what-built>The full demo-mode AI seam, frontend included: a Demo/Live badge in the top bar, a seeded daily commentary card, a seeded structured-output chart, a click-to-explain drawer on holdings, and the BYO-key popup. All seeded with zero key; flips to live when a key is entered. Backend + frontend suites green; KeyLeakageIntegrationTest green.</what-built>
  <action>Pause for human verification after the automated frontend build + tests pass. Claude has already built and wired everything; this checkpoint confirms the visual/functional result and captures the README screenshots the user asked for.</action>
  <how-to-verify>
    1. From a clean state run `docker compose up` and wait for backend + frontend healthy.
    2. Open the app, log in as alice/demo1234.
    3. Confirm the top bar shows a "Demo" badge.
    4. Confirm the Daily Commentary card renders authored, LLM-looking text consistent with Alice's growth holdings (AAPL/MSFT/NVDA/AMZN/TSLA).
    5. Confirm the structured-output chart renders the seeded sector-exposure data (title + bars) — this is the structured-output panel demo. Take a screenshot (README artifact).
    6. Click a holding row (e.g. AAPL) — the explain drawer slides in with a seeded narrative naming that ticker. Take a screenshot (README artifact).
    7. Click "Connect Live AI" — the BYO-key popup opens. Verify it looks screenshot-worthy (provider radios, password input, the "never saved or transmitted" reassurance line). Take a screenshot (README artifact).
    8. (Optional, with a real key) Select Anthropic, paste a real key, submit — confirm the badge flips to "Live" and commentary/explain now call the live model. Then clear the key (return to demo) and confirm the badge flips back to "Demo".
    9. Switch personas to Bob (income) and Charlie (balanced) — confirm each shows distinct, persona-appropriate commentary.
  </how-to-verify>
  <verify>
    <human-check>Demo badge + seeded commentary + structured-output chart + explain drawer + BYO-key popup render correctly; badge flips Demo↔Live; persona commentary distinct; screenshots captured.</human-check>
  </verify>
  <done>User has visually confirmed the seam and captured README screenshots.</done>
  <resume-signal>Type "approved" or describe issues (e.g. commentary doesn't match holdings, chart not rendering, popup styling, badge not flipping).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → POST /api/ai/key | The user's key crosses one-way client→server; must never persist client-side |
| store/component reactive state | Key must never land in Pinia state, localStorage, sessionStorage, or Vue refs after submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | BYOKeyModal / ai store | mitigate | setKey POSTs the key then the local ref is cleared; key never assigned to reactive state, localStorage, OR sessionStorage; BYOKeyModal.test.ts#keyNotInLocalStorage asserts both storages + ref-cleared |
| T-06-12 | Information Disclosure | error display | mitigate | Generic "Key rejected" copy only; never render provider/exception detail that could echo the key |
| T-06-13 | Spoofing/CSRF | POST/DELETE /api/ai/key | mitigate | axios singleton attaches X-XSRF-TOKEN + session cookie (auth.ts interceptor); backend endpoint authenticated |
| T-06-14 | Tampering | structured-output stub source | accept | Static seeded JSON in /public is demo-only content (no secrets, no live LLM); Phase 8 replaces the source with a typed live record (AI-06) — low-value, read-only fixture |
</threat_model>

<verification>
- `npm run test -- --run` full frontend suite PASSES (incl. 5 AI component tests: BYOKeyModal/AiModeBadge/ExplainDrawer/CommentaryCard/StructuredOutputChart, no regressions)
- `npm run build` (vue-tsc) succeeds with no type errors
- Human-verify checkpoint: demo badge + seeded commentary + structured-output chart + explain drawer + BYO-key popup all render; badge flips Demo↔Live; persona commentary distinct; screenshots captured
</verification>

<success_criteria>
- AI store + 5 components (incl. StructuredOutputChart) + DashboardView/TopBar/HoldingsTable wiring complete
- Demo mode shows seeded commentary + seeded structured-output chart + explain with zero key; popup flips to Live; key never persists client-side (localStorage AND sessionStorage)
- Structured-output chart renders seeded content in demo mode (ROADMAP Phase 6 Success Criterion #1) — demo stub only, live AI-06 deferred to Phase 8
- Frontend build + tests green
- Human-verified for screenshots (README artifacts captured)
</success_criteria>

<output>
Create `.planning/phases/06-demo-mode-ai-seam/06-04-SUMMARY.md` when done. Record any human-verify feedback, the final screenshot locations, and the note that the structured-output chart is a demo stub fed by /ai-structured-demo.json (Phase 8 swaps the source for a live BeanOutputConverter typed record, AI-06).
</output>
</content>
