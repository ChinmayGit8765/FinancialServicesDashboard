---
phase: 08-live-ai-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/quantlens/ai/api/InsightEntry.java
  - backend/src/main/java/com/quantlens/ai/api/StructuredInsightRecord.java
  - backend/src/main/java/com/quantlens/ai/service/StructuredOutputService.java
  - backend/src/main/java/com/quantlens/ai/api/AiController.java
  - backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java
  - backend/src/test/java/com/quantlens/ai/StructuredOutputServiceTest.java
  - backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java
  - frontend/src/stores/ai.ts
  - frontend/src/__tests__/components/StructuredOutputChart.test.ts
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "GET /api/ai/structured returns a StructuredInsightRecord (title, subtitle, series:[{label,value}]) scoped to the authenticated user's persona"
    - "In demo mode the endpoint deserializes the seeded STRUCTURED_INSIGHT JSON for the persona into the SAME StructuredInsightRecord (no provider call) via ObjectMapper.readValue"
    - "In live mode the endpoint fills the record via BeanOutputConverter (.entity(StructuredInsightRecord.class)) through ChatClientStrategy.forSession"
    - "The AiSeedRunner seeds valid STRUCTURED_INSIGHT JSON per persona (GROWTH/INCOME/BALANCED) matching the record schema; seed_log bumped to ai-v4"
    - "The frontend StructuredOutputChart renders the same StructuredChartDto whether sourced from the demo seed path or the live LLM path; the store now fetches GET /api/ai/structured"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/ai/api/StructuredInsightRecord.java"
      provides: "Structured-output root record"
      contains: "record StructuredInsightRecord"
    - path: "backend/src/main/java/com/quantlens/ai/api/InsightEntry.java"
      provides: "Nested labeled-value record"
      contains: "record InsightEntry"
    - path: "backend/src/main/java/com/quantlens/ai/service/StructuredOutputService.java"
      provides: "Demo ObjectMapper.readValue parse + live .entity() path"
      contains: "readValue"
    - path: "backend/src/main/java/com/quantlens/ai/api/AiController.java"
      provides: "GET /api/ai/structured endpoint"
      contains: "/structured"
    - path: "backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java"
      provides: "STRUCTURED_INSIGHT seeds per persona + ai-v4"
      contains: "STRUCTURED_INSIGHT"
    - path: "frontend/src/stores/ai.ts"
      provides: "fetchStructured -> /api/ai/structured"
      contains: "/api/ai/structured"
  key_links:
    - from: "AiController.structured"
      to: "StructuredOutputService.getInsight"
      via: "resolvePortfolioId(authentication) -> getInsight(portfolioId)"
      pattern: "structuredOutputService\\.getInsight"
    - from: "StructuredOutputService.getInsight (demo)"
      to: "AiSeedContentRepository STRUCTURED_INSIGHT seed"
      via: "findByTypeAndSubjectId + objectMapper.readValue"
      pattern: "readValue\\("
    - from: "StructuredOutputService.getInsight (live)"
      to: "BeanOutputConverter"
      via: "strategy.forSession(...).call().entity(StructuredInsightRecord.class)"
      pattern: "\\.entity\\(StructuredInsightRecord\\.class\\)"
    - from: "frontend ai store fetchStructured"
      to: "GET /api/ai/structured"
      via: "axios.get"
      pattern: "axios\\.get<StructuredChartDto>\\('/api/ai/structured'\\)"
---

<objective>
Deliver the structured-output slice (AI-06): a typed `StructuredInsightRecord` (+ nested `InsightEntry`), a `StructuredOutputService` that returns the SAME record in both modes (demo → `ObjectMapper.readValue` of seeded JSON; live → `BeanOutputConverter` via `.entity()`), a `GET /api/ai/structured` endpoint, per-persona `STRUCTURED_INSIGHT` seeds (ai-v4), and the frontend one-line URL swap that points `StructuredOutputChart` at the live endpoint — with the same chart DTO path proven for both modes.

Purpose: Structured output is the second remaining Spring AI building block. The Phase-6 `StructuredOutputChart.vue` currently reads a static `/ai-structured-demo.json`; this plan upgrades it to the real structured-output mechanism (demo seeded answer + live LLM-generated typed output) without changing the chart component.

Output: `StructuredInsightRecord`, `InsightEntry`, `StructuredOutputService`, the `/api/ai/structured` endpoint, ai-v4 STRUCTURED_INSIGHT seeds, `StructuredOutputServiceTest`, extended `AiControllerIntegrationTest`, the store URL swap, and a frontend live-DTO test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-ai-features/08-RESEARCH.md
@.planning/phases/08-live-ai-features/08-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

ChatService demo/live shape (analog for StructuredOutputService — read directly):
- Constructor injects `ChatClientStrategy strategy`, `LlmKeySessionHolder keyHolder`, `ObjectMapper objectMapper`.
- Demo parse uses `objectMapper.readValue(...)`; live uses `strategy.forSession(keyHolder).prompt()...call()`.

CommentaryService persona derivation (copy verbatim):
```java
Portfolio portfolio = portfolioRepository.findById(portfolioId)
        .orElseThrow(() -> new ResponseStatusException(HttpStatus.UNAUTHORIZED));
String personaKey = portfolio.getStyle().toUpperCase();  // "GROWTH"|"INCOME"|"BALANCED"
```
Error pattern (copy verbatim):
```java
} catch (ResponseStatusException rse) { throw rse; }
catch (Exception e) { throw new ResponseStatusException(HttpStatus.BAD_GATEWAY, "AI provider temporarily unavailable"); }
```

AiSeedContentRepository (in com.quantlens.ai.seed — same module): `Optional<AiSeedContent> findByTypeAndSubjectId(String type, String subjectId)`; AiSeedContent has `getContent()`.

AiController (extend — current constructor + endpoints):
```java
public AiController(PortfolioRepository portfolioRepository, ExplainPositionService explainService,
                    CommentaryService commentaryService, ChatService chatService) { ... }
private Long resolvePortfolioId(Authentication authentication) { ... }  // IDOR-safe, reuse
// @GetMapping("/commentary") commentary(Authentication) — analog for the new /structured endpoint
```

Frontend StructuredChartDto (already correct shape — no change):
```typescript
export interface StructuredChartDto { title: string; subtitle?: string; series: { label: string; value: number }[] }
```
ai store fetchStructured currently does `axios.get<StructuredChartDto>('/ai-structured-demo.json')` (lines 145-156) — swap the URL only.

AiSeedRunner constants: `AI_SEED_VERSION="ai-v3"` (line 65), `EXPLAIN/COMMENTARY/RAG_QA` constants (lines 66-68); upsert loop at lines 96-109; `AiSeedContent(type, subjectId, content)` constructor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StructuredInsightRecord + InsightEntry + StructuredOutputService (demo parse + live .entity)</name>
  <files>backend/src/main/java/com/quantlens/ai/api/InsightEntry.java, backend/src/main/java/com/quantlens/ai/api/StructuredInsightRecord.java, backend/src/main/java/com/quantlens/ai/service/StructuredOutputService.java, backend/src/test/java/com/quantlens/ai/StructuredOutputServiceTest.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/ai/service/ChatService.java (demo ObjectMapper.readValue pattern + live call + 502 catch — exact analog)
    - backend/src/main/java/com/quantlens/ai/service/CommentaryService.java (persona key derivation lines 87-89; error catch lines 103-109)
    - backend/src/main/java/com/quantlens/ai/api/ExplainResponseDto.java + CitationDto.java (record DTO style)
    - backend/src/main/java/com/quantlens/ai/seed/AiSeedContentRepository.java (findByTypeAndSubjectId)
    - 08-RESEARCH.md Pattern 2 (lines 300-345: .entity()/BeanOutputConverter, format-instruction injection) + the "Assumed: DemoModeAdvisor structured output path" block (lines 659-686) + Pitfall 2 (demo seed must be valid JSON; parse explicitly in demo) + Pitfall 5 (wrap List under a named field)
    - 08-PATTERNS.md "StructuredInsightRecord" (174-200), "InsightEntry" (203-220), "StructuredOutputService" (224-313)
  </read_first>
  <behavior>
    - demoPath_seedJsonDeserialized_toRecord: with hasKey()=false, getInsight(portfolioId) reads the STRUCTURED_INSIGHT seed for the persona and returns a StructuredInsightRecord with non-blank title and non-empty series — no strategy.forSession call
    - livePath_entityReturnsRecord_shapeValid: with hasKey()=true, getInsight routes through strategy.forSession(...).call().entity(StructuredInsightRecord.class) and returns that record; all series values are finite numbers (e.g. 0..100)
    - unknownPortfolio_throws401: findById empty → ResponseStatusException UNAUTHORIZED
    - providerError_wrappedAs502: a thrown provider exception on the live path surfaces as 502 BAD_GATEWAY with a generic message (never echoing the cause)
  </behavior>
  <action>
    Create `InsightEntry` record (`String label, double value`) in `com.quantlens.ai.api` (08-PATTERNS lines 207-217; `double` not BigDecimal so BeanOutputConverter's JSON schema maps cleanly to a JSON number). Create `StructuredInsightRecord` record (`String title, String subtitle, java.util.List<InsightEntry> series`) in `com.quantlens.ai.api` (08-PATTERNS lines 192-196). Wrapping `series` under a named field avoids the OpenAI top-level-array restriction (08-RESEARCH Pitfall 5). No custom Jackson annotations.

    Create `StructuredOutputService` as a `@Service` in `com.quantlens.ai.service` mirroring `ChatService`: constructor-inject `ChatClientStrategy strategy`, `LlmKeySessionHolder keyHolder`, `PortfolioRepository portfolioRepository`, `AiSeedContentRepository seedRepo`, `ObjectMapper objectMapper`. `getInsight(Long portfolioId)`: resolve `portfolio` + `personaKey` via the verbatim CommentaryService pattern. Branch on `keyHolder.hasKey()` BEFORE `.entity()` (08-RESEARCH Pitfall 2): demo → `String seedContent = seedRepo.findByTypeAndSubjectId("STRUCTURED_INSIGHT", personaKey).map(AiSeedContent::getContent).orElse("{\"title\":\"Sector Exposure\",\"subtitle\":\"Demo\",\"series\":[]}")` then `return objectMapper.readValue(seedContent, StructuredInsightRecord.class)`. Live → `return strategy.forSession(keyHolder).prompt().system("Return ONLY valid JSON matching the schema. You are a portfolio analyst.").user(u -> u.text("Analyze sector exposure for portfolio: {summary}").param("summary", buildPortfolioSummary(portfolioId))).advisors(spec -> spec.param("AI_SEED_TYPE","STRUCTURED_INSIGHT").param("AI_SEED_SUBJECT", personaKey)).call().entity(StructuredInsightRecord.class)`. `buildPortfolioSummary` may be a small private helper returning a short persona/style string (no new repository wiring required beyond what is injected). Use the verbatim CommentaryService catch (ResponseStatusException rethrow + generic 502; never echo `e.getMessage()`). Use a constant `STRUCTURED_INSIGHT = "STRUCTURED_INSIGHT"`.

    Create `StructuredOutputServiceTest` (Mockito, `@ExtendWith(MockitoExtension.class)`): `@Mock ChatClientStrategy`, `@Mock LlmKeySessionHolder`, `@Mock PortfolioRepository`, `@Mock AiSeedContentRepository`, real `ObjectMapper`. Stub `portfolioRepository.findById` → a Portfolio with `getStyle()="Growth"`. Demo test stubs `seedRepo.findByTypeAndSubjectId` to return an AiSeedContent whose content is valid `StructuredInsightRecord` JSON. Live test mocks the ChatClient fluent chain (08-PATTERNS lines 530-541: `prompt()->requestSpec`, `system/user/advisors->requestSpec`, `call()->callSpec`, `callSpec.entity(StructuredInsightRecord.class)->returnVal`). Add the unknownPortfolio and providerError tests.
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot&& .\mvnw.cmd -q -o test -Dtest=StructuredOutputServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - StructuredOutputServiceTest GREEN: demo seed→record (no forSession call), live .entity()→record, 401 on unknown portfolio, 502 on provider error.
    - Demo branch parses with `objectMapper.readValue` BEFORE any `.entity()` call (grep: `readValue` present; demo path never calls `strategy.forSession`).
    - `series` is a named field under the root record (no top-level `List` .entity()).
    - 502 catch contains no `e.getMessage()`/`e.toString()` reference.
  </acceptance_criteria>
  <done>The typed records and service compile; both demo and live paths return the same StructuredInsightRecord; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/ai/structured endpoint + ai-v4 STRUCTURED_INSIGHT seeds + integration coverage</name>
  <files>backend/src/main/java/com/quantlens/ai/api/AiController.java, backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java, backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java</files>
  <read_first>
    - backend/src/main/java/com/quantlens/ai/api/AiController.java (commentary endpoint lines 111-116; constructor lines 68-76; resolvePortfolioId lines 161-168)
    - backend/src/main/java/com/quantlens/ai/seed/AiSeedRunner.java (AI_SEED_VERSION line 65; constants 66-68; buildFixtures + upsert loop 96-109)
    - backend/src/test/java/com/quantlens/ai/AiControllerIntegrationTest.java (existing structure for the new /structured integration assertion)
    - 08-PATTERNS.md "AiController extend" (317-344) + "AiSeedRunner extend" (391-437: ai-v4 bump, per-persona JSON fixtures, upsert handles insertion) + Pitfall 2 (seed JSON must match the record exactly)
  </read_first>
  <action>
    Extend `AiController`: add a `StructuredOutputService structuredOutputService` constructor parameter + final field. Add `@GetMapping("/structured") @Transactional(readOnly=true) public ResponseEntity<StructuredInsightRecord> structured(Authentication authentication)` that calls `resolvePortfolioId(authentication)` then `ResponseEntity.ok(structuredOutputService.getInsight(portfolioId))` (08-PATTERNS lines 333-338). Reuse the existing `resolvePortfolioId` (IDOR-safe) — do not duplicate.

    Extend `AiSeedRunner`: bump `AI_SEED_VERSION` to `"ai-v4"` (the upsert loop inserts the new rows on existing DBs and the new completion marker is written last). Add `private static final String STRUCTURED_INSIGHT = "STRUCTURED_INSIGHT"`. In `buildFixtures()` add three `new AiSeedContent(STRUCTURED_INSIGHT, "GROWTH"|"INCOME"|"BALANCED", <valid JSON>)` rows using the persona JSON from 08-PATTERNS lines 405-432. The JSON MUST be valid and match `StructuredInsightRecord` exactly (`title`, `subtitle`, `series:[{label,value}]`) — any deviation throws JsonMappingException in the demo path (08-RESEARCH Pitfall 2). No change to the upsert loop logic.

    Extend `AiControllerIntegrationTest`: add a test `structured_demoMode_returnsRecordShape` that logs in as alice, GETs `/api/ai/structured`, asserts 200 and a body containing `"title"` and a non-empty `series` (parse to a StructuredInsightRecord or assert on the JSON). Follow the existing commentary/explain integration assertions in the same file. (The KeyLeakage + no-network proofs for this endpoint live in 08-03.)
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot&& .\mvnw.cmd -q -o test -Dtest=AiControllerIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/ai/structured returns 200 in demo with a StructuredInsightRecord shape (title + non-empty series) for alice (GROWTH).
    - AiSeedRunner seeds three STRUCTURED_INSIGHT rows (GROWTH/INCOME/BALANCED) with valid record-matching JSON; AI_SEED_VERSION is ai-v4.
    - `resolvePortfolioId` reused (no duplicate principal-resolution logic added).
  </acceptance_criteria>
  <done>The endpoint is live, seeds are in place at ai-v4, and the demo integration assertion is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend URL swap to /api/ai/structured + live-DTO chart test</name>
  <files>frontend/src/stores/ai.ts, frontend/src/__tests__/components/StructuredOutputChart.test.ts</files>
  <read_first>
    - frontend/src/stores/ai.ts (fetchStructured lines 145-156 — swap the URL + error message only; all async-state code unchanged)
    - frontend/src/api/ai.ts (StructuredChartDto already matches the record — no type change)
    - frontend/src/__tests__/components/StructuredOutputChart.test.ts (existing 7 tests must keep passing; add the live-DTO test)
    - 08-PATTERNS.md "frontend/src/stores/ai.ts modify" (629-660) + "StructuredOutputChart.test.ts extend" (663-695)
  </read_first>
  <action>
    In `frontend/src/stores/ai.ts`, change the `fetchStructured` action: replace `axios.get<StructuredChartDto>('/ai-structured-demo.json')` with `axios.get<StructuredChartDto>('/api/ai/structured')` (08-PATTERNS line 651). Update the doc comment to reflect that this now hits the live backend (demo seeded JSON via the same endpoint, live LLM-generated record when a key is set) and update the error string to `'Failed to load structured output'` (08-PATTERNS line 658). Leave the `asyncState<StructuredChartDto>()` declaration, loading/error handling, and `$reset()` untouched — `StructuredChartDto` already matches `StructuredInsightRecord`.

    In `StructuredOutputChart.test.ts`, keep all existing tests; add `renders live DTO shape from /api/ai/structured (title + series)` per 08-PATTERNS lines 677-692: mount `StructuredOutputChart` with a `StructuredChartDto` prop (`title`, `subtitle`, `series:[{label,value}]`) and assert the title text renders and the `VChart` component exists. The component itself requires NO changes — it already accepts `StructuredChartDto | null`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/__tests__/components/StructuredOutputChart.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fetchStructured` calls `/api/ai/structured` (grep the store) and no longer references `/ai-structured-demo.json`.
    - All existing StructuredOutputChart tests pass plus the new live-DTO test.
    - `npm run build` (vue-tsc gate) stays green: `cd frontend && npm run build`.
  </acceptance_criteria>
  <done>The chart is sourced from the live endpoint with the same DTO; existing + new frontend tests GREEN and the build gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/ai/structured | Authenticated browser request; portfolio identity must derive only from the principal |
| StructuredOutputService → ChatModel (live) | LLM produces JSON deserialized via BeanOutputConverter |
| Seed JSON → StructuredInsightRecord (demo) | Authored DB content deserialized via ObjectMapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-IDOR-ST | Information Disclosure | GET /api/ai/structured | mitigate | Portfolio resolved ONLY via `resolvePortfolioId(authentication)` (verbatim IDOR pattern); no portfolioId from request |
| T-08-LEAK-ST | Information Disclosure | StructuredOutputService live catch | mitigate | Provider errors wrapped as generic 502 BAD_GATEWAY; never echo `e.getMessage()` (could carry the LLM key). KeyLeakage gate covers /api/ai/structured in 08-03 |
| T-08-JSON | Tampering | BeanOutputConverter / ObjectMapper deserialization | mitigate | Typed record fields (String/double/List); Jackson handles escaping; demo seed JSON is authored + schema-validated by the demo integration test |
| T-08-DEMO-NET-ST | Availability | demo structured path | mitigate | Demo branches on `hasKey()==false` and parses seed JSON before any `.entity()`/forSession call — no network. No-network proof in 08-03 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages (frontend echarts/vue-echarts already installed; no backend deps). 08-RESEARCH Package Legitimacy Audit confirms zero installs |
</threat_model>

<verification>
- `StructuredOutputServiceTest` GREEN (demo seed→record, live .entity()→record, 401, 502).
- `AiControllerIntegrationTest` GREEN (GET /api/ai/structured demo shape).
- `StructuredOutputChart.test.ts` GREEN (existing 7 + live-DTO test); `npm run build` green.
- Full backend suite stays green: `cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot&& .\mvnw.cmd -q -o test"`.
</verification>

<success_criteria>
- AI-06 delivered: at least one AI response is a typed Java record (StructuredInsightRecord) via structured output that drives the Vue ECharts chart, with the SAME DTO path in demo (seeded JSON) and live (BeanOutputConverter).
- `GET /api/ai/structured` is principal-scoped and never leaks the key.
- The frontend renders from the live endpoint with no chart-component changes; build + tests green.
</success_criteria>

<output>
Create `.planning/phases/08-live-ai-features/08-02-SUMMARY.md` when done. Record any deviation in the BeanOutputConverter `.entity()` call site (e.g. native structured output enablement) and confirm the seed JSON matches the record schema.
</output>
