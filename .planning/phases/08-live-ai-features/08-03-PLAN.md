---
phase: 08-live-ai-features
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java
  - backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java
autonomous: false
requirements: [AI-05, AI-06]
must_haves:
  truths:
    - "GET /api/ai/structured never returns the LLM key in its response body or any log line (KeyLeakage gate extended)"
    - "Any configured FINNHUB_API_KEY never appears in a response body or log line (Finnhub key-leak threat documented and asserted)"
    - "In demo mode GET /api/ai/structured returns seeded content with the counting advisor at zero — provably no provider/network call"
    - "The full backend suite and the frontend build/test gates are green for the whole phase"
  artifacts:
    - path: "backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java"
      provides: "Key-leak assertions for /api/ai/structured + Finnhub key threat"
      contains: "/api/ai/structured"
    - path: "backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java"
      provides: "No-network proof for the structured demo path"
      contains: "demoMode_structured"
  key_links:
    - from: "KeyLeakageIntegrationTest"
      to: "GET /api/ai/structured"
      via: "authenticatedGet + doesNotContain(testKey)"
      pattern: "/api/ai/structured"
    - from: "AiDemoModeIntegrationTest.demoMode_structured"
      to: "NoNetworkProofConfig.NEXT_CALL_COUNT"
      via: "counting advisor stays zero in demo"
      pattern: "NEXT_CALL_COUNT"
---

<objective>
Close the phase security and offline-proof gate: extend `KeyLeakageIntegrationTest` so the new `GET /api/ai/structured` endpoint never leaks the LLM key (and document/assert the Finnhub key-leak threat), and extend `AiDemoModeIntegrationTest` with an executable no-network proof for the structured demo path. Then confirm the full backend suite and the frontend build/test gates are green for the whole phase, and pause for a human-verify checkpoint covering the live tool-call and structured-chart UAT items that require real keys.

Purpose: The key-leak gate is mandatory and must cover every new endpoint plus the new Finnhub credential. The demo paths (seeded quote + seeded structured JSON) must be provably offline before any live key path is trusted.

Output: extended `KeyLeakageIntegrationTest`, extended `AiDemoModeIntegrationTest`, a green full-suite run, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-ai-features/08-RESEARCH.md
@.planning/phases/08-live-ai-features/08-PATTERNS.md
@.planning/phases/08-live-ai-features/08-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

KeyLeakageIntegrationTest (extend — current shape):
- Generates `testKey = "TEST-SENTINEL-KEY-" + UUID`, logs in as alice, POSTs /api/ai/key (anthropic), then asserts `doesNotContain(testKey)` on /status, /explain/AAPL, /commentary, /chat, AND on every captured log line.
- Helpers present: `loginAndGetSessionCookie(username)`, `authenticatedGet(path, cookie)`. A ListAppender is attached to the root logger in @BeforeEach.
- New assertion goes after step 5 (chat): GET /api/ai/structured must be in {200, 502} and `doesNotContain(testKey)`.

AiDemoModeIntegrationTest (extend — current shape):
- `@Import(NoNetworkProofConfig.class)`; `@BeforeEach resetCounter()` sets `NEXT_CALL_COUNT.set(0)`.
- Existing demo tests assert 200 + seeded non-blank content + `NEXT_CALL_COUNT.get() == 0`.
- Helpers present: `loginAndGetSessionCookie`, `authenticatedGet`. CountingCallAdvisor sits at HIGHEST_PRECEDENCE+1 (just after DemoModeAdvisor).

Test env: no `FINNHUB_API_KEY` set → `@Value("${FINNHUB_API_KEY:}")` resolves blank → the tool/quote demo path runs (seeded), Finnhub is never called; the Finnhub leak assertion is vacuously safe but documents the threat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend KeyLeakageIntegrationTest for /api/ai/structured + Finnhub key, and AiDemoModeIntegrationTest no-network proof</name>
  <files>backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java, backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java</files>
  <read_first>
    - backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java (steps 1-6; helpers; ListAppender)
    - backend/src/test/java/com/quantlens/ai/AiDemoModeIntegrationTest.java (demoMode_commentary test lines 80-96; NoNetworkProofConfig lines 139-167)
    - 08-PATTERNS.md "KeyLeakageIntegrationTest extend" (578-596) + "AiDemoModeIntegrationTest extend" (600-625)
    - 08-RESEARCH.md Pitfall 3 (Finnhub key leak via exception URL) + Security Domain (lines 809-829)
  </read_first>
  <action>
    Extend `KeyLeakageIntegrationTest.apiKeyNeverAppearsInResponseOrLogs`: after the existing step 5 (chat) and before step 6 (log assertion), add a GET to `/api/ai/structured` via `authenticatedGet("/api/ai/structured", sessionCookie)` (08-PATTERNS lines 586-593). Assert the status is in `{HttpStatus.OK, HttpStatus.BAD_GATEWAY}` (200 demo seed shape, or 502 if the fake live key reaches the provider) and the body `doesNotContain(testKey)`. Add a comment documenting the Finnhub key-leak threat (T-08-LEAK-FH): the test env has no `FINNHUB_API_KEY`, so the seeded fallback runs and Finnhub is never called — the assertion is vacuously safe for the Finnhub key but the threat is documented (08-PATTERNS line 596). The existing step-6 log assertion already covers all log lines, so it now also guards the structured endpoint.

    Extend `AiDemoModeIntegrationTest`: add `demoMode_structured_returnsSeededContent_withZeroNetworkCalls` (08-PATTERNS lines 606-622): log in as alice, GET `/api/ai/structured`, assert 200, body contains `"title"` and does NOT contain `"\"title\":\"\""` (non-blank), and `NoNetworkProofConfig.NEXT_CALL_COUNT.get()` is zero — the executable proof that DemoModeAdvisor short-circuited before any provider call. The existing `@BeforeEach resetCounter()` and `@Import(NoNetworkProofConfig.class)` cover this test with no new configuration.
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot&& .\mvnw.cmd -q -o test -Dtest=KeyLeakageIntegrationTest,AiDemoModeIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - KeyLeakageIntegrationTest GREEN with the new /api/ai/structured assertion; testKey absent from the structured response and all log lines.
    - AiDemoModeIntegrationTest GREEN with `demoMode_structured_returnsSeededContent_withZeroNetworkCalls`: 200, non-blank title, counting advisor == 0 (no-network proof).
    - Finnhub key-leak threat documented in KeyLeakageIntegrationTest comment.
  </acceptance_criteria>
  <done>The key-leak gate covers /api/ai/structured and the Finnhub key, and the structured demo path is provably offline.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + frontend green gate</name>
  <files>backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java</files>
  <read_first>
    - .planning/phases/08-live-ai-features/08-VALIDATION.md (Sampling Rate + Phase gate: full backend + frontend green; KeyLeakage incl. Finnhub; no-network proof)
  </read_first>
  <action>
    Run the full backend test suite and the frontend build + test gates to confirm the whole phase is green (no regressions from 08-01/08-02/08-03). If any failure surfaces, fix it within the owning file and re-run. Do not introduce new behavior here — this is the phase-gate verification step. Record the final green state (test counts) in the SUMMARY. (The single file listed in `files_modified` is a placeholder for any green-gate touch-up; prefer fixing in the originating plan's file if a regression is found there.)
  </action>
  <verify>
    <automated>cd backend && cmd /c "set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot&& .\mvnw.cmd -q -o test" && cd ../frontend && npm run test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Full backend suite GREEN (includes StockQuoteToolServiceTest, MultiProviderRoutingTest, StructuredOutputServiceTest, AiControllerIntegrationTest, KeyLeakageIntegrationTest, AiDemoModeIntegrationTest, QuantLensModulithTest).
    - Frontend `npm run test` GREEN (StructuredOutputChart live-DTO test included) and `npm run build` (vue-tsc gate) passes.
    - No real network used by any test (mocked Finnhub + provider; demo seed paths).
  </acceptance_criteria>
  <done>Backend + frontend gates are green for the whole phase with no network dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — live tool call, structured chart, multi-provider switch</name>
  <files>(none — manual verification of the running stack)</files>
  <action>
    Pause for the user to verify the live UAT items that require real keys and a running stack. Present the what-built summary and the how-to-verify steps below, then wait for the resume signal. Do not auto-advance — this checkpoint is blocking.
  </action>
  <what-built>
    Live AI features for Phase 8: a Finnhub `@Tool` for live quotes (demo = seeded last close, live = real Finnhub with 15-min TTL cache + after-hours labeling), structured output (`GET /api/ai/structured`) driving the `StructuredOutputChart` via the same typed DTO in demo and live, and multi-provider routing through `ChatClientStrategy.forSession()`. All automated tests are green and the demo paths are provably offline.
  </what-built>
  <how-to-verify>
    These three checks require real keys and a running stack (per 08-VALIDATION Manual-Only Verifications):
    1. `docker compose up` from a clean checkout. Log in as alice (demo1234). Confirm the structured-output chart panel renders the seeded DTO with zero keys (demo mode).
    2. Open the BYO-key popup, enter a real Anthropic key, then ask in chat "what's AAPL trading at?" — confirm the LLM invokes the quote tool and the answer includes a price + an as-of timestamp (with a Finnhub key set, it is the live Finnhub price; without one, the seeded last close). Confirm no key or token appears anywhere in the UI or server logs.
    3. Switch the BYO-key provider to OpenAI (enter a real OpenAI key) and repeat a structured-output request — confirm a successful live AI call from the same entry point (success criterion 3), and that the structured chart still renders the same DTO shape.
  </how-to-verify>
  <verify>
    <human-check>User confirms demo chart renders zero-key, live quote tool is invoked with no key leak, and the OpenAI provider switch yields a successful live call rendering the same DTO.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issue (quote not invoked, chart not rendering, key visible, provider switch failed).</resume-signal>
  <done>User has approved the live tool-call, structured-chart, and multi-provider-switch behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/ai/structured | Authenticated request; response and logs must never carry the LLM key |
| FinnhubQuoteClient → finnhub.io | Outbound request carrying the Finnhub credential — must never leak via logs |
| demo path → DB seed | Demo must read only the DB and never reach a provider (counting-advisor proof) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-LEAK-ST | Information Disclosure | /api/ai/structured response + logs | mitigate | KeyLeakageIntegrationTest asserts the LLM key is absent from the structured response and every log line |
| T-08-LEAK-FH | Information Disclosure | Finnhub key in exception/log | mitigate | Threat documented + asserted; FinnhubQuoteClient (08-01) logs only the ticker; no FINNHUB_API_KEY in test env → seeded fallback, Finnhub never called |
| T-08-DEMO-NET | Availability / Information Disclosure | demo structured + tool paths | mitigate | CountingCallAdvisor==0 proves DemoModeAdvisor short-circuits before any provider/network call |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages in the phase (08-RESEARCH Package Legitimacy Audit: zero installs); legitimacy gate not required |
</threat_model>

<verification>
- `KeyLeakageIntegrationTest` GREEN (now covering /api/ai/structured + documented Finnhub threat).
- `AiDemoModeIntegrationTest` GREEN (structured no-network proof).
- Full backend suite GREEN; frontend `npm run test` + `npm run build` GREEN.
- Human-verify checkpoint approved for the live tool-call, structured-chart, and provider-switch UAT items.
</verification>

<success_criteria>
- The key-leak gate (mandatory) covers every new endpoint and the Finnhub credential.
- Demo paths (quote tool + structured output) are provably offline.
- The whole phase is green across backend + frontend with no network dependency.
- Live UAT (tool invocation, structured chart, multi-provider switch) confirmed by the user.
</success_criteria>

<output>
Create `.planning/phases/08-live-ai-features/08-03-SUMMARY.md` when done. Record final test counts (backend + frontend) and the human-verify outcome.
</output>
