---
phase: 09-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.yml
  - backend/src/main/java/com/quantlens/security/config/SecurityConfig.java
  - backend/src/main/java/com/quantlens/portfolio/service/package-info.java
  - backend/src/main/java/com/quantlens/portfolio/api/package-info.java
  - backend/src/main/java/com/quantlens/analytics/service/package-info.java
  - backend/src/main/java/com/quantlens/analytics/api/package-info.java
  - backend/src/main/java/com/quantlens/mcp/package-info.java
  - backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java
  - backend/src/main/java/com/quantlens/mcp/tools/PortfolioSummaryResult.java
  - backend/src/main/java/com/quantlens/mcp/tools/RiskMetricsResult.java
  - backend/src/main/java/com/quantlens/mcp/tools/PositionDetailResult.java
  - backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java
  - backend/src/test/java/com/quantlens/mcp/McpAuthIntegrationTest.java
  - .mcp.json
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "The backend compiles and boots with spring-ai-starter-mcp-server-webmvc on the classpath; an HTTP endpoint is registered at /mcp on port 8080"
    - "ApplicationModules.verify() passes with the new com.quantlens.mcp module declared (named interfaces it depends on actually exist)"
    - "POST /mcp without credentials returns 401 (auth gate live before any tool is reachable)"
    - "PortfolioMcpTools exposes three @McpTool beans (get_portfolio_summary, get_risk_metrics, get_position_detail) that delegate to PortfolioService + RiskCalculator — no recompute"
    - ".mcp.json contains a quantlens product entry pointing at http://localhost:8080/mcp with Basic auth"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java"
      provides: "Three @McpTool methods delegating to portfolio + analytics services with SecurityContextHolder principal resolution and per-tool try/catch error hygiene"
      contains: "@McpTool"
    - path: "backend/src/main/java/com/quantlens/mcp/package-info.java"
      provides: "Modulith module boundary for mcp (allowedDependencies on the exposed service + domain + api named interfaces)"
      contains: "ApplicationModule"
    - path: "backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java"
      provides: "RED tool-correctness + error-hygiene scaffold (golden values, mock SecurityContext for alice)"
      contains: "GOLDEN_SHARPE"
    - path: "backend/src/test/java/com/quantlens/mcp/McpAuthIntegrationTest.java"
      provides: "RED/GREEN HTTP-level 401 gate for POST /mcp"
      contains: "mcp_withoutAuth_returns401"
    - path: ".mcp.json"
      provides: "quantlens product MCP server entry"
      contains: "http://localhost:8080/mcp"
  key_links:
    - from: "backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java"
      to: "com.quantlens.portfolio.service.PortfolioService"
      via: "constructor injection + delegation"
      pattern: "PortfolioService"
    - from: "backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java"
      to: "com.quantlens.analytics.service.RiskCalculator"
      via: "constructor injection + delegation"
      pattern: "RiskCalculator"
    - from: "backend/src/main/java/com/quantlens/security/config/SecurityConfig.java"
      to: "/mcp"
      via: "CSRF ignoringRequestMatchers + httpBasic"
      pattern: "/mcp"
---

<objective>
Build the thinnest end-to-end MCP server slice for QuantLens: add the Spring AI WebMVC MCP-server starter, configure the Streamable-HTTP server at `/mcp`, protect `/mcp` with HTTP Basic + CSRF exemption, create the `com.quantlens.mcp` Modulith module with three `@McpTool` beans that delegate to the already-golden-tested `PortfolioService` and `RiskCalculator` (NO recompute), commit the `quantlens` `.mcp.json` entry, and lay down RED test scaffolds. This plan also RESOLVES the phase's open architectural questions at compile time and records the resolution in the SUMMARY.

Purpose: Stand up a genuinely useful product MCP server (the freshest 2026 resume signal) as a thin protocol adapter over existing analytics. Covers MCP-01 (the @McpTool server) and the `.mcp.json` half of MCP-02.

Output: A compiling, booting backend with `/mcp` registered and auth-gated; three tool beans; result records; `.mcp.json` quantlens entry; RED scaffolds for tool correctness, the 401 gate (which passes immediately), and error hygiene.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Backend build commands (Windows host — JDK 21 REQUIRED before any Maven invocation):
- PowerShell: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"` then `cd backend; .\mvnw.cmd ...`
- The host default `java` may be JDK 11; ALWAYS set JAVA_HOME first.
- Do NOT pass `-o`/offline — the starter must download from Maven Central on first build.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcp-server/09-RESEARCH.md
@.planning/phases/09-mcp-server/09-PATTERNS.md
@.planning/phases/09-mcp-server/09-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor delegates to. Extracted from the codebase. No exploration needed. -->

DELEGATE TARGET — PortfolioService (com.quantlens.portfolio.service.PortfolioService, @Service @Transactional(readOnly=true)):
- `List<HoldingDto> getHoldings(Long portfolioId)`
- `List<AllocationSliceDto> getAllocation(Long portfolioId)`
- `PortfolioPnlDto getPortfolioPnl(Long portfolioId)`

DELEGATE TARGET — RiskCalculator (com.quantlens.analytics.service.RiskCalculator, @Service @Transactional(readOnly=true)):
- `RiskScorecardDto computeRiskScorecard(Long portfolioId)`

PRINCIPAL RESOLUTION — PortfolioRepository (com.quantlens.portfolio.domain.PortfolioRepository):
- `Optional<Long> findPortfolioIdByUsername(String username)`  // single JPQL correlated subquery; empty => 401

DTO contracts the tools READ FROM (do NOT return these directly — map to *Result records):
- `record HoldingDto(String ticker, String name, String sector, BigDecimal quantity, BigDecimal avgCostBasis, BigDecimal currentPrice, BigDecimal currentMarketValue, BigDecimal portfolioWeight, BigDecimal unrealizedPnlAbs, BigDecimal unrealizedPnlPct)`  (com.quantlens.portfolio.api)
- `record AllocationSliceDto(String label, BigDecimal weight, BigDecimal marketValue)`  (com.quantlens.portfolio.api)
- `record PortfolioPnlDto(BigDecimal totalMarketValue, BigDecimal totalCostBasis, BigDecimal totalUnrealizedGainAbs, BigDecimal totalUnrealizedGainPct, BigDecimal dailyChangeAbs, BigDecimal dailyChangePct, List<DateValueDto> equityCurve)`  (com.quantlens.portfolio.api)
- `record RiskScorecardDto(double sharpeRatio, double annualizedVolatility, double maxDrawdown, double beta, List<VarResultDto> var)`  (com.quantlens.analytics.api)
- `record VarResultDto(String method, double confidence, int horizonDays, BigDecimal amount, double percentage)`  (com.quantlens.analytics.api)  // method ∈ {"HISTORICAL","PARAMETRIC","CVaR_HISTORICAL"}

PRINCIPAL PATTERN (copy from AnalyticsController.java:141-148, adapt to pull from SecurityContextHolder — no Authentication parameter in @McpTool methods):
  Authentication auth = SecurityContextHolder.getContext().getAuthentication();
  if (auth == null || !auth.isAuthenticated()) throw <unauthorized>;
  return portfolioRepository.findPortfolioIdByUsername(auth.getName()).orElseThrow(<unauthorized>);

EXISTING SecurityConfig.filterChain (com.quantlens.security.config.SecurityConfig) — current CSRF block ends with:
  .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
  .ignoringRequestMatchers("/api/auth/login", "/api/auth/logout")
  .ignoringRequestMatchers(
      AntPathRequestMatcher.antMatcher(HttpMethod.POST, "/api/ai/key"),
      AntPathRequestMatcher.antMatcher(HttpMethod.POST, "/api/ai/chat"))
  // there is NO .httpBasic(...) today — formLogin only.
  // .authorizeHttpRequests already ends with .anyRequest().authenticated() — /mcp is protected by default.

EXISTING Modulith named interfaces (CRITICAL — verify before declaring mcp deps):
- portfolio.domain → `@NamedInterface("domain")` EXISTS (entities + PortfolioRepository, AppUserRepository).
- portfolio.service → NO named interface today (module-internal). PortfolioService lives here.
- analytics.service → NO named interface today. RiskCalculator lives here.
- portfolio.api / analytics.api (the DTO records) → NO named interface today.
- analytics/package-info.java today: allowedDependencies = {"marketdata::domain","portfolio::domain"}.
- The ai module (analog) reaches across ONLY via portfolio::domain — it never touches PortfolioService/RiskCalculator. The mcp module is the FIRST consumer of those service beans across a module boundary, so the service + api packages MUST be newly exposed as named interfaces for ApplicationModules.verify() to pass.

GOLDEN VALUES (seed=42, alice Growth Portfolio — from RiskCalculatorTest.java, reuse verbatim):
- GOLDEN_SHARPE = 0.36442669 (±0.001)
- GOLDEN_ANNUAL_VOL = 0.34621361 (±0.0001)
- GOLDEN_MAX_DRAWDOWN = -0.33891522 (±0.0001)
- GOLDEN_BETA = 1.94917921 (±0.001)
- GOLDEN_HIST_VAR_AMOUNT = 1464.52 (±0.01)  // VarResultDto where method=="HISTORICAL", amount field

TEST BASE — AbstractPostgresIntegrationTest (com.quantlens.AbstractPostgresIntegrationTest): provides @SpringBootTest(webEnvironment=RANDOM_PORT) + @ActiveProfiles("test") + static pgvector:pg16 container. Extend it; do NOT re-annotate @SpringBootTest.
Seed users: alice/bob/charlie, password demo1234. AppUserRepository.findByUsername(String) → Optional<AppUser>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP starter + application.yml config + expose service/api named interfaces + mcp Modulith module</name>
  <files>backend/pom.xml, backend/src/main/resources/application.yml, backend/src/main/java/com/quantlens/portfolio/service/package-info.java, backend/src/main/java/com/quantlens/portfolio/api/package-info.java, backend/src/main/java/com/quantlens/analytics/service/package-info.java, backend/src/main/java/com/quantlens/analytics/api/package-info.java, backend/src/main/java/com/quantlens/mcp/package-info.java</files>
  <read_first>
    - backend/pom.xml (dependencyManagement already imports spring-ai-bom 1.1.6; add the starter under <dependencies> with NO <version>)
    - backend/src/main/resources/application.yml (merge MCP keys under the EXISTING `spring.ai` key — do not create a second top-level `spring`)
    - PATTERN (Maven): 09-PATTERNS.md "Maven Dependency" section
    - PATTERN (yml): 09-PATTERNS.md "application.yml (modify)" + 09-RESEARCH.md "Complete application.yml additions"
    - PATTERN (Modulith analog): backend/src/main/java/com/quantlens/analytics/package-info.java and backend/src/main/java/com/quantlens/portfolio/domain/package-info.java (the `@NamedInterface("domain")` shape)
    - RESEARCH blocks: 09-RESEARCH.md §Standard Stack (artifact id), §"Modulith module declaration", Open Questions 1 & 2
  </read_first>
  <action>
    Add the dependency `org.springframework.ai:spring-ai-starter-mcp-server-webmvc` (NO version element — BOM 1.1.6 pins it) to backend/pom.xml inside the existing `<dependencies>` block, with a comment marking it Phase 9 and noting it is the same official `org.springframework.ai` group as the existing starters (Package Legitimacy Audit: Approved).

    Merge an MCP server config block into application.yml under the existing `spring.ai` key: set `mcp.server.name: quantlens-mcp`, `version: 1.0.0`, `protocol: STREAMABLE`, `type: SYNC`, `annotation-scanner.enabled: true` (explicit per Open Question 2), and `streamable-http.mcp-endpoint: /mcp` (explicit for documentation). Do NOT add a second top-level `spring:` block.

    Expose the cross-module dependencies the mcp module needs as Spring Modulith named interfaces (these do NOT exist yet — the mcp module is the first cross-boundary consumer of the service beans). Create four `package-info.java` files, each annotating its package with `@org.springframework.modulith.NamedInterface("<name>")`:
      - `com.quantlens.portfolio.service` → named interface `"service"` (exposes PortfolioService)
      - `com.quantlens.portfolio.api` → named interface `"api"` (exposes HoldingDto, AllocationSliceDto, PortfolioPnlDto, DateValueDto)
      - `com.quantlens.analytics.service` → named interface `"service"` (exposes RiskCalculator)
      - `com.quantlens.analytics.api` → named interface `"api"` (exposes RiskScorecardDto, VarResultDto)
    Match the exact one-line annotation style of `com/quantlens/portfolio/domain/package-info.java`.

    Create `com.quantlens.mcp.package-info.java` declaring `@org.springframework.modulith.ApplicationModule(displayName = "MCP", allowedDependencies = {"portfolio::service", "portfolio::api", "portfolio::domain", "analytics::service", "analytics::api"})`. Rationale comments: `portfolio::service` for PortfolioService delegation; `portfolio::api` for the DTOs it reads; `portfolio::domain` for PortfolioRepository.findPortfolioIdByUsername; `analytics::service`/`analytics::api` for RiskCalculator + its DTOs.

    OPEN-QUESTION RESOLUTION (record exact findings in 09-01-SUMMARY): Spring Modulith named-interface references take the form `module::interfaceName`. If `ApplicationModules.verify()` (run in Task 3 / QuantLensModulithTest) rejects any of these references — e.g. the analytics module has no `api` sub-package boundary, or the existing `portfolio.domain` NamedInterface conflicts — adjust the named-interface names to the actual exposed packages and re-verify. Do NOT relax the boundary by deleting `allowedDependencies` or adding a blanket open dependency; the module must declare exactly the interfaces it uses. Capture the final working `allowedDependencies` list and the named-interface declarations verbatim in the SUMMARY under an "Open Questions Resolved" heading.
  </action>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q dependency:tree | grep -q "spring-ai-starter-mcp-server-webmvc" && echo "STARTER_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw dependency:tree` lists `spring-ai-starter-mcp-server-webmvc` (and transitively an MCP SDK jar — note the SDK groupId/artifactId in the SUMMARY for Task 2's CallToolResult resolution).
    - application.yml has `spring.ai.mcp.server.streamable-http.mcp-endpoint: /mcp` and `annotation-scanner.enabled: true`, merged under the single existing `spring.ai` key (no duplicate `spring:` root — `grep -c "^spring:" application.yml` returns 1).
    - Five new `package-info.java` files exist: four `@NamedInterface(...)` exposures + one `mcp` `@ApplicationModule`.
    - The project compiles: `./mvnw -q -DskipTests compile` succeeds.
  </acceptance_criteria>
  <done>Starter on classpath, MCP yml config present, service/api packages exposed as named interfaces, mcp ApplicationModule declared, project compiles. SDK jar coordinates for CallToolResult recorded for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SecurityConfig edits + result records + PortfolioMcpTools (3 @McpTool beans, principal + error hygiene) + RED test scaffolds + .mcp.json</name>
  <files>backend/src/main/java/com/quantlens/security/config/SecurityConfig.java, backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java, backend/src/main/java/com/quantlens/mcp/tools/PortfolioSummaryResult.java, backend/src/main/java/com/quantlens/mcp/tools/RiskMetricsResult.java, backend/src/main/java/com/quantlens/mcp/tools/PositionDetailResult.java, backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java, backend/src/test/java/com/quantlens/mcp/McpAuthIntegrationTest.java, .mcp.json</files>
  <read_first>
    - backend/src/main/java/com/quantlens/security/config/SecurityConfig.java (lines 111-141 — the .csrf(...) block and where .httpBasic must be inserted before .exceptionHandling)
    - backend/src/main/java/com/quantlens/analytics/api/AnalyticsController.java (lines 141-148 — resolvePortfolioId; lines 76-82 — delegate→return shape)
    - backend/src/main/java/com/quantlens/ai/api/GlobalAiExceptionHandler.java (the log-internally / safe-static-message error principle)
    - backend/src/main/java/com/quantlens/portfolio/service/PortfolioService.java (getHoldings / getAllocation / getPortfolioPnl signatures + the HoldingDto/PortfolioPnlDto/AllocationSliceDto fields used)
    - backend/src/main/java/com/quantlens/analytics/service/RiskCalculator.java (computeRiskScorecard; how VarResultDto entries are labelled "HISTORICAL"/"PARAMETRIC")
    - backend/src/test/java/com/quantlens/analytics/RiskCalculatorTest.java (golden constants + alice portfolio resolution via AppUserRepository.findByUsername + PortfolioRepository.findByUserId)
    - backend/src/test/java/com/quantlens/analytics/AnalyticsControllerIntegrationTest.java (lines 54-64 — the unauthenticated-401 TestRestTemplate pattern)
    - .mcp.json (existing context7 + project-db entries to extend)
    - PATTERN: 09-PATTERNS.md §PortfolioMcpTools (imports, principal pull, delegation, error hygiene), §Result Record Files, §SecurityConfig.java (two edits), §PortfolioMcpToolsTest, §McpAuthIntegrationTest, §.mcp.json
    - RESEARCH blocks: 09-RESEARCH.md §Pattern 1 (@McpTool), §Pattern 3 (result records), §Pattern 4 (CallToolResult error shape — class path is Open Question 1, resolve at compile using Task 1's recorded SDK coordinates), §Pattern 5 (SecurityConfig), §"`.mcp.json` product server entry"
  </read_first>
  <behavior>
    - get_portfolio_summary (auth=alice): returns totalMarketValue/totalCostBasis/total*Gain*/dailyChange* equal to PortfolioService.getPortfolioPnl(aliceId) and an allocation list matching getAllocation(aliceId). No stack trace ever in output.
    - get_risk_metrics (auth=alice): sharpeRatio≈GOLDEN_SHARPE(±0.001), annualizedVolatility≈GOLDEN_ANNUAL_VOL(±0.0001), maxDrawdown≈GOLDEN_MAX_DRAWDOWN(±0.0001), beta≈GOLDEN_BETA(±0.001), historicalVar95≈GOLDEN_HIST_VAR_AMOUNT(±0.01) sourced from the VarResultDto whose method=="HISTORICAL".
    - get_position_detail("AAPL", auth=alice): returns the single HoldingDto for AAPL from getHoldings(aliceId) (ticker/name/sector/quantity/avgCostBasis/currentPrice/currentMarketValue/portfolioWeight/unrealizedPnlAbs/unrealizedPnlPct). Ticker is sanitized (uppercase, alphanumeric + '.', max 10 chars) before use.
    - Forced internal exception (broken repository/service): the tool returns an MCP error result with isError true and content that does NOT contain "RuntimeException", "com.quantlens", "java.lang", or the sentinel secret string — the safe message is static.
    - POST /mcp with no Authorization header → HTTP 401 (auth gate fires before any tool dispatch).
  </behavior>
  <action>
    SECURITY CONFIG (two additive edits to SecurityConfig.filterChain — do NOT touch authorizeHttpRequests; `/mcp` already falls under .anyRequest().authenticated()):
      1. Add `AntPathRequestMatcher.antMatcher("/mcp")` as a third matcher inside the existing second `.ignoringRequestMatchers(...)` call (alongside the POST /api/ai/key and /api/ai/chat matchers), with a comment: MCP Streamable-HTTP POST cannot carry the XSRF-TOKEN cookie; exemption is safe because /mcp requires HTTP Basic.
      2. Insert `.httpBasic(basic -> basic.realmName("QuantLens MCP"))` after the `.csrf(...)` block and before `.exceptionHandling(...)`, with a comment that it coexists with formLogin (Vue keeps session cookies; MCP uses Basic).

    RESULT RECORDS (in com.quantlens.mcp.tools — concrete records, no interfaces, no Spring/JPA annotations, per RESEARCH §Pattern 3):
      - `PortfolioSummaryResult(BigDecimal totalMarketValue, BigDecimal totalCostBasis, BigDecimal totalUnrealizedGainAbs, BigDecimal totalUnrealizedGainPct, BigDecimal dailyChangeAbs, BigDecimal dailyChangePct, List<AllocationEntry> allocation)` plus nested `record AllocationEntry(String sector, BigDecimal weight, BigDecimal marketValue)`.
      - `RiskMetricsResult(double sharpeRatio, double annualizedVolatility, double maxDrawdown, double beta, BigDecimal historicalVar95, BigDecimal parametricVar95)`.
      - `PositionDetailResult(String ticker, String name, String sector, BigDecimal quantity, BigDecimal avgCostBasis, BigDecimal currentPrice, BigDecimal currentMarketValue, BigDecimal portfolioWeight, BigDecimal unrealizedPnlAbs, BigDecimal unrealizedPnlPct)`.

    PortfolioMcpTools (`@Component` in com.quantlens.mcp.tools — REQUIRED for annotation scanner per RESEARCH Pitfall 4): constructor-inject PortfolioService, RiskCalculator, PortfolioRepository, and ObjectMapper. Private `resolvePortfolioId()` pulls Authentication from `SecurityContextHolder.getContext().getAuthentication()` (no parameter injection in @McpTool methods), checks `isAuthenticated()`, and resolves via `portfolioRepository.findPortfolioIdByUsername(auth.getName())` (copy AnalyticsController:141-148 logic; on null/empty throw the module's `McpAuthException` or equivalent). Three methods annotated `@McpTool(name=..., description=...)` from `org.springframework.ai.mcp.annotation` with snake_case names get_portfolio_summary / get_risk_metrics / get_position_detail; get_position_detail takes a `@McpToolParam(description="Ticker symbol, e.g. AAPL", required=true) String ticker` and sanitizes it (uppercase, strip to alphanumeric + '.', cap 10 chars) before filtering getHoldings.

    Each method delegates to the service, maps the returned DTO into the corresponding *Result record (for risk: read the VarResultDto list, pick method=="HISTORICAL" → historicalVar95 and method=="PARAMETRIC" → parametricVar95), and wraps the ENTIRE body in try/catch. Return the MCP error shape per RESEARCH §Pattern 4: resolve the CallToolResult class path at compile time using the SDK jar coordinates recorded in Task 1 (expected `io.modelcontextprotocol...McpSchema.CallToolResult` or a Spring AI wrapper). If the builder/class is NOT available on the classpath, FALL BACK to the concrete-result-record approach: add a `String status` ("ok"/"error") + `String errorMessage` (nullable) field pair to each result record and return that — document the chosen approach in the SUMMARY. On catch: `log.error("<tool> failed (not forwarded to client)", e)` and return an error result whose content is a STATIC safe string (e.g. "Portfolio summary unavailable") — NEVER e.getMessage(), NEVER class names.

    RED TEST SCAFFOLDS:
      - `com.quantlens.mcp.PortfolioMcpToolsTest extends AbstractPostgresIntegrationTest`: @Autowired PortfolioMcpTools, AppUserRepository, PortfolioRepository. @BeforeEach resolves alice's portfolioId (findByUsername("alice") → findByUserId) AND sets `SecurityContextHolder.getContext().setAuthentication(new UsernamePasswordAuthenticationToken("alice", null, List.of()))`; @AfterEach `SecurityContextHolder.clearContext()`. Reuse the golden constants verbatim. Tests: getPortfolioSummary_returnsCorrectTotalValue (assert totalMarketValue equals getPortfolioPnl(aliceId).totalMarketValue via compareTo), getRiskMetrics_matchesGoldenValues (isCloseTo within tolerances), getPositionDetail_aapl_returnsCorrectHolding (ticker=="AAPL", fields match the AAPL HoldingDto), toolException_doesNotLeakStackTrace (force an internal failure — e.g. a tool variant or Mockito spy that makes the delegate throw RuntimeException("INTERNAL_DB_ERROR_secret") — assert isError true and content excludes "RuntimeException"/"com.quantlens"/the sentinel). Where the tool returns a CallToolResult, add a small `extractTextContent(result)` helper.
      - `com.quantlens.mcp.McpAuthIntegrationTest extends AbstractPostgresIntegrationTest`: @Autowired TestRestTemplate. `mcp_withoutAuth_returns401()` posts to `/mcp` with body `"{}"` and asserts `HttpStatus.UNAUTHORIZED`.

    .mcp.json: add a third `quantlens` server alongside context7 and project-db: `{ "type": "http", "url": "http://localhost:8080/mcp", "headers": { "Authorization": "Basic ${QUANTLENS_MCP_AUTH:-YWxpY2U6ZGVtbzEyMzQ=}" } }`. The base64 token decodes to alice:demo1234 (the seeded demo credential shown on the login screen — safe to commit; document as a demo credential in the SUMMARY).
  </action>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q -DskipTests test-compile && echo "TEST_COMPILE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - SecurityConfig contains both `AntPathRequestMatcher.antMatcher("/mcp")` (inside ignoringRequestMatchers) and `.httpBasic(` — confirm: `grep -c 'antMatcher("/mcp")' SecurityConfig.java` ≥ 1 AND `grep -c "httpBasic" SecurityConfig.java` ≥ 1.
    - PortfolioMcpTools.java contains exactly three `@McpTool` annotations with names get_portfolio_summary, get_risk_metrics, get_position_detail; `grep -v '^[[:space:]]*//' PortfolioMcpTools.java | grep -c "@McpTool"` returns 3.
    - PortfolioMcpTools imports `org.springframework.ai.mcp.annotation.McpTool` and resolves the principal via `SecurityContextHolder` (grep both present); no `e.getMessage()` appears in any catch block (grep absent).
    - Three result records exist in com.quantlens.mcp.tools and none `implements`/`extends` an interface.
    - Both test classes compile and extend AbstractPostgresIntegrationTest; PortfolioMcpToolsTest references GOLDEN_SHARPE and a no-stack-trace assertion; McpAuthIntegrationTest references `mcp_withoutAuth_returns401`.
    - .mcp.json parses as JSON and contains the `quantlens` entry with url `http://localhost:8080/mcp`: `node -e "const j=require('./.mcp.json'); if(!j.mcpServers.quantlens||j.mcpServers.quantlens.url!=='http://localhost:8080/mcp') process.exit(1)"` from repo root succeeds.
    - `./mvnw -q -DskipTests test-compile` succeeds (main + test sources compile; CallToolResult or fallback resolved).
  </acceptance_criteria>
  <done>SecurityConfig gates /mcp (httpBasic + CSRF exemption); three @McpTool beans delegate with principal resolution + static-message error hygiene; result records concrete; RED scaffolds and .mcp.json quantlens entry committed; test sources compile. CallToolResult-vs-fallback decision recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Boot + Modulith verify gate — prove /mcp registers, auth 401 fires, module boundary valid; record open-question resolutions</name>
  <files>backend/src/test/java/com/quantlens/QuantLensModulithTest.java, backend/src/test/java/com/quantlens/mcp/McpAuthIntegrationTest.java, .planning/phases/09-mcp-server/09-01-SUMMARY.md</files>
  <read_first>
    - backend/src/test/java/com/quantlens/QuantLensModulithTest.java (runs ApplicationModules.of(QuantLensApplication.class).verify())
    - backend/src/test/java/com/quantlens/mcp/McpAuthIntegrationTest.java (the 401 gate just authored)
    - 09-RESEARCH.md §Open Questions (1: CallToolResult class path; 2: annotation-scanner) and §Assumptions Log (A1-A3)
    - 09-VALIDATION.md "Wave 0 Requirements" (the open-question verification line)
  </read_first>
  <action>
    Run the targeted verification gate that proves the slice boots and the boundary holds, WITHOUT requiring a running MCP client (full protocol smoke is manual / a Wave-2 enhancement per RESEARCH). This is a verification-and-record task — no new production code unless a gate fails:
      - Run QuantLensModulithTest to confirm `ApplicationModules.verify()` passes with the new mcp module and named interfaces. If it fails, return to Task 1's open-question resolution: correct the named-interface names / allowedDependencies until verify passes (without weakening the boundary), then re-run.
      - Run McpAuthIntegrationTest to confirm POST /mcp without auth returns 401 (this proves the app context boots with the starter, /mcp is mapped, and the security gate fires before the MCP handler).
    Record in 09-01-SUMMARY.md under "Open Questions Resolved":
      - A1/Open Q1: the actual CallToolResult class path used (or that the concrete-result-record fallback was taken) and the SDK jar coordinates.
      - A3/Open Q (SecurityContextHolder availability): note that auth resolution is exercised by PortfolioMcpToolsTest in Plan 09-02 (which calls beans directly with a mock context); if any boot-time issue surfaced, record it.
      - Open Q2: confirm annotation-scanner picked up the three tools at boot (no error) — note the observed behavior.
    Note: PortfolioMcpToolsTest correctness assertions (golden values) are expected RED here and are GREENed in Plan 09-02; do not implement them in this plan. This task gates only Modulith validity + the 401 boot proof.
  </action>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q test -Dtest=QuantLensModulithTest,McpAuthIntegrationTest && echo "WAVE0_GATE_GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `QuantLensModulithTest` passes — ApplicationModules.verify() accepts the mcp module + its declared named-interface dependencies (boundary is valid, no cycles, no illegal references).
    - `McpAuthIntegrationTest#mcp_withoutAuth_returns401` passes — the application context boots with the MCP starter, `/mcp` is a live endpoint, and unauthenticated POST returns 401.
    - 09-01-SUMMARY.md exists with an "Open Questions Resolved" section recording: final allowedDependencies + named interfaces, CallToolResult class path (or fallback chosen), annotation-scanner boot behavior, and that .mcp.json uses the demo credential alice:demo1234.
  </acceptance_criteria>
  <done>Modulith boundary verified, /mcp boots and is auth-gated (401), and all phase open questions are resolved-and-recorded in the SUMMARY. Tool-correctness greening is deferred to Plan 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Claude Code) → /mcp over HTTP | Untrusted machine-client requests cross here; must present HTTP Basic credentials |
| @McpTool method → portfolio/analytics services | Principal-derived portfolioId crosses here; no client-supplied portfolio/user id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of privilege | POST /mcp (unauthenticated access) | mitigate | `/mcp` falls under `.anyRequest().authenticated()`; `.httpBasic()` added; McpAuthIntegrationTest asserts 401 without credentials (success criterion 2) |
| T-09-02 | Elevation of privilege (IDOR) | resolvePortfolioId in PortfolioMcpTools | mitigate | portfolioId derived ONLY from `SecurityContextHolder` principal via findPortfolioIdByUsername; NO portfolio/user id accepted as a tool param (ticker identifies a security, not a user) |
| T-09-03 | Information disclosure | @McpTool error responses | mitigate | every tool body wrapped in try/catch; log full exception internally; return CallToolResult(isError=true) with a STATIC safe string — never e.getMessage()/class names. PortfolioMcpToolsTest#toolException_doesNotLeakStackTrace asserts no markers (greened in 09-02; scaffold here) |
| T-09-04 | Tampering | CSRF on /mcp | accept (documented) | `/mcp` exempt from CSRF because the MCP client cannot replay the XSRF-TOKEN cookie; exemption is safe ONLY because HTTP Basic auth is required — this is not a relaxation, the endpoint stays authenticated |
| T-09-05 | Tampering | ticker param injection (get_position_detail) | mitigate | sanitize ticker: uppercase, alphanumeric + '.', max 10 chars before passing to getHoldings filtering; DB filtering is parameterized JPA |
| T-09-06 | Information disclosure | .mcp.json committed credential | accept (documented) | base64 token decodes to alice:demo1234 — the seeded demo password already shown on the login screen and in README; not a secret. Overridable via QUANTLENS_MCP_AUTH env var |
| T-09-SC | Tampering | spring-ai-starter-mcp-server-webmvc install | mitigate | RESEARCH Package Legitimacy Audit: same official `org.springframework.ai` group as existing pinned starters → Approved, no [ASSUMED]/[SUS] checkpoint required |
</threat_model>

<verification>
- `./mvnw -q -DskipTests compile` and `test-compile` succeed (Tasks 1-2).
- `./mvnw test -Dtest=QuantLensModulithTest,McpAuthIntegrationTest` green (Task 3): module boundary valid + /mcp boots auth-gated.
- `dependency:tree` shows `spring-ai-starter-mcp-server-webmvc`.
- `.mcp.json` parses and contains the quantlens entry at http://localhost:8080/mcp.
- PortfolioMcpToolsTest golden-value assertions are intentionally RED at end of this plan (greened in 09-02).
</verification>

<success_criteria>
- spring-ai-starter-mcp-server-webmvc on the classpath; `/mcp` registered on port 8080; ApplicationModules.verify() passes with the mcp module.
- Three @McpTool beans (get_portfolio_summary/get_risk_metrics/get_position_detail) delegate to PortfolioService + RiskCalculator (no recompute), resolve principal via SecurityContextHolder, and use static-message error hygiene.
- SecurityConfig gates /mcp (httpBasic + CSRF exemption); POST /mcp without auth → 401.
- .mcp.json quantlens product entry committed; RED scaffolds for tool correctness + error hygiene in place.
- All phase open questions resolved at compile/boot time and recorded in 09-01-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/09-mcp-server/09-01-SUMMARY.md` when done — include an "Open Questions Resolved" section (CallToolResult class path or fallback, final Modulith named interfaces + allowedDependencies, annotation-scanner boot behavior, .mcp.json demo credential note).
</output>
