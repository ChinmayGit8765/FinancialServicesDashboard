---
phase: 09-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java
  - backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java
  - README.md
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "get_portfolio_summary returns a total market value equal to PortfolioService.getPortfolioPnl for the authenticated user (seeded alice)"
    - "get_risk_metrics returns Sharpe/vol/drawdown/beta/historical-VaR matching the RiskCalculator golden values"
    - "get_position_detail(AAPL) returns AAPL's correct holding detail for the authenticated user"
    - "A forced internal tool failure produces an MCP error result containing NO stack-trace markers (no exception class names, no internal message)"
    - "The full backend test suite passes (no regressions from the MCP additions)"
    - "The README documents how an MCP client (Claude Code) connects to /mcp, the auth required, and the catalog of the three tools"
  artifacts:
    - path: "backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java"
      provides: "GREEN tool-correctness + error-hygiene assertions against golden/service values"
      contains: "isCloseTo"
    - path: "README.md"
      provides: "Product MCP Server section: connect steps, auth, tool catalog (MCP-02)"
      contains: "get_portfolio_summary"
  key_links:
    - from: "backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java"
      to: "com.quantlens.mcp.tools.PortfolioMcpTools"
      via: "direct bean call with mock SecurityContext (alice)"
      pattern: "tools\\.get(PortfolioSummary|RiskMetrics|PositionDetail)"
    - from: "README.md"
      to: ".mcp.json quantlens entry"
      via: "documented connect instructions referencing http://localhost:8080/mcp"
      pattern: "/mcp"
---

<objective>
Turn the RED scaffolds from Plan 09-01 GREEN: prove each `@McpTool` returns correct seeded/golden values and that forced failures never leak stack traces, keep the full backend suite green, and write the README Product MCP Server section (connect steps + auth + tool catalog) that completes MCP-02.

Purpose: Verify the MCP tools are genuinely correct (delegating to golden-tested services, not recomputing) and make the server usable by any MCP client via clear documentation. This is the credibility + usability half of the phase.

Output: GREEN PortfolioMcpToolsTest (correctness + error hygiene), full backend suite green, and a README MCP section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Backend build commands (Windows host — JDK 21 REQUIRED before Maven):
- PowerShell: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"` then `cd backend; .\mvnw.cmd ...`
- Bash tool: `JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw ...`
- Do NOT pass `-o`/offline.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mcp-server/09-RESEARCH.md
@.planning/phases/09-mcp-server/09-PATTERNS.md
@.planning/phases/09-mcp-server/09-VALIDATION.md
@.planning/phases/09-mcp-server/09-01-SUMMARY.md

<interfaces>
<!-- The bean under test + the golden anchors. Extracted from the codebase / Plan 09-01 output. -->

UNDER TEST — PortfolioMcpTools (com.quantlens.mcp.tools.PortfolioMcpTools, @Component):
- get_portfolio_summary method (no args) → PortfolioSummaryResult or CallToolResult (see 09-01-SUMMARY for the return-shape decision: CallToolResult vs concrete-record fallback)
- get_risk_metrics method (no args)
- get_position_detail method (String ticker)
- Resolves principal from SecurityContextHolder; delegates to PortfolioService + RiskCalculator.

GOLDEN ANCHORS (seed=42, alice Growth Portfolio — copy verbatim from RiskCalculatorTest.java):
- GOLDEN_SHARPE = 0.36442669 (±0.001)
- GOLDEN_ANNUAL_VOL = 0.34621361 (±0.0001)
- GOLDEN_MAX_DRAWDOWN = -0.33891522 (±0.0001)
- GOLDEN_BETA = 1.94917921 (±0.001)
- GOLDEN_HIST_VAR_AMOUNT = 1464.52 (±0.01)

SERVICE ANCHORS (assert against these directly for non-risk tools — no hardcoded magic numbers):
- PortfolioService.getPortfolioPnl(aliceId).totalMarketValue() — compare get_portfolio_summary.totalMarketValue with compareTo (BigDecimal).
- PortfolioService.getHoldings(aliceId) filtered to ticker "AAPL" — compare get_position_detail("AAPL") fields.

TEST PATTERN (from 09-01 scaffold + RiskCalculatorTest):
- extends AbstractPostgresIntegrationTest (RANDOM_PORT, ActiveProfiles("test"), pgvector container).
- @BeforeEach: resolve alice portfolioId (appUserRepository.findByUsername("alice") → portfolioRepository.findByUserId) AND set SecurityContextHolder authentication to UsernamePasswordAuthenticationToken("alice", null, List.of()).
- @AfterEach: SecurityContextHolder.clearContext().
- Golden assertions: assertThat(value).isCloseTo(GOLDEN_*, within(tol)) (org.assertj).
- If tools return CallToolResult: add extractTextContent(result) and ObjectMapper-deserialize the JSON into the *Result record before asserting; if the concrete-record fallback was chosen in 09-01, assert record fields directly. The 09-01-SUMMARY states which.

ERROR-HYGIENE TARGET: force the delegate to throw RuntimeException("INTERNAL_DB_ERROR_secret") (Mockito spy/mock on PortfolioService or PortfolioRepository, or a deliberately broken-dependency tool instance) → assert isError true AND content excludes "RuntimeException", "com.quantlens", "java.lang", and "INTERNAL_DB_ERROR_secret".

README anchors: existing headings include "## Dev MCP Servers (DEVX-01)" (line ~125), "## Demo Personas" (alice/bob/charlie, demo1234), "## Architecture". Add the NEW product-server section distinctly (do not merge into the dev-MCP section).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Green PortfolioMcpToolsTest — tool correctness against golden/service values + error hygiene; full suite green</name>
  <files>backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java, backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java</files>
  <read_first>
    - backend/src/test/java/com/quantlens/mcp/PortfolioMcpToolsTest.java (the RED scaffold from 09-01)
    - backend/src/main/java/com/quantlens/mcp/tools/PortfolioMcpTools.java (confirm the return shape decided in 09-01)
    - .planning/phases/09-mcp-server/09-01-SUMMARY.md ("Open Questions Resolved" — CallToolResult vs fallback, exact return type per tool)
    - backend/src/test/java/com/quantlens/analytics/RiskCalculatorTest.java (golden constants + alice resolution + isCloseTo/within usage)
    - backend/src/main/java/com/quantlens/portfolio/service/PortfolioService.java (getPortfolioPnl / getHoldings field semantics for the service-anchor assertions)
    - PATTERN: 09-PATTERNS.md §PortfolioMcpToolsTest (SecurityContext mock, golden assertion, error-hygiene test), §Shared Patterns (AbstractPostgresIntegrationTest inheritance)
    - RESEARCH blocks: 09-RESEARCH.md §Test Strategy Details (tool-correctness, error-hygiene), §Validation Architecture
  </read_first>
  <behavior>
    - getPortfolioSummary_returnsCorrectTotalValue: result.totalMarketValue compareTo getPortfolioPnl(aliceId).totalMarketValue() == 0.
    - getRiskMetrics_matchesGoldenValues: sharpeRatio≈GOLDEN_SHARPE(±0.001), annualizedVolatility≈GOLDEN_ANNUAL_VOL(±0.0001), maxDrawdown≈GOLDEN_MAX_DRAWDOWN(±0.0001), beta≈GOLDEN_BETA(±0.001), historicalVar95.doubleValue()≈GOLDEN_HIST_VAR_AMOUNT(±0.01).
    - getPositionDetail_aapl_returnsCorrectHolding: ticker=="AAPL"; quantity/avgCostBasis/currentMarketValue/unrealizedPnlAbs match the AAPL HoldingDto from getHoldings(aliceId).
    - toolException_doesNotLeakStackTrace: forced internal failure → isError true; content contains none of "RuntimeException", "com.quantlens", "java.lang", "INTERNAL_DB_ERROR_secret".
  </behavior>
  <action>
    Implement the four test methods in PortfolioMcpToolsTest so they assert real values (replace any RED placeholders/TODOs from the 09-01 scaffold). Resolve alice's portfolioId in @BeforeEach and set the mock SecurityContext as in the scaffold; call the tool beans directly (no MCP protocol). For correctness:
      - portfolio summary: assert totalMarketValue against the live PortfolioService.getPortfolioPnl(aliceId) value (service-anchored, not a hardcoded number), and assert the allocation list is non-empty.
      - risk metrics: assert all five golden values with the documented tolerances.
      - position detail: call getPositionDetail("AAPL"), assert ticker/name/sector and the numeric fields against the matching HoldingDto from getHoldings(aliceId).
    Unwrap per the 09-01 return-shape decision: if CallToolResult, extract the text content and ObjectMapper-deserialize into the *Result record; if the concrete-record fallback was used, assert fields directly. The 09-01-SUMMARY states which.
    For error hygiene: construct the failure per 09-01's design — if PortfolioMcpTools is testable with an injected mock (Mockito @MockBean on PortfolioService/PortfolioRepository that throws RuntimeException("INTERNAL_DB_ERROR_secret")), use that; otherwise use the broken-dependency tool variant the 09-01 scaffold exposed. Assert isError true and the content excludes all leak markers.
    If a correctness assertion reveals a mapping bug in PortfolioMcpTools (e.g. wrong VarResultDto entry picked, ticker filter mismatch), fix PortfolioMcpTools.java — keep the static-message error hygiene intact and do NOT change service math.
    Then run the FULL backend suite and confirm no regressions; fix any MCP-introduced failure (do not touch unrelated phases' tests).
  </action>
  <verify>
    <automated>cd backend && JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot" ./mvnw -q test -Dtest=PortfolioMcpToolsTest,McpAuthIntegrationTest && echo "MCP_TESTS_GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw test -Dtest=PortfolioMcpToolsTest,McpAuthIntegrationTest` passes (all four correctness/hygiene tests + the 401 gate green).
    - getRiskMetrics assertion uses the golden constants with the documented tolerances (grep GOLDEN_SHARPE, GOLDEN_BETA, isCloseTo).
    - getPortfolioSummary asserts against the live PortfolioService value (not a hardcoded literal) — the test contains a getPortfolioPnl/getHoldings call.
    - toolException test asserts isError true and `.doesNotContain("RuntimeException")` / `"com.quantlens"` / the sentinel.
    - Full suite green: `./mvnw verify` (or `./mvnw test`) completes with BUILD SUCCESS — record the test count in the SUMMARY.
  </acceptance_criteria>
  <done>All three tools proven correct against golden/service values, error hygiene proven (no stack-trace markers), and the full backend suite is green with no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: README Product MCP Server section — connect steps, auth, tool catalog (MCP-02)</name>
  <files>README.md</files>
  <read_first>
    - README.md (headings: "## Dev MCP Servers (DEVX-01)" ~line 125, "## Demo Personas", "## Architecture" — place the new section near the dev-MCP one but clearly distinct as the PRODUCT server)
    - .mcp.json (the committed quantlens entry: type http, url http://localhost:8080/mcp, Basic ${QUANTLENS_MCP_AUTH:-YWxpY2U6ZGVtbzEyMzQ=})
    - .planning/phases/09-mcp-server/09-01-SUMMARY.md (final tool names/params/return fields to document accurately)
    - PATTERN: 09-PATTERNS.md §.mcp.json (the quantlens entry shape), §SecurityConfig (auth model to describe)
    - RESEARCH blocks: 09-RESEARCH.md §"`.mcp.json` product server entry" (notes on type:http alias, base64 token = alice:demo1234, env override), §System Architecture Diagram
  </read_first>
  <action>
    Add a new top-level README section titled "## Product MCP Server (MCP-01, MCP-02)" (distinct from the existing "Dev MCP Servers (DEVX-01)" section, which is for dev tooling — make the distinction explicit in one sentence). Document:
      - What it is: QuantLens exposes its portfolio analytics as a product MCP server over Streamable HTTP at `http://localhost:8080/mcp`, embedded in the same Spring Boot app (port 8080). Mention it is the freshest 2026 integration signal.
      - How to connect with Claude Code: the committed root `.mcp.json` already contains a `quantlens` server entry; bring the stack up with `docker compose up`, then verify with `claude mcp get quantlens` and the `/mcp` command inside a Claude Code session. State that any MCP client supporting Streamable HTTP can connect using the same url + Authorization header.
      - Auth: `/mcp` requires authentication (HTTP Basic). The `.mcp.json` default `Authorization: Basic ${QUANTLENS_MCP_AUTH:-YWxpY2U6ZGVtbzEyMzQ=}` decodes to `alice:demo1234` — the seeded demo credential shown on the login screen (safe to commit; override via the `QUANTLENS_MCP_AUTH` env var with a base64 `username:password`). Note that unauthenticated requests return 401.
      - Tool catalog (a markdown table): `get_portfolio_summary` (no params → total market value, cost basis, unrealized P&L, daily change, sector allocation for the authenticated user), `get_risk_metrics` (no params → Sharpe, annualized volatility, max drawdown, beta vs SPX500, 95% historical & parametric VaR), `get_position_detail` (param `ticker`, e.g. AAPL → that holding's quantity, cost basis, current price, market value, weight, unrealized P&L). State that all values are computed from the seeded data by the same golden-tested PortfolioService + RiskCalculator the dashboard uses (no recompute) and that the portfolio is scoped to the authenticated user's principal (IDOR-safe).
    Keep the wording accurate to the implemented tools per the 09-01 SUMMARY (names, the single ticker param). Do NOT invent additional tools or params.
  </action>
  <verify>
    <automated>cd "C:/bullshitprojects/FinanceDash/FinancialServicesDashboard" && grep -q "Product MCP Server" README.md && grep -q "get_portfolio_summary" README.md && grep -q "get_risk_metrics" README.md && grep -q "get_position_detail" README.md && grep -q "http://localhost:8080/mcp" README.md && echo "README_MCP_OK"</automated>
  </verify>
  <acceptance_criteria>
    - README.md has a "## Product MCP Server (MCP-01, MCP-02)" section, distinct from the dev-MCP section.
    - The section documents the connect steps (docker compose up + claude mcp get quantlens + /mcp), the HTTP Basic auth (alice:demo1234 demo credential, QUANTLENS_MCP_AUTH override, 401 when unauthenticated), and a tool-catalog table naming get_portfolio_summary, get_risk_metrics, get_position_detail with their params/outputs.
    - The verify grep (`Product MCP Server` + three tool names + the /mcp url) passes.
    - Tool names/params in the README match the implemented tools (no invented tools).
  </acceptance_criteria>
  <done>README has an accurate Product MCP Server section covering connection, auth, and the three-tool catalog — completing MCP-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Claude Code) → /mcp over HTTP | Documented connect path; client must present HTTP Basic credentials |
| @McpTool method → portfolio/analytics services | Principal-derived portfolioId; verified by direct-bean tests with a mock alice context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information disclosure | @McpTool error responses | mitigate | PortfolioMcpToolsTest#toolException_doesNotLeakStackTrace asserts the forced-failure result is isError with NO "RuntimeException"/"com.quantlens"/"java.lang"/sentinel markers — proving the static-message hygiene from 09-01 holds |
| T-09-07 | Information disclosure | README credential documentation | accept (documented) | README states the .mcp.json token is the demo credential alice:demo1234 (already on the login screen), overridable via QUANTLENS_MCP_AUTH — not a leaked secret |
| T-09-08 | Tampering | README tool catalog accuracy | mitigate | tool names/params documented MUST match the implemented @McpTool beans per 09-01-SUMMARY; no invented tools/params that would mislead a client into malformed calls |
| T-09-01 | Elevation of privilege | POST /mcp auth gate (regression) | mitigate | McpAuthIntegrationTest#mcp_withoutAuth_returns401 re-run in Task 1 confirms the auth gate still fires after greening |
</threat_model>

<verification>
- `./mvnw test -Dtest=PortfolioMcpToolsTest,McpAuthIntegrationTest` green (tool correctness + error hygiene + 401 gate).
- `./mvnw verify` (full backend suite) BUILD SUCCESS — no regressions; test count recorded in SUMMARY.
- README grep: "Product MCP Server" + get_portfolio_summary + get_risk_metrics + get_position_detail + http://localhost:8080/mcp all present.
</verification>

<success_criteria>
- All three @McpTool methods proven correct against the golden-tested services (portfolio summary total = service value; risk metrics = golden Sharpe/vol/drawdown/beta/VaR; position detail = AAPL holding).
- Forced internal failure returns an MCP error with no stack-trace markers (error hygiene proven).
- Full backend test suite green (no regressions from the MCP module).
- README documents how to connect an MCP client to /mcp, the HTTP Basic auth, and the three-tool catalog (MCP-02 complete).
</success_criteria>

<output>
Create `.planning/phases/09-mcp-server/09-02-SUMMARY.md` when done — record the full-suite test count, the return-shape used (CallToolResult vs fallback), and confirmation that MCP-01 + MCP-02 success criteria are met.
</output>
