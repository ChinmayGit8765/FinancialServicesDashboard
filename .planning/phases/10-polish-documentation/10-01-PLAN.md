---
phase: 10-polish-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/java/com/quantlens/config/OpenApiConfig.java
  - backend/src/main/java/com/quantlens/security/config/SecurityConfig.java
  - backend/src/test/java/com/quantlens/OpenApiDocsIntegrationTest.java
  - backend/src/test/java/com/quantlens/QuantLensModulithTest.java
  - README.md
  - docs/screenshots/.gitkeep
  - frontend/src/components/ai/StructuredOutputChart.vue
autonomous: true
requirements: [DOCS-01]
user_setup: []

must_haves:
  truths:
    - "GET /v3/api-docs returns 200 without authentication"
    - "The OpenAPI spec documents portfolio, analytics, and AI endpoints"
    - "The OpenAPI spec exposes no LLM/API key field names (no secret leak)"
    - "ApplicationModules.verify() passes as a living module-boundary contract"
    - "The Modulith Documenter renders a module diagram + canvases headlessly without error"
    - "README has a Screenshots section + Capture Guide referencing the BYO-key live path"
    - "StructuredOutputChart.vue no longer carries a stale DEMO STUB / Phase 8 forward reference"
  artifacts:
    - path: "backend/src/main/java/com/quantlens/config/OpenApiConfig.java"
      provides: "OpenAPI metadata bean (title/description/version + auth scheme notes)"
      contains: "OpenAPI quantLensOpenApi"
    - path: "backend/src/test/java/com/quantlens/OpenApiDocsIntegrationTest.java"
      provides: "Paths-present + publicly-accessible + no-key-leak integration assertions"
      contains: "extends AbstractPostgresIntegrationTest"
    - path: "backend/src/test/java/com/quantlens/QuantLensModulithTest.java"
      provides: "verify() contract + Documenter diagram generation"
      contains: "generatesModuleDocumentation"
    - path: "docs/screenshots/.gitkeep"
      provides: "Committed location for human-captured live-mode screenshots"
    - path: "README.md"
      provides: "Screenshots section + Capture Guide + API/Architecture docs pointers"
      contains: "## Screenshots"
  key_links:
    - from: "backend/src/main/java/com/quantlens/security/config/SecurityConfig.java"
      to: "/v3/api-docs"
      via: "additive permitAll requestMatchers before anyRequest().authenticated()"
      pattern: "/v3/api-docs"
    - from: "backend/pom.xml"
      to: "springdoc-openapi-starter-webmvc-ui"
      via: "explicit-version dependency (no BOM)"
      pattern: "springdoc-openapi-starter-webmvc-ui"
    - from: "backend/src/test/java/com/quantlens/QuantLensModulithTest.java"
      to: "target/spring-modulith-docs"
      via: "new Documenter(MODULES).writeModulesAsPlantUml().writeModuleCanvases()"
      pattern: "Documenter"
---

<objective>
Make the repository read as senior-engineer work and close the demo loop — the final phase. Deliver three cohesive slices: (1) a live, leak-free OpenAPI spec via springdoc (DOCS-01 / SC-2); (2) the Spring Modulith boundary contract promoted to a diagram-generating living artifact (SC-3); (3) a screenshot-ready, docs-pointered README plus cosmetic cleanup of the stale StructuredOutputChart.vue comment (DOCS-01 / SC-1).

Purpose: The generated+verified module diagram, complete OpenAPI spec, and screenshot-rich README are the three senior-engineer signals in the phase goal. The live-mode screenshot CAPTURE itself is a human step (the user's own LLM key) — this plan lays down everything around it (placeholders, guide, structure) and lets the capture land in 10-HUMAN-UAT.md.

Output: One new dependency, one new @Configuration bean, an additive SecurityConfig permit, one new integration test, one extended Modulith test, an additive README, a screenshots placeholder, and a Vue comment fix. No new business logic, no DB changes, no new Modulith modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-documentation/10-CONTEXT.md
@.planning/phases/10-polish-documentation/10-RESEARCH.md
@.planning/phases/10-polish-documentation/10-PATTERNS.md
@.planning/phases/10-polish-documentation/10-VALIDATION.md

# Source files the executor edits or models against
@backend/pom.xml
@backend/src/main/java/com/quantlens/security/config/SecurityConfig.java
@backend/src/test/java/com/quantlens/QuantLensModulithTest.java
@backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java
@backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java
@frontend/src/components/ai/StructuredOutputChart.vue
@README.md

<build_env>
Windows host. Set JAVA_HOME to Temurin 21 before every backend build:
- PowerShell: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"` then `.\mvnw.cmd ...`
- Do NOT pass `-o`/offline — springdoc 2.8.17 must download from Maven Central on first build.
- Frontend: `cd frontend; npm run test`.
</build_env>

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

springdoc OpenAPI bean is built from `io.swagger.v3.oas.models.*` types (transitive via springdoc-openapi-starter-webmvc-ui). Key imports:
  io.swagger.v3.oas.models.OpenAPI
  io.swagger.v3.oas.models.Components
  io.swagger.v3.oas.models.info.Info
  io.swagger.v3.oas.models.security.SecurityRequirement
  io.swagger.v3.oas.models.security.SecurityScheme

Existing SecurityConfig.filterChain permitAll block (the EXACT text to extend, lines 87-92):
  .requestMatchers("/api/auth/login", "/api/auth/logout", "/api/auth/personas", "/actuator/health").permitAll()
  .anyRequest().authenticated()

QuantLensModulithTest is a plain JUnit class (NO @SpringBootTest, NO DB). Existing single method:
  applicationModulesShouldBeValid() → ApplicationModules.of(QuantLensApplication.class).verify()
Documenter lives at org.springframework.modulith.docs.Documenter (already on test classpath via spring-modulith-starter-test, BOM 1.4.11).

Integration test base: AbstractPostgresIntegrationTest provides @SpringBootTest(RANDOM_PORT) + @ActiveProfiles("test") + Testcontainers Postgres + auto-configured TestRestTemplate. Inject via `@Autowired private TestRestTemplate restTemplate;`. Model the no-key-leak assertion style on ai/KeyLeakageIntegrationTest (GET then `.doesNotContain(...)`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAPI spec — springdoc dep + OpenApiConfig bean + SecurityConfig permit + RED→GREEN integration test</name>
  <files>backend/pom.xml, backend/src/main/java/com/quantlens/config/OpenApiConfig.java, backend/src/main/java/com/quantlens/security/config/SecurityConfig.java, backend/src/test/java/com/quantlens/OpenApiDocsIntegrationTest.java</files>
  <read_first>
    - @backend/pom.xml (dependency-block comment style; the spring-modulith-starter-core block to insert AFTER)
    - @backend/src/main/java/com/quantlens/security/config/SecurityConfig.java (the exact permitAll requestMatchers block, lines 87-92)
    - @backend/src/test/java/com/quantlens/ai/KeyLeakageIntegrationTest.java (TestRestTemplate + doesNotContain assertion style)
    - @backend/src/test/java/com/quantlens/AbstractPostgresIntegrationTest.java (base class — RANDOM_PORT, TestRestTemplate)
    - @.planning/phases/10-polish-documentation/10-PATTERNS.md (OpenApiConfig + SecurityConfig + OpenApiDocsIntegrationTest analogs)
  </read_first>
  <behavior>
    - Test apiDocs_isPubliclyAccessible: unauthenticated GET /v3/api-docs returns 200 (permitAll active; no auth header).
    - Test apiDocs_exposesPortfolioAnalyticsAiPaths: response body contains "/api/portfolio", "/api/portfolio/risk", and "/api/ai/".
    - Test apiDocs_doesNotLeakKeyFields: response body does NOT contain "apiKey", "llmKey", or "password" as schema field names.
  </behavior>
  <action>
    Add the springdoc dependency to backend/pom.xml: groupId org.springdoc, artifactId springdoc-openapi-starter-webmvc-ui, version 2.8.17 EXPLICIT (no BOM manages springdoc — version is mandatory; omitting it breaks the build per RESEARCH Pitfall 1). Insert it after the existing spring-modulith-starter-core block, with a Phase-10 comment matching the existing dependency-comment style. Do NOT add per-endpoint @Operation annotations — auto-discovery of @RestController beans is sufficient (per-endpoint docs are deferred to v2).

    Create OpenApiConfig.java in package com.quantlens.config (parallel to security.config). A single @Configuration class with one @Bean public OpenAPI quantLensOpenApi(): set Info title "QuantLens API", description "AI-augmented portfolio & market intelligence dashboard", version "1.0.0"; add two security schemes documenting the real auth surfaces — "session-cookie" (APIKEY, in COOKIE, name JSESSIONID, set by POST /api/auth/login) and "http-basic-mcp" (HTTP, scheme "basic", for /mcp machine-client auth); add a SecurityRequirement for "session-cookie". Javadoc must note that springdoc auto-discovers @RestController endpoints and that /mcp (@McpTool on a @Component, not @RestController) is automatically excluded — no @Hidden needed.

    Edit SecurityConfig: extend the EXISTING permitAll .requestMatchers(...) call (the one ending in "/actuator/health") with four additional path strings — "/v3/api-docs", "/v3/api-docs/**", "/swagger-ui.html", "/swagger-ui/**" — keeping the same indentation and placed BEFORE .anyRequest().authenticated(). Both the exact "/v3/api-docs" AND the "/v3/api-docs/**" wildcard are required (RESEARCH Pitfall 2 — the test hits the exact path). Add a one-line comment noting these are public demo API-doc paths. Do NOT add a CSRF exemption (springdoc endpoints are GET-only) and do NOT broaden any other matcher.

    Create OpenApiDocsIntegrationTest.java in package com.quantlens extending AbstractPostgresIntegrationTest, @Autowired TestRestTemplate. Implement the three <behavior> tests using unauthenticated restTemplate.getForEntity("/v3/api-docs", String.class). Use descriptive AssertJ .as(...) messages. The no-leak assertions are the T-10-01 schema-hygiene gate — keep them as hard assertions, not warnings.
  </action>
  <acceptance_criteria>
    - `grep -c 'springdoc-openapi-starter-webmvc-ui' backend/pom.xml` returns 1 and the block carries `<version>2.8.17</version>`.
    - OpenApiConfig.java exists with `@Bean public OpenAPI quantLensOpenApi(` and both "session-cookie" and "http-basic-mcp" scheme names present.
    - `grep -c '/v3/api-docs' backend/src/main/java/com/quantlens/security/config/SecurityConfig.java` returns >=2 (exact + wildcard), all inside the permitAll block before anyRequest().authenticated().
    - OpenApiDocsIntegrationTest defines apiDocs_isPubliclyAccessible, apiDocs_exposesPortfolioAnalyticsAiPaths, apiDocs_doesNotLeakKeyFields.
    - The three new tests pass against a real booted context.
  </acceptance_criteria>
  <verify>
    <automated>$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"; .\mvnw.cmd -q test -pl backend -Dtest=OpenApiDocsIntegrationTest</automated>
  </verify>
  <done>OpenApiDocsIntegrationTest is GREEN: /v3/api-docs returns 200 unauthenticated, documents portfolio/analytics/ai paths, and leaks no apiKey/llmKey/password field names.</done>
</task>

<task type="auto">
  <name>Task 2: Modulith living contract + headless diagram generation</name>
  <files>backend/src/test/java/com/quantlens/QuantLensModulithTest.java</files>
  <read_first>
    - @backend/src/test/java/com/quantlens/QuantLensModulithTest.java (existing single-method class — extend, do not replace)
    - @.planning/phases/10-polish-documentation/10-PATTERNS.md (QuantLensModulithTest additive-method analog + Documenter calls)
    - @.planning/phases/10-polish-documentation/10-RESEARCH.md (Pattern 3 + Pitfall 3 — no @SpringBootTest)
  </read_first>
  <action>
    Extend QuantLensModulithTest, keeping it a plain JUnit class — NO @SpringBootTest, NO @ActiveProfiles, NO Spring context (ApplicationModules uses classpath scanning; adding a context inflates runtime and pulls in a DB per RESEARCH Pitfall 3). Extract the modules to a private static final ApplicationModules MODULES = ApplicationModules.of(QuantLensApplication.class) field. Keep applicationModulesShouldBeValid() but have it call MODULES.verify() (it remains the living module-boundary contract). Add a second @Test method named generatesModuleDocumentation() that runs new Documenter(MODULES).writeModulesAsPlantUml().writeModuleCanvases().writeAggregatingDocument() — this writes PlantUML source + canvases + aggregated doc to target/spring-modulith-docs (auto-created; git-ignored under **/target/; no external Graphviz/PlantUML binary needed). The test asserts only that generation completes without throwing. Add the import org.springframework.modulith.docs.Documenter. Do NOT weaken any module boundary to make generation pass — verify() already passes (Phase 9).
  </action>
  <acceptance_criteria>
    - QuantLensModulithTest contains both applicationModulesShouldBeValid and generatesModuleDocumentation methods.
    - The class has NO @SpringBootTest / @ExtendWith(SpringExtension) annotation.
    - After the test runs, `backend/target/spring-modulith-docs/` contains at least one generated file (e.g. all-modules.puml or an aggregating .adoc).
  </acceptance_criteria>
  <verify>
    <automated>$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"; .\mvnw.cmd -q test -pl backend -Dtest=QuantLensModulithTest</automated>
  </verify>
  <done>Both Modulith test methods pass; the Documenter writes a module diagram + canvases to target/spring-modulith-docs without throwing.</done>
</task>

<task type="auto">
  <name>Task 3: README Screenshots + Docs pointers + StructuredOutputChart cleanup</name>
  <files>README.md, docs/screenshots/.gitkeep, frontend/src/components/ai/StructuredOutputChart.vue</files>
  <read_first>
    - @README.md (existing section structure — "## Stochastic Forecasting Models", "## Product MCP Server", "## OAuth Upgrade Path"; heading level + horizontal-rule divider + docs/ link style)
    - @frontend/src/components/ai/StructuredOutputChart.vue (stale header comment lines 2-11 "DEMO STUB"/"Phase 8 will…" + stale sr-only figcaption lines 122-125)
    - @.planning/phases/10-polish-documentation/10-PATTERNS.md (README additive-section + StructuredOutputChart corrected-comment analogs)
  </read_first>
  <action>
    README.md — additive only, no rewrite. Insert a "## Screenshots" section using the existing heading + horizontal-rule divider style. It contains: a one-line note that capture is a human step done in LIVE mode via the BYO-key popup; a table of placeholder image paths under docs/screenshots/ with captions for the key demo views (dashboard with all AI panels + fan-chart model selector, RAG Q&A chat with citations, structured-output chart, a Heston fan chart, the BYO-key popup); and a "### Capture Guide" with numbered steps (docker compose up → open the app → log in as alice/demo1234 → open the BYO-key popup → paste a real Anthropic or OpenAI key, session-only → AI mode badge flips DEMO→LIVE → screenshot each AI panel → cycle the fan-chart model selector → save PNGs to docs/screenshots/). Add an "## API & Architecture Docs" pointer section linking: OpenAPI spec at http://localhost:8080/v3/api-docs, Swagger UI at http://localhost:8080/swagger-ui.html, the generated Modulith diagram (regenerate with `.\mvnw.cmd test -pl backend -Dtest=QuantLensModulithTest` → backend/target/spring-modulith-docs/all-modules.puml), the stochastic-model rationale doc docs/MODELS.md, and the existing OAuth Upgrade Path section. Place these new sections after Cold-Start Verification / before or near Architecture — preserve all existing content.

    Create docs/screenshots/.gitkeep (empty file) so the placeholder directory is committed (target/ is git-ignored but docs/ is tracked).

    StructuredOutputChart.vue — replace the stale <script setup> header comment ("StructuredOutputChart — DEMO STUB only." + "Phase 8 (AI-06) will swap…") with an accurate present-tense description (mirror ExplainDrawer.vue's style): the component renders a horizontal bar chart from a StructuredChartDto; in demo mode DashboardView injects a seeded fixture via the ai store; in live mode (AI-06) the same store slot is populated by a live BeanOutputConverter-typed record from /api/ai/structured — same DTO shape, no chart changes; three states shimmer/error/chart. Also replace the stale sr-only <figcaption> ("Phase 8 will replace this demo fixture…") with an accurate screen-reader description of the demo↔live behavior. No logic, template structure, or styling changes beyond these two comment/caption edits.
  </action>
  <acceptance_criteria>
    - `grep -c '## Screenshots' README.md` returns 1 and a Capture Guide with the BYO-key live path is present.
    - README contains the OpenAPI/Swagger URLs and a docs/MODELS.md link in an API/Architecture docs pointer section.
    - docs/screenshots/.gitkeep exists and is tracked by git.
    - `grep -c 'DEMO STUB' frontend/src/components/ai/StructuredOutputChart.vue` returns 0 and `grep -c 'Phase 8' frontend/src/components/ai/StructuredOutputChart.vue` returns 0.
    - Frontend Vitest suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend; npm run test</automated>
  </verify>
  <done>README has Screenshots + Capture Guide + Docs pointers; docs/screenshots/.gitkeep is committed; StructuredOutputChart.vue header + figcaption are accurate (no DEMO STUB / Phase 8 reference); the 86-test Vue suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → /v3/api-docs, /swagger-ui | Public (permitAll) doc endpoints — intentionally open for a demo; must expose no secrets |
| Swagger UI → state-changing /api/** endpoints | UI renders the spec; any write still crosses the existing CSRF + auth filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | OpenAPI JSON schema (/v3/api-docs) | mitigate | OpenApiDocsIntegrationTest#apiDocs_doesNotLeakKeyFields hard-asserts the body contains no "apiKey", "llmKey", or "password" field name — extends the existing KeyLeakage hygiene to the schema level |
| T-10-02 | Elevation of Privilege | SecurityConfig permitAll scope | mitigate | permitAll is scoped to exactly /v3/api-docs, /v3/api-docs/**, /swagger-ui.html, /swagger-ui/** — added BEFORE .anyRequest().authenticated(); no other matcher broadened; all other /api/** + /mcp stay authenticated |
| T-10-03 | Tampering | Swagger UI invoking write endpoints | accept | Swagger UI only renders the spec; state-changing endpoints retain the existing CSRF filter + auth — no bypass introduced. Low risk |
| T-10-SC | Tampering | springdoc-openapi-starter-webmvc-ui:2.8.17 Maven install | mitigate | RESEARCH Package Legitimacy Audit verdict = Approved (official springdoc GitHub release co-released with Spring Boot 3.5.13). Maven dep, not npm — no [ASSUMED]/[SUS] checkpoint required |
</threat_model>

<verification>
Phase-level checks (run before /gsd:verify-work):
- Quick: `$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"; .\mvnw.cmd -q test -pl backend -Dtest=OpenApiDocsIntegrationTest,QuantLensModulithTest`
- Full backend: `$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"; .\mvnw.cmd verify` (≥215 existing + new tests green)
- Frontend: `cd frontend; npm run test` (86 tests green)
- File checks: `## Screenshots` present in README; `docs/screenshots/.gitkeep` tracked; no "DEMO STUB"/"Phase 8" in StructuredOutputChart.vue; `backend/target/spring-modulith-docs/` populated after the Modulith test.
Do NOT use `-o`/offline — springdoc must download on first build.
</verification>

<success_criteria>
- DOCS-01 / SC-2: GET /v3/api-docs returns 200 unauthenticated and documents portfolio, analytics, and AI endpoints (OpenApiDocsIntegrationTest GREEN).
- SC-2 security: the OpenAPI spec leaks no apiKey/llmKey/password field names.
- SC-3: ApplicationModules.verify() passes AND the Documenter renders a module diagram + canvases headlessly to target/spring-modulith-docs.
- DOCS-01 / SC-1: README has a Screenshots section + Capture Guide referencing the BYO-key live path, plus docs/screenshots/ placeholder and API/Architecture docs pointers. (Actual live-mode capture is a human UAT step — see 10-HUMAN-UAT.md.)
- Polish: StructuredOutputChart.vue carries no stale DEMO STUB / Phase 8 forward reference; frontend + backend suites stay green.
</success_criteria>

<output>
Create `.planning/phases/10-polish-documentation/10-01-SUMMARY.md` when done.
</output>
