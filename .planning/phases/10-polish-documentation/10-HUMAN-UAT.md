---
status: partial
phase: 10-polish-documentation
source: [10-VERIFICATION.md, 10-01-SUMMARY.md, 10-REVIEW.md]
started: 2026-06-10
updated: 2026-06-10
---

## Current Test

[awaiting human/live testing — all automatable gates pass: backend full suite green (219+),
frontend 86 green; OpenApiDocsIntegrationTest 3/3 (public 200 + paths + no leak incl. passwordHint/actuator);
QuantLensModulithTest 2/2 + components.puml generated; README Screenshots + Capture Guide + Docs pointers present;
code-review 6/6 fixed]

## Tests

The DOCS-01 / SC-1 deliverable is the live-mode screenshot capture — inherently a human step
(needs your own LLM key + visual capture). Everything around it is in place.

### 1. (live) Capture the demo screenshots
expected: `docker compose up` → http://localhost:5173 → log in as alice/demo1234 → open the BYO-key
popup → paste a real Anthropic or OpenAI key (session-only) → mode badge flips DEMO→LIVE → screenshot
each AI panel; cycle the fan-chart model selector (GBM→Merton→Heston→Bootstrap). Save PNGs to
`docs/screenshots/` using the filenames in the README table (dashboard.png, fan-chart.png, rag-qa.png,
structured-output.png, byo-key-popup.png). README images then render.
result: [pending]

### 2. (live) Swagger UI is usable
expected: http://localhost:8080/swagger-ui.html loads the interactive explorer; portfolio/analytics/AI
endpoints are listed; the BYO-key intake endpoint is NOT shown; login/personas are not falsely marked
as auth-required. `GET /v3/api-docs` returns the raw JSON.
result: [pending]

### 3. (spot-check) Module diagram renders
expected: open `backend/target/spring-modulith-docs/components.puml` (regenerated by QuantLensModulithTest)
in a PlantUML viewer → shows the module graph (ai → portfolio+analytics, analytics → portfolio,
mcp → portfolio+analytics, auth + config standalone). Optionally embed the rendered PNG in the README.
result: [pending]

### 4. (spot-check) No-leak holds live
expected: scan `GET /v3/api-docs` output → contains no apiKey VALUE, no AiKeyRequest schema, no
passwordHint, no /actuator path, no LLM/Finnhub token. (Proven by tests; confirm live.)
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

None blocking — the OpenAPI spec (public + leak-free), the Modulith living contract + generated diagram,
and the README screenshot scaffolding + Capture Guide are all in place and test-verified. The live-mode
screenshot capture (item 1) inherently needs the user's own LLM key — it is the documented payoff of the
BYO-key popup and the intended manual step for the README images.
